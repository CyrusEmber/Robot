# 工作集账本（提案）

**本文件是提案，不是常驻文档。** 通过并实施后：其内容移入 `work/active/<id>.md`，本文件删除；
在此之前也不进入任何默认读取链（`AGENTS.md` 不指向它）。这一点本身就是它的第一条规矩。

**问题（2026-09-20 实测）**：账本"增长被机械要求、收缩没有路径"——`PLAN.md` 45.5 KB/144 行里
两行占 22%（`#18` 4270 字符、`#27` 5785 字符）；`HARNESS.md` 挂账 `#3` 单行 1838 字符；
`ACCEPTANCE.md` 已达 229 KB/2063 行；同一摘要出现在 3 个文件里；`PLAN.md` 留 6 处
"原状态/仍未落地/未接通/曾"这类已作废措辞。新文件必须有 `FILEMAP` 行、版本必须有四件套（有闸门），
而"关掉一条"只能靠人手删。

**目标**：默认读取成本随**活跃工作**增长，不随**历史累积**增长。

## 1. 不变式

1. **现状覆盖更新**：活跃事项只写当前状态，不追加已作废状态。
2. **规则归机制**：参数归配置，口径归实现与测试，不变式归断言。
3. **关闭退出工作集**：剩余动作另立事项，过程与证据按需检索。
4. **事实不重复**：事项引用规则与运行记录，不复制数值、摘要与完整结论。

## 2. 文件结构与职责

```
work/active/<id>.md                             # 一个未完成事项 = 一份文件
work/closed/<year>/<id>.md                      # 关闭即离开默认集；文件本身是身份记录
rl_exp/versions/lizard/acceptance/records/<日期-对象-主题>.md   # 验收证据，扁平一层
rl_exp/tools/verify/check_work_docs.py          # 列举 / 定位 / 检查
```

不新增 `WORK.md`、`INDEX.md`、`ids.json`、`policy.json`。各文件职责：

| 文件 | 保留 | 移出 |
|---|---|---|
| `AGENTS.md` | 简短读取规则、发现命令、按需读取方式 | — |
| `PLAN.md` | 目标、路线、优先级原则 | 挂账清单 → `work/active/`；已关闭行 → `work/closed/` |
| `HARNESS.md` | 评测台必要说明 | 待办 → `work/active/`；版本历史/修订记录 → `records/`；具体规则 → 对应机制 |
| `FILEMAP.md` | 目录职责 | 逐事项/逐批次的登记行 |
| `ACCEPTANCE.md` | 迁移期旧路径说明（短） | 历史正文按批次 → `records/` |

**补 1（必须做，否则最大的一块没被治理）**：默认集里的大文件必须点名并入预算。`PLAN.md` 45.5 KB
与 `HARNESS.md` 26.8 KB 中"要保留"的部分仍在默认或任务级读取链上：先移出历史与挂账，再谈预算；
`HARNESS.md` 还被 skill `isaaclab-eval-harness` 指为 SSOT，其留存部分单独设上限。

## 3. 事项格式（一份文件，不拆提案/设计/任务）

共有：稳定 ID、标题、**所属范围**、当前状态、问题与本次范围、本次规则变更及落点。
活跃另有：下一步、关闭条件、**必要依赖**。关闭另有：结果或取消/替代理由、**证据引用**、必要的决策解释。

- 状态只用 `open / in_progress / blocked / done / cancelled / superseded`。
- 归档后不保留"下一步"；未完成动作必须转为活跃事项。
- 规则落点写 `路径#锚点`；**无规则变化时显式声明**，不要求空表。

**补 2 引用分型**（否则闸门要么太弱要么太强）：

- `depends_on:` 必须指向现行机制或活跃事项，**指向归档即报错**。
- `evidence:` 允许指向 `acceptance/records/<stem>`，只查存在性，默认不展开。

**补 3 范围受控**：`所属范围` 用短词表，常量写在检查器里，未知范围即报错（否则列表没人看得懂）。

**补 4 替代要成链**：`superseded` 必须有 `superseded_by`，目标能被解析（事项 ID 或机制落点）。

## 4. 验收记录（扁平，文件名即索引）

```
records/2026-09-20-lizard-v14-terrain-reproducibility.md   # Lizard v14：地形生成可重复性验收
records/2026-09-20-eval-tools-record-bindings.md           # Eval tools：记录绑定一致性验收
records/2026-09-20-runrecord-isolated-rebuild.md           # Runrecord：隔离重建验收
```

正文五节，二级标题固定：**适用范围 / 验收条件 / 结果 / 证据引用 / 未覆盖边界**。检查器只查"五节在不在"，
不查内容、不要求字段齐备（避免空表填充）。家族、版本只在相关时出现。发现方式：先筛文件名，再 `rg`
正文，不建 INDEX。文件名形状钉 `\d{4}-\d{2}-\d{2}-[a-z0-9-]+\.md`。

**补 10 文件名即身份 ⇒ 引用与改名上闸**：引用一律 `records/<stem>#<anchor>`；闸门查"每条 `evidence:`
都能解析到存在的文件"；**被引用过的记录不得改名或删除**（`git diff` 侧：改名必须在同一次变更里更新引用者）。

**补 11 同对象多份必须声明替代范围**（否则"最新即生效"，正是本方案否掉的那条）：
① 同一天同一对象 = **追加进同一份**（一份记录 = 一个批次），不建 `-2`；
② 不同日期的同对象记录，新那份的"适用范围"必须写明"替代 X 的哪部分、哪部分仍有效"（可选
`supersedes:` 指向存在的 stem）。

**补 13 迁移 `ACCEPTANCE.md` 时要在文件内部再分流一次**（229 KB 那份是最大风险源，它不是纯验收）：

| 原内容类型 | 去处 | 实例 |
|---|---|---|
| 验收证据（条件/结果/边界） | `records/<batch>.md` | 地形可重复性、双跑摘要 |
| **仍有效的约束** | **机制**（协议 yaml / 测试 / 断言） | "v1/v2/v3 不得混表"、"fall 用几何定义不用终止项" |
| 待决或未覆盖动作 | 活跃事项 | "归档位置替代需确认" |
| 决策解释（为什么这么选） | 随记录保留，或不留 | "为什么不再用 `--weights-only`" |

第二行是关键：这类约束现在只活在散文里，整份文件一旦降级为"按需检索"，它就从默认读取链消失。
**迁移不是搬家，是逐条判"它是规则还是过程"；规则先落机制，过程才搬家。**

## 5. 一个工具，三种能力

| 能力 | 行为 |
|---|---|
| 列举 | 默认只输出活跃事项的 ID、标题、状态、范围 |
| 定位 | 按 ID 返回文件位置；读历史需显式指定 |
| 检查 | 格式、身份、引用、目录状态、预算 |

不保存生成看板 ⇒ **取消生成块与逐字节重建要求**，改为验证"发现结果正确"。已知在办哪个事项就直接定位。
历史检索继续用 `rg`，暂不自建搜索系统。

**补 5 格式只定义一处**：schema 住在检查器 docstring 里，`AGENTS.md` 只指路；并加一条"格式只定义一处"
的看守（本仓 `check_*_single_source.py` 同款手法）。

## 6. 读取规则与预算

默认流程：读 `AGENTS.md` 规则 → 定位用户指定事项（未指定则列举活跃摘要）→ 读该事项与相关机制 →
只有追溯需要时才检索归档。实测用 `rg`、不要 glob `work/**`（会带出 `closed/`）。

三项预算分开（避免混淆"目录大小"与"实际上下文"）：

| 预算 | 内容 |
|---|---|
| 默认入口 | `AGENTS.md` + `PLAN.md` 留存部分 + 列举输出字节 |
| 活跃内容总量 | 防事项长期不关闭 |
| 单事项上限 | 防一个事项吸收旁支工作 |

初期阈值是检查器里的**具名常量**，不先建配置体系。变更必须有明确理由，**不得因超额自动提高**。
报错给出超额量与主要贡献文件。

**补 6 报错要点名补救集**：关闭 / 移出范围（cancel、supersede）/ 把散文搬进机制或记录。
**拆文件不算补救**（总量不变），也不应被当成默认动作——写进错误文案。

## 7. 关闭流程（一次变更内完成）

1. 更新机制或必要规范；2. 登记规则落点与验收证据；3. 剩余动作拆为独立事项；
4. 原事项移入 `closed/`；5. 跑检查。

活跃事项可保留历史证据 ID，默认不展开。**工作依赖解析到归档 = 报错**；必须长期遵守的约束要引用现行机制。
**关闭记录持续受落点检查**（后续重构删掉规则归宿也能抓住）。验收正文不采用"最后提到即生效"：
每次验收声明适用范围与证据；替代旧结论必须指明替代范围；初期不建全仓"当前结论总表"（避免新副本）。

## 8. 最小机械闸门（只实现能清楚反证的）

| # | 检查 | 破坏测试 |
|---|---|---|
| 1 | ID 唯一、目录与状态一致 | 重复 ID；把 `done` 放进 active |
| 2 | 必填字段与引用有效 | 删关闭依据；引用不存在目标 |
| 3 | 规则落点有效且位于现行机制 | 删锚点；把落点移进 `closed/` |
| 4 | 归档不得有活跃动作字段 | 给关闭项加"下一步" |
| 5 | 工作依赖不得指向归档 | 直接引用，或经 ID 间接引用 |
| 6 | 默认输出、活跃总量、单项预算 | 分别制造超额 |
| 7 | 发现结果正确 | 新增/关闭事项后列表变化正确 |
| 8 | **身份不丢**（补 7） | `git diff --diff-filter=D` 删掉 `closed/` 文件；改写既有 `id:` |
| 9 | **值不重复的窄代理**（补 8） | 事项正文出现 `sha256:` 摘要串或成块数值 |
| 10 | **FILEMAP 不是文件登记册**（补 14） | 加一行以具体文件名开头的表格行（只允许目录名、入口名） |
| 11 | **默认集不含那四份必读**（补 15） | 在 `AGENTS.md`/skill/事项里写"先读 FILEMAP/PLAN/HARNESS/FAMILY"；`AGENTS.md` 超上限 |

**补 7**：当前目录只能保证"当前唯一"，"永不复用 ID"由提交侧差异检查保证（本仓 `framework_pin_check`
同款手法：差异即判据）。**补 8**：不变式 4 只给这一条窄代理（摘要是最容易腐烂的复制品，套件一改就假）；
其余靠评审，不宣称保真。

落点：全部进 `hooks/pre-commit`（触发面 `work/**`、`ACCEPTANCE` 目录、`PLAN.md`、`AGENTS.md`、`FILEMAP.md`），
并作为一条进离线套件（`MAX_CHECKS` 46 → 47，棘轮跟着抬）。不单建 ID 登记册：关闭文件即身份记录。

## 9. 迁移与验收

**顺序不可颠倒**：闸门与工具先落地（可反证）→ 三事项试点 → 才动四份大文档。

**试点：三个事项**（一个关闭项、两个活跃项），核对原始归属与**真实状态**：

| 试点对象 | 形态 |
|---|---|
| `18b 地形证据`（已关闭） | 落点已存在：`foot_relief` docstring / 协议 v3 / 测试 |
| `#5 诊断 run 闸门`（活跃） | 尚无落点，将来要落（要落成断言） |
| `#6 --headless 弃用`（活跃） | 落点将是 `run_ablation.py` 自身 |

试点验收：活跃发现正确、关闭项默认消失；**已登记规则移进归档后必红**；引用/身份/预算破坏测试均被检出；
分别报告默认入口大小、活跃总量、单项最大值；**不把试点节省量当成全仓已节省**。

**补 9 迁移规则**（否则活跃集**在迁移那一刻爆掉**）：
① 原表里已是 ✅ 的行**直接进 `closed/`，不生成新事项**；② 一条行若含多个未完成动作，拆成 ≤3 个活跃事项；
③ **迁移清单本身是临时产物，不得留成新的长期文档**（可只活在提交信息里）。

批量迁移的顺序（按"分流压力"从小到大）：`HARNESS.md` → `PLAN.md` → `FILEMAP.md` → `ACCEPTANCE.md`。
四份文档的现行体积：45.5 KB / 26.8 KB / 82.8 KB / 229 KB。

迁移后二次验收：活跃总量 ≤ 预算；抽查若干关闭项，确认"规则有落点或明确取消"（人评审）；
旧纪律同步修改——`check_version_docs.py` 的"逐文件 FILEMAP 登记"取消，改为登记目录 + 发现规则；
版本四件套检查保留。

## 10. 明确不保证（写进方案，别让后人误会）

1. 规则无遗漏；
2. 迁移语义等价；
3. 关闭理由充分；
4. **AI 实际不读归档**；
5. 对象词表稳定（无 INDEX ⇒ `terrain-reproducibility` 与 `terrain-repro` 搜不到一起，闸门只能查形状）。

前四项需要评审，第五项需要命名习惯。闸门只能让"便宜路径 = 正确路径"，不能禁止模型去 `rg` 全仓。

## 11. 九份文档的单一职责（必须一起简化）

**原则**：每份文档只承担一种职责；**能从目录、代码或记录发现的信息，不再手工列一份**。
**文档变短与"减少默认必读文件"必须同时做**——只变短不减少默认读取，token 不降。

| 文档 | 保留 | 移出或删除 |
|---|---|---|
| `FILEMAP.md` | 主要目录职责、关键入口、不直观的布局关系 | 逐文件清单、版本状态、完成记录、验收结论 |
| 家族 `PLAN.md` | 当前目标、路线、优先级、尚有效的备选方向 | 挂账 → `work/active/`；已完成过程 → 归档 |
| `HARNESS.md` | 评测台职责、使用入口、关键兼容约束的**引用** | 待办 → active；历史修订 → 归档；实现细节回代码 |
| `FAMILY.md` | 家族身份、特有约束、必要的版本关系说明 | 重复参数、活跃待办、验收读数、可发现的版本清单 |
| `ACCEPTANCE.md` | 迁移期间的简短定位说明 | 正文 → 仓根 `acceptance/records/` |
| `ARCH_PLAN.md` | 仍待实施的架构目标与取舍 | 已实施部分的过程 → 归档；现行约束 → 落到机制 |
| `README.md` | 项目简介、安装运行入口、最短上手路径 | 项目进度、历史验收、详细内部文件地图 |
| `AGENTS.md` | 必须遵守的工作规则、读取顺序、检查入口 | 项目历史、当前状态、重复操作教程 |

三处谨慎处理（每处都有一条判据，否则会二次膨胀）：

1. **`FILEMAP.md` 改成"目录导航"，不再是文件登记册**：只登记 `work/` 的用途、
   `acceptance/records/` 的命名规则、主要代码目录。**新增事项、验收批次或配方版本不应要求改 FILEMAP**；
   相应的**逐版本 FILEMAP 登记闸门随之调整，版本四件套完整性检查继续保留**。
2. **`FAMILY.md` 的版本关系不许为了短而盲删**：目录可推导的不列；"为什么分出这条线""哪些版本不可比较"
   这类**不可推导**的信息仍有价值。已有结构化血统与生命周期记录（`versions\lines.json`、各版本 `base.json`、
   `versions\recipes.json`）时**引用它，不另维护同一份表**。
3. **`HARNESS.md` 与 `ARCH_PLAN.md` 不许把有效规则一起归档**：迁移前**逐段**判三类 ——
   **当前行为约束 → 机制；尚未实施的意图 → active；历史决策过程 → closed**。**不因一段很长就整体搬走；
   判不出类别的段落不许搬。** （注：`check_version_docs.py` 已禁 `ARCH_PLAN.md` 出现加粗状态词，
   所以"已实施/未实施"没有现成标记物，只能逐段判定；改完后这条闸门自然更容易满足。）

两份相邻文档的判据要写在文件头，否则一年后又混成一份：**`FAMILY` = 身份与不变量，`PLAN` = 当前意图与优先级**；
**`README` = 最短上手路径并指向 `FILEMAP`，不复制目录表**。

## 12. 默认读取集（省 token 的真正开关）

顺序：**短工作规则（`AGENTS.md`）→ 定位相关事项（`check_work_docs.py --list/--locate`）→ 读该事项与必要机制、文档。**

**不再默认读** `FILEMAP.md` / `PLAN.md` / `HARNESS.md` / `FAMILY.md`——它们只在相关任务里按需打开。落地两条：

- `AGENTS.md` 写入该顺序，并**给自身设上限**（它成为新入口 ⇒ 它最可能变成默认集里最大的一块）。
- 这四份的"不被默认读"**不得被别的文档重新打开**：skill、`README`、事项文件里不得再写"先读这四份"，
  `AGENTS.md` 也不把它们列为必读。

预算项随之定：默认入口 = `AGENTS.md` + 列举输出字节（`PLAN.md` 留存部分是否计入，取决于它是否**真的**
退出默认集——这一项随迁移结果定，不先写死）。

## 13. 本提案自身的处置

通过后：内容移入 `work/active/ledger-restructure.md`（首个活跃事项），本文件删除；
实施完成后该事项关闭，证据（试点与破坏测试的输出）进 `records/<日期>-ledger-restructure.md`。


