{
  "base": "flat-v0",
  "note": "the rough dev-state env (family rough_env_cfg) built on the family flat root: base.json names `flat-v0` because that root IS the wiring these four recipes share, so the declared difference is the rough delta alone -- and it reads the live dev yaml, so it carries no frozen version to compare against"
}
