{
  "format": 4,
  "recipe": "rough-v0@1",
  "line": "lizard/main",
  "note": "Hard B, lineage reading: base.json names this recipe's mother, so the declared difference is against that recipe -- not against the framework stock cfg, which would bury this version's own delta in everything its ancestors wrote. Env paths are grouped by who wrote them: the element set that touched the path (several elements can touch one path in sequence), a version-resolved component, or wiring -- the shared wiring reading this recipe's own document. params_version is filtered as identity: it says which recipe this is, and it is what makes the two recipes different at all.",
  "env": {
    "allowed": {
      "v0_rough_height_scanner": {
        "paths": [
          "observations.policy.height_scan",
          "scene.height_scanner"
        ],
        "why": "the perceptive height scanner the flat root dropped, with its policy term: the rig sits under the importer's flattened prim path, a 0.2 m grid at the 50 Hz policy cadence"
      },
      "v0_rough_terrain": {
        "paths": [
          "scene.terrain.terrain_generator",
          "scene.terrain.terrain_type"
        ],
        "why": "undo the flat conversion: the generator terrain type plus the lizard-scaled six-sub-terrain generator, read from rough_env_cfg rather than restated"
      },
      "v0_rough_terrain_curriculum": {
        "paths": [
          "curriculum.terrain_levels"
        ],
        "why": "the terrain difficulty curriculum the flat root dropped: flat ground has nothing to climb"
      }
    }
  },
  "agent": {
    "allowed": {
      "experiment_name": "one recipe, one log directory (versioning.mdc A): lizard_rough against the flat root's lizard_flat"
    }
  }
}
