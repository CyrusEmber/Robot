# Lizard-Rough-v1 实施计划（存档 · 追溯补录）

> 状态：**已训练，结论已出**——14000 iters（2026-08-31 启动 → 09-01 评测回填），方案冻结。
> 本文件为计划层追溯记录（2026-09-01 补录）；SSOT 分工：参数值 = 本目录冻结
> `lizard_params.yaml`，DR 收窄明细表 / 曲线 / 跑分 = `NOTES.md` + `tb_scalars.csv`，
> 跨版本路线 = `rl_exp/PLAN.md`。复现：任务 id `Lizard-Rough-v1`（obs 266）常驻注册，
> 或 git tag `v1` 整树快照。
> 修订：v1 初稿（追溯补录）

## 1. 目的与假设

teacher Phase 1 首次实际训练。假设：**特权 + 锁脊柱 + 轻扰动**能出步态。
判定分叉（对照实验，根 PLAN §4.6）：

- 仍趴窝 → 激励逃生舱坐实，重启挂账 #7（奖励修复方案）；
- 走起来 → 趴窝门槛在扰动/形态，v2 起逐档加回 DR。

## 2. 当时方案

- **纯参数版本**：v0 yaml 仅 domain_randomization 段全部收窄（无一归零；逐项数值见
  NOTES.md，不在本文复制），奖励 / 动作 / 命令（论文值）/ 仿真 / obs 结构零改动。
- 无新增 spec term（`TEACHER_PRIVILEGED_SPEC` v1 = 空集）。
- 预算 4000 iters，seed 42。

## 3. 验收线

~1000 iters 内 feet_air_time 持续 >0、base 位移；~3000–5000 慢速命令成型
（按本机 flat 实证 10000 iters + 特权 2–3× 加速估）。
**判死刑信号** = 1000 iters reward 仍平 + air≈0（配方问题，非迭代数问题）。

## 4. 计划执行偏差声明

- 迭代数：计划 4000 → 实际 14000（曲线仍在上行，跑满为止；NOTES 已回填）。

## 5. 结局（详见 NOTES.md）

1. **特权救活趴窝成立**：零动作 success 0.254 → v1 0.58–0.64，激励逃生舱不再是 Phase 1 阻塞项。
2. fall 随迭代上升（nominal 0.028→0.333）但 recovery 同步变好——奖励不罚摔，后续版本盯 fall 上限。
3. gap_40cm completion ≈0.1：不会起跳越沟，Phase 1 未达该难度。
4. 下一步 = v2 同协议对照，判特权补全净增益。

## 6. 修订记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-01 | v1 初稿（追溯补录） | 补录本计划文档；内容由 NOTES.md（含 09-01 回填）/ FAMILY 版本历史 / 根 PLAN 整理，不改方案实质 |
