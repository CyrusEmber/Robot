# v10 支撑异常诊断计划（固定 model_14999.pt，不训练，不改奖励）

- 目的：区分四类成因——异常支撑（头颈/腹承重）、动作失稳、台阶几何限制、
  belly 惩罚与越障冲突。产出一张诊断表 + 几段关键视频，回填本目录 NOTES.md。
- 输入：`E:\IsaacLab\logs\rsl_rl\lizard_rough_teacher_v10\2026-09-09_18-15-19\model_14999.pt`
- 已有先验（零成本，先入表）：
  - nominal eval flat 列：completion 0.566 / success 0.66——平地能走但位移打折
  - stairs_20cm：completion 0.83 / success 0.62——连续 20cm 金字塔台阶不是墙
  - gap_40cm：completion 0.11——真墙在 gap，不在台阶
  - 训练全程 belly_contact_force ≈ 0——训练分布内无仰面 hack

## Phase 1 站立闸（2 runs，5–10s）

| run | 做法 | 判定 |
|---|---|---|
| 1a | 平地，默认关节目标 5s（`eval.py` 无 checkpoint 的 zero-action 模式同款） | 倒 → 资产/出生/PD，停查 |
| 1b | ckpt 接管，零速度命令 10s | 倒 → 策略静态稳定问题 |

着地闸（2026-09-11 补，用户提出「零命令 10s 脚不在地上」后加）：tilt/drift 之外必须报
**几只脚承重**——只看 tilt 会漏掉「站得住但脚悬空」。口径：

- 着地 = 该脚接触传感器法向力 > 1 N（`FOOT_CONTACT_N`）
- 辅助几何口径 = 脚 body 原点世界 z；flat 列地面 z=0，**落地基线 ≈0.07–0.08 m**
  （脚 body 原点本身的常量偏置，不是离地间隙）→ 判「悬空」看 z 显著高于同 run 的
  贴地脚，不看绝对值
- 判据：零动作/零命令稳定段应四脚着地（非四脚着地占比 ≈ 0，仅落地瞬态允许例外）

条件：训练同资产同 PD，DR 全关（nominal），固定出生姿态。

## Phase 2 平地直行（9 runs）

- 0.3 / 0.5 / 1.0 m/s 直行各 10s；**3 个 reset seed**（非朝向——平地各向同性，
  有信息量的是 reset 扰动抽样：关节缩放 0.5–1.5 + 出生速度随机）
- 50Hz 记录：base/胸段/头颈倾角与离地高度；各 link 接触力；四脚接触状态与
  承重比例；实际速度；tracking / belly / feet_slide 加权奖励（manager 每 step
  自带，直接读）
- 预定判据：**头颈承重 = 法向力 > 10% 体重且持续 > 0.5s**；时序上分清
  「先低头撑地」还是「先失稳后找支撑」
- 视频：侧视，每档至少 1 段

## Phase 3 单级台阶（≤ 24 runs，早停）

- 5 / 10 / 15 / 20 cm 孤立单级台阶，上/下分开测；平台 ≥ 5m（容 3.6m 身长）；
  0.3 m/s 正对接近；每档 3 次（不同 reset seed）；先低后高，明显失败即停
- 20cm 卡住 → 补 0.5 / 1.0 m/s 动量档（+4 runs）——准静态边界与动量边界分开报
- 记录：通过/卡住、卡住部位、腹接触时长、接触期间是否仍前进
- 判据：「短暂搭腹后通过」与「持续趴住不动」分开标记

## 诊断表（跑完回填）

| 观察 | 处理方向 |
|---|---|
| 1a 默认目标也站不稳 | 资产、出生或 PD |
| 头颈承重、脚悬空，belly≈0 且 tracking 高 | 接触覆盖与奖励漏洞 → 预注册反制（升 belly 权重 / 加头颈接触罚，走训练消融） |
| base 持续倒置但 eval 不报 fall | 评估盲区（tilt<40°+clearance>0.6 的支撑姿态躲过几何判据） |
| 平地正常，超过某高度才卡 | 地形尺度与课程 |
| 搭腹能通过但受明显惩罚 | 短暂越障接触与持续异常支撑分开处理 |

## 实现方式（复用，不新造轮子）

- 脚本：`rl_exp/tools/diagnose/diagnose_support.py` 单文件
- 照抄 `eval.py` 三件套：`apply_eval_mode` nominal 关 DR / 直写
  `term.vel_command_b` 注入命令 / 50Hz snapshot 时序
- 单级台阶：`suites.py` 模式（curriculum=True 等比例锁列 + 单值参数范围 +
  seed 钉死）写 `single_step(h)` 生成器；平地 = MeshPlaneTerrainCfg
- ContactSensor 扩 head/胸/颈 body 名单——诊断 cfg 内做，不动家族代码
- 产出：`rl_exp/tools/diagnose/out/<run>/` json + png + 视频；诊断表回填本文档

## 限制声明

eval 时把 belly 权重调零不会改变固定策略的动作，不能证明「去掉惩罚就能过」。
本轮只测实际接触与奖励账本；证实冲突后才值得做训练消融。

**本轮未验证的东西（勿当已验证）**：
- `min_clearance` 全轮为 `null`——本任务无 height_scanner，fall 判据实际只用 tilt 分支；
  「eval 检测链正常」**未被证明**，只能说 tilt 分支在本条件下不误报。
- 原地/直行条件下测的是**支撑与平动**；命令含 yaw 分量时的转向跟踪未测。
- 侧向扰动下的补撑能力未测（悬空脚是否只是"没用上"还是"用不了"，本轮答不了）。

## 结果（2026-09-11，ckpt model_14999.pt，实现 rl_exp/tools/diagnose/diagnose_support.py）

有效数据（**2026-09-11 口径校验后**，见「测量校验」节）：
`out/v10_run5`（Phase 0 校验 + Phase 1/2/3，真实体重/库四元数/命名已钉）；Phase 0 明细
`out/v10_meas/p0_measure_check.json`。旧记录 `smoke_p1c/p1d`（体重错了 21%、无逐脚）、
`v10_run1/2/3`、`v10_run4`（体重口径错 + 手写 yaw）**全部作废**，仅留档不入结论。

### Phase 0 测量校验（先量对，再下结论；`--phases 0`，明细 `out/v10_meas/p0_measure_check.json`）

| 校验项 | 结果 |
|---|---|
| 四元数布局 | 本 IsaacLab 是 **xyzw**（`utils/math.py:453/577` 明文）。走 lib 后 `quat_apply_inverse(q, vel_w)` 与 `root_lin_vel_b` 的残差**中位数 0.0**（mean 0.0023，max 0.456 只出现在 step 0 出生帧）；`quat_apply_inverse(q, ẑ)` 与 `projected_gravity_b` 残差中位数 **0.0** → 坐标链自洽 |
| 手写 vs lib | 首版按 wxyz 手写：yaw 差 **177.6°**（−12.96° vs 164.64°），tilt 13.58° vs lib 15.81°（该分支**漏掉 roll**）。→ 一律禁止手写旋转分量 |
| 奖励 yaw 帧 | `yaw_quat` 帧速度与体速度差中位数 0.0094 m/s（≈v·sin(tilt)，机身有俯仰时**本就该**不等）——不是误差 |
| 体重 | 真实质量 **72.00 kg** → 706.3 N；静立稳态足部合力 636.6 N（比值 **0.90**）。旧口径取 0.5 s 瞬时接触力和 = **856 N（+21%）** → 所有载荷比例被压低 |
| 命名 | 出生步 base 系实测：rr(−0.60,−0.79) rl(−0.60,+0.79) rf(+0.48,−0.83) lf(+0.48,+0.83) → **rr=右后, rl=左后, rf=右前, lf=左前**（前对 `<侧><前后>`、后对 `<前后><侧>`，与 `check_joints_v8.py:7-8` 的"lf/rf 在前"一致） |
| 接触（全频率 vs 降采样） | `rl_foot`：全 50 Hz **duty 2% / 均值 1.02 N**；每 5 帧抽样后 **duty 0% / 均值 0.00 N**。其余三脚两种口径一致（rr 0.87/0.85、rf 0.865/0.85、lf 0.97/0.95） |
| 横向口径 | 零命令 4 s：yaw 帧 vy 0.067 ≈ 体 0.060 ≈ 世界 0.061 m/s → 静止无侧滑，三口径一致 |

### Phase 1 站立闸 —— tilt/PD 闸通过，**着地闸不通过**（`out/v10_run5`，修正口径）

脚序 = `[rr, rl, rf, lf]`；命名由出生步 base 系实测钉死（+x 头侧、+y 左）：
`rr(−0.60, −0.79)` `rl(−0.60, +0.79)` `rf(+0.48, −0.83)` `lf(+0.48, +0.83)`
→ **rr=右后, rl=左后, rf=右前, lf=左前**。体重 = 真实质量 72.00 kg × g = **706.3 N**
（不是接触力估计；静立稳态足部合力 636.6 N，比值 **0.90**）。

| case | max_tilt_deg | drift_m | feet_down 均值/最小 | 非四脚着地 | 承重占比 rr/rl/rf/lf | 占比之和 | 脚最低 z rr/rl/rf/lf [m] | fell |
|---|---|---|---|---|---|---|---|---|
| p1a 零动作 5s | 18.6 | 0.076 | 3.85 / 0 | 0.06（前 3 帧落地瞬态） | 0.19/0.27/0.33/0.22 | 1.01 | 0.074/0.074/0.070/0.074（四脚贴地） | 否 |
| p1b 策略零命令 10s | 17.8 | +0.31 | **2.84 / 0** | **1.00** | 0.03/**0.01**/0.23/**0.62** | 0.89 | 0.080/**0.123**/0.104/0.074 | 否 |

**发现**：ckpt 零命令 10 s 全程**从未四脚着地**（恒定 2–3 脚承重）；**左后 `rl_foot`**
（全频率 50 Hz 口径）duty 仅 **2%**、均值 **1.02 N**（≈0.14% 体重），脚原点最低 z
0.123 m（贴地基线 0.074）→ 该脚基本悬空；**左前 `lf_foot` 独扛 62%** 体重，右前 23%，
右后 3%，左后 1%。同资产同 PD 的零动作 run（p1a）四脚全着地且载荷均衡
（19/27/33/22%，和 1.01 ≈ 静立比值 0.90 量级）→ **是策略的姿态偏好，非资产/PD/出生**。

- 口径自洽性：p1b 载荷占比之和 0.89 ≈ 静立“足部合力/体重”实测 0.90。旧口径（体重取
  接触力 856 N）下该和为 0.73 → 明显失真，那是首版的错。
- tilt 闸仍通过（17.8° ≪ 40° 判据），drift 0.31 m；belly 恒 0。
- **降采样口径警告**：json 里的逐脚时序按 5 帧抽 1；`rl` 在下采样口径下 duty 0%、均值
  0.00 N（"全程 0 力"），全 50 Hz 口径是 2%/1.02 N。两种说法都要带上口径。

- 该状态在 eval 几何判据（tilt<40° + clearance>0.6×站高）下**完全不可见**——eval 没有
  脚接触项，"三脚站"与"四脚站"同分；且本轮 **clearance 分支根本没被走到**（无 height
  scanner，`min_clearance` 恒 null），所以"eval 检测正常"这句话本轮无权下。
- **可复现性警告**：同 seed 重跑，drift 反号（+0.31 / −0.42）、tilt 13.2/17.8——单跑数值
  不可复现（warp/numpy 侧抽样未播种）。可用的结论是跨跑一致的项：**非四脚着地占比 = 1.00**
  与 belly 恒 0，以及悬空脚恒为后肢。

### Phase 2 平地直行 —— 「无异常支撑」成立，但**移动本身有三条异常**（`out/v10_run5`，3 seed × 3 档）

先说原判据（头颈承重 / 搭腹）的结论：**依然全部未见**——`neck_frac`、`belly_frac` 9/9 恒 0，
`first_event` 全是 `feet_lift`，「先低头撑地」从未发生。

| cmd | fwd | overshoot | fwd_std | **slip_y** | yaw_drift | turn_rate | drift_x | drift_y | crab | v_world | feet<2 | foot_duty rr/rl/rf/lf | foot_load_frac rr/rl/rf/lf | ledger |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 0.3 | 0.445/0.461/0.451 | +0.48/+0.54/+0.50 | 0.13–0.14 | **−0.156…−0.166** | −7.4/−1.6/−8.4° | −0.16…−0.84 | 3.89–4.24 | **−2.39…−2.73** | −19.1…−20.5° | 0.47–0.49 | 0.15–0.18 | 0.25/0.11/0.65/0.80 | 0.09/0.08/0.27/0.46 | 1.470–1.489 |
| 0.5 | 0.625/0.631/0.656 | +0.25/+0.26/+0.31 | 0.14–0.15 | **−0.224…−0.236** | −1.7/−2.2/−2.7° | −0.18…−0.27 | 5.62–6.04 | **−3.36…−3.63** | −19.0…−20.7° | 0.66–0.69 | 0.22–0.23 | 0.25/0.13/0.59/0.73 | 0.08/0.09/0.27/0.43 | 1.474–1.496 |
| 1.0 | 1.057/1.069/1.080 | +0.06/+0.07/+0.08 | 0.12–0.20 | **−0.375…−0.389** | −0.6/−8.0/−9.1° | −0.06…−0.91 | 9.58–10.02 | **−4.84…−6.19** | −19.5…−20.0° | 1.11–1.14 | 0.32–0.35 | 0.24/0.18/0.48/0.62 | 0.09/0.11/0.26/0.41 | 1.445–1.481 |

**异常 1 —— 恒定 ~20° 蟹行，且已拆出主因是侧滑不是转弯**：世界横移 −2.4…−6.2 m/10 s，
`crab` 19.0–20.7°（9/9，与档速无关）。拆分口径：`slip_y` = **yaw 帧**（奖励核同款，lib
`yaw_quat`）横向速度 = **相对机身侧滑**，实测 −0.156…−0.389 m/s，与体坐标口径一致到
0.01 m/s；`yaw_drift` 只有 −0.6…−9.1°（≤0.9°/s）→ **机身几乎不转，横移几乎全是侧滑**。
静止（Phase 0，零命令 4 s）时三个口径都 ≈0.06 m/s → 蟹行是**运动态**现象，不是出生残留。

**异常 2 —— 超速而账本满分**：`overshoot` 低速 +48…54%、1.0 档 +6…8%；跟踪账本却 1.445–1.496
（上限 1.5）——`track_lin_vel_xy_lin`（`teacher_mdp.py:455-472`）把超速 `clamp(max=speed_c)`，
**只罚慢、不罚快**，误差最优解天然朝超速偏。**账本不能用来判"跟得准不准"**。

**异常 3 —— 前后肢分工失衡 + 随速度提前腾空**：`foot_duty` 前两脚 0.48–0.80（几乎常触地）
vs 后两脚 0.11–0.25（多数时间悬空）；载荷前两脚合计 **0.65–0.73**。前后差**不能全算策略锅**
（`check_joints_v8.py:19-24`：前腿 + 球头挂在 chest/neck 的 ~1 m 杠杆上，前肢静态承重偏高
是资产属性），但**左右差**（走时 lf 0.46 vs rf 0.27 ≈ 1.7×；站时 62% vs 23% ≈ 2.7×）+
恒定侧滑指向推力左右不对称。`feet<2` 随速度单调升 0.15 → 0.35（1 m/s 时 1/3 时间少于
两脚着地）。

### Phase 3 单级台阶 5–20 cm 上/下（0.3 m/s 正对）—— 全档 3/3 通过

progress 一律 ~10 m、**腹/胸/颈接触全程为 0**、无卡住部位；up_20 攀爬期
feet 断续 + tilt 瞬态 ~12°，down_20 下落期 tilt 瞬态 15.9°，均数步内恢复。

### 诊断表回填

| 观察 | 结论 |
|---|---|
| 默认关节目标也站不稳 | **未见**（6.1° 稳站，四脚着地） |
| 头颈承重、脚悬空，belly≈0 且 tracking 高 | **脚悬空复现**（零命令全程非四脚着地，**左后**脚 duty 2%、均值 1.02 N）；但头颈承重 0、非"找支撑"型异常——是姿态偏好，非接触覆盖/奖励漏洞 |
| **零命令下脚不在地上（用户提出）** | **复现**：恒定 2–3 脚承重，左后脚基本悬空；tilt 判据看不出来（着地闸已补进 Phase 1） |
| **平地直行横向漂移（用户提出"移动很奇怪"）** | **复现**：恒定 ~20° 蟹行（9/9、档速无关），世界横移 2.4–6.2 m/10 s；**拆分后主因是相对机身侧滑**（−0.16…−0.39 m/s），机身只转 ≤9° |
| **平地速度不跟命令** | **复现**：低速超速 +48…54%（账本仍 1.47–1.49）→ 奖励核单边（只罚慢），账本不能判"跟得准" |
| base 持续倒置但 eval 不报 fall | 未复现；**变体已复现**——支撑退化（三脚站）同样躲过 eval 判据；且本轮 clearance 分支未被走到（无 scanner），"eval 检测正常"无权下判 |
| 平地正常，超过某级高度才卡住 | **未见**（≤20cm 全过，无能力边界） |
| 搭腹能通过但受明显惩罚 | **未见**（无任何搭腹） |

**结论：在 0.3–1.0 m/s、nominal DR 关、单级 ≤20cm 台阶条件下，原担忧的"支撑异常"
（头颈承重、搭腹越障）完全不出现；但发现三条原判据看不见的真实异常——① 静止期
退化为 2–3 脚支撑（左后脚基本悬空）、② 平地直行恒定 ~20° 蟹行，主因是相对机身侧滑
（机身只转 ≤9°）、③ 低速超速 +48…54% 而跟踪账本仍满分。三条都在 tilt 判据的盲区里。**
剩余未覆盖区：更高速度、robust 级扰动（4 m/s 踢 + DR）、连续窄踏面（0.7m 金字塔楼梯，
eval 已知 completion 0.83）、命令含 yaw 分量的转向跟踪、以及 clearance 分支本身。
下一步按用户排序：先做速度目标修正（横向误差/超速/停车进目标，独立消融），再用温和
侧向扰动测悬空脚能否补撑，最后才决定是否加静止支撑约束。

### 实现备注（坑与修法，防再踩）

1. `reset_base` 基类默认 yaw ±π 随机——体坐标命令导致各 env 往随机方向走，
   台阶列全部失真；诊断脚本钉 `pose_range["yaw"]=(0,0)`。
2. 单级台阶条件：`platform_width` 必须**严格大于** `size−2*border`；恰好相等
   时 floor 除法给 num_steps=1 = 两级台阶（离线 trimesh 校验抓出）。
3. rsl_rl wrapper 的 `reset()` 返回 `(obs, extras)` 元组，策略吃裸 obs。
4. 接触力/体重需先走若干物理步再读（reset 后首读为 0）。
5. 脚着地要同时存**接触力**（判据）与**脚 body z**（几何复核）：只存 `feet_down`
   整数计数就没法回答"哪只脚悬空"，逐脚 `fz_feet`/`foot_z` 必须落进 json
   （2026-09-11 补，p1c 存档因缺逐脚时序只能重跑）。
6. 脚 body 原点在贴地时有 ~0.07 m 常量偏置——"悬空"要跟同 run 的贴地脚比，
   不能拿绝对 z 比 0。
7. **脚序 = `[rr, rl, rf, lf]`**，命名由出生步 base 系实测钉死：**rr=右后, rl=左后,
   rf=右前, lf=左前**（前对 `<侧><前后>`、后对 `<前后><侧>`）。读载荷数组前先确认顺序
   ——首版把 rl 写成"右后"、把载荷读成"后重前轻"，方向全反。
8. **四元数是 xyzw**（`utils/math.py:453`：`q_x, q_y, q_z, q_w = quat[:, 0..3]`）。
   一切旋转走 lib（`quat_apply` / `quat_apply_inverse` / `yaw_quat` /
   `euler_xyz_from_quat`），**别手写分量**：首版按 wxyz 手写 → yaw 差 177.6°、
   tilt 分支漏掉 roll（那才是"180° 怪现象"的真因，不是资产偏置）。
9. **体重取真实质量 × g**（`data.body_mass.sum()` = 72.0 kg → 706.3 N），不要拿某一
   时刻的足部接触力之和当体重：0.5 s 时读到 856 N（+21%），把所有载荷比例压低了 21%。
   交叉校验用**稳态**（末尾 30%）足部合力，实测 0.90×m·g 属正常（残余动/非静立）。
10. **json 时序是下采样**（5 帧抽 1）："某脚全程 0 力"这类话必须回到全 50 Hz 口径再确认
    （rl 全频率 duty 2%、均值 1.02 N，抽样后成 0/0）。
11. **别把仿真的 stdout 接管道过滤器**（`| findstr ...`）：过滤器提前退出会掐断 python
    的 stdout → 进程被杀、只落一半 json（首次 Phase 2 重跑就 5/9 断）。重定向到文件再筛。
12. **残差报中位数，别报 max**：出生落地那一帧把 max 拉高到 0.46，中位数是 0.0——
    用 max 会误判坐标链坏了。
