# v10 支撑异常诊断计划（固定 model_14999.pt，不训练，不改奖励）

- 目的：区分四类成因——异常支撑（头颈/腹承重）、动作失稳、台阶几何限制、
  belly 惩罚与越障冲突。产出一张诊断表 + 几段关键视频，回填本目录 NOTES.md。
- 输入：`E:\IsaacLab\logs\rsl_rl\lizard_rough_teacher_v10\2026-09-09_18-15-19\model_14999.pt`
- 已有先验（零成本，先入表）：
  - nominal eval flat 列：completion 0.566 / success 0.66——平地能走但位移打折
  - stairs_20cm：completion 0.83 / success 0.62——连续 20cm 金字塔台阶不是墙
  - gap_40cm：completion 0.11——真墙在 gap，不在台阶
  - 训练全程 belly_contact_force ≈ 0——训练分布内无仰面 hack

## Phase 1 站立闸（2 runs，5–10s）

| run | 做法 | 判定 |
|---|---|---|
| 1a | 平地，默认关节目标 5s（`eval.py` 无 checkpoint 的 zero-action 模式同款） | 倒 → 资产/出生/PD，停查 |
| 1b | ckpt 接管，零速度命令 10s | 倒 → 策略静态稳定问题 |

着地闸（2026-09-11 补，用户提出「零命令 10s 脚不在地上」后加）：tilt/drift 之外必须报
**几只脚承重**——只看 tilt 会漏掉「站得住但脚悬空」。口径：

- 着地 = 该脚接触传感器法向力 > 1 N（`FOOT_CONTACT_N`）
- 辅助几何口径 = 脚 body 原点世界 z；flat 列地面 z=0，**落地基线 ≈0.07–0.08 m**
  （脚 body 原点本身的常量偏置，不是离地间隙）→ 判「悬空」看 z 显著高于同 run 的
  贴地脚，不看绝对值
- 判据：零动作/零命令稳定段应四脚着地（非四脚着地占比 ≈ 0，仅落地瞬态允许例外）

条件：训练同资产同 PD，DR 全关（nominal），固定出生姿态。

## Phase 2 平地直行（9 runs）

- 0.3 / 0.5 / 1.0 m/s 直行各 10s；**3 个 reset seed**（非朝向——平地各向同性，
  有信息量的是 reset 扰动抽样：关节缩放 0.5–1.5 + 出生速度随机）
- 50Hz 记录：base/胸段/头颈倾角与离地高度；各 link 接触力；四脚接触状态与
  承重比例；实际速度；tracking / belly / feet_slide 加权奖励（manager 每 step
  自带，直接读）
- 预定判据：**头颈承重 = 法向力 > 10% 体重且持续 > 0.5s**；时序上分清
  「先低头撑地」还是「先失稳后找支撑」
- 视频：侧视，每档至少 1 段

## Phase 3 单级台阶（≤ 24 runs，早停）

- 5 / 10 / 15 / 20 cm 孤立单级台阶，上/下分开测；平台 ≥ 5m（容 3.6m 身长）；
  0.3 m/s 正对接近；每档 3 次（不同 reset seed）；先低后高，明显失败即停
- 20cm 卡住 → 补 0.5 / 1.0 m/s 动量档（+4 runs）——准静态边界与动量边界分开报
- 记录：通过/卡住、卡住部位、腹接触时长、接触期间是否仍前进
- 判据：「短暂搭腹后通过」与「持续趴住不动」分开标记

## 诊断表（跑完回填）

| 观察 | 处理方向 |
|---|---|
| 1a 默认目标也站不稳 | 资产、出生或 PD |
| 头颈承重、脚悬空，belly≈0 且 tracking 高 | 接触覆盖与奖励漏洞 → 预注册反制（升 belly 权重 / 加头颈接触罚，走训练消融） |
| base 持续倒置但 eval 不报 fall | 评估盲区（tilt<40°+clearance>0.6 的支撑姿态躲过几何判据） |
| 平地正常，超过某高度才卡 | 地形尺度与课程 |
| 搭腹能通过但受明显惩罚 | 短暂越障接触与持续异常支撑分开处理 |

## 实现方式（复用，不新造轮子）

- 脚本：`rl_exp/tools/diagnose/diagnose_support.py` 单文件
- 照抄 `eval.py` 三件套：`apply_eval_mode` nominal 关 DR / 直写
  `term.vel_command_b` 注入命令 / 50Hz snapshot 时序
- 单级台阶：`suites.py` 模式（curriculum=True 等比例锁列 + 单值参数范围 +
  seed 钉死）写 `single_step(h)` 生成器；平地 = MeshPlaneTerrainCfg
- ContactSensor 扩 head/胸/颈 body 名单——诊断 cfg 内做，不动家族代码
- 产出：`rl_exp/tools/diagnose/out/<run>/` json + png + 视频；诊断表回填本文档

## 限制声明

eval 时把 belly 权重调零不会改变固定策略的动作，不能证明「去掉惩罚就能过」。
本轮只测实际接触与奖励账本；证实冲突后才值得做训练消融。

## 结果（2026-09-11，ckpt model_14999.pt，实现 rl_exp/tools/diagnose/diagnose_support.py）

有效数据：Phase 1 = out/smoke_p1c（旧口径）+ out/smoke_p1d（加逐脚口径）；
Phase 2 = out/v10_run1（旧口径，仅头颈/搭腹判据）+ **out/v10_run4（加横向/逐脚口径，
主用）**；Phase 3 = out/v10_run3（单级台阶 + 出生 yaw 钉 0）。
v10_run1（随机朝向）与 v10_run2（地形 bug=两级台阶）作废，仅留档。

### Phase 1 站立闸 —— tilt/PD 闸通过，**着地闸不通过**

旧存档（`out/smoke_p1c`，当时只记脚数，逐脚力未存）：

| case | max_tilt_deg | drift_m | max_belly_fz_n | feet_down 均值/最小 | 非四脚着地 | fell |
|---|---|---|---|---|---|---|
| p1a 零动作 5s | 6.1 | 0.03 | 0.0 | 3.78 / 0 | 0.06（仅前 3 帧落地瞬态） | 否 |
| p1b 策略零命令 10s | 13.2 | −0.42 | 0.0 | **1.94 / 0** | **1.00（100/100 帧）** | 否 |

补录（`out/smoke_p1d`，2026-09-11 加逐脚口径后重跑，脚序 = rr, rl, rf, lf）：

| case | max_tilt_deg | drift_m | feet_down 均值/最小 | 非四脚着地 | 承重占比 rr/rl/rf/lf | 脚最低 z rr/rl/rf/lf [m] | 稳态 z [m] | fell |
|---|---|---|---|---|---|---|---|---|
| p1a 零动作 5s | 6.0 | 0.08 | 3.85 / 0 | 0.06（瞬态） | 0.15/0.23/0.27/0.18 | 0.074/0.074/0.070/0.074 | 四脚 ≈0.07 | 否 |
| p1b 策略零命令 10s | 17.8 | +0.31 | **2.84 / 0** | **1.00** | 0.02/**0.01**/0.19/**0.52** | 0.080/**0.133**/0.105/0.074 | 0.084/**0.223**/0.125/0.074 | 否 |

**发现（用户提出、数据证实）**：ckpt 零命令 10 s 全程**从未四脚着地**；`rl_foot`（右后）
10 s 内 **0 帧受力**，稳态脚原点 z = 0.223 m ≈ 落地基线（0.07）之上 15 cm，即右脚全程
悬空；`lf_foot`（左前）独扛 **52%** 体重，`rf` 19%，`rr` 仅 2%。同资产同 PD 的零动作
run（p1a）四脚全着地且载荷均衡（15/23/27/18%）→ **策略偏向三点支撑，非资产/PD/出生**。

- tilt 闸仍通过（17.8° ≪ 40° 判据），drift 0.3 m；belly 恒 0。
- 该状态在 eval 几何判据（tilt<40° + clearance>0.6×站高）下**完全不可见**——eval 没有
  脚接触项，"三脚站"与"四脚站"同分。这正是诊断表里「base 倒置但 eval 不报 fall」
  一行的弱化版：不是倒置，而是静默的支撑退化。
- **可复现性警告**：p1c/p1d 同 seed，drift 反号（−0.42 / +0.31）、tilt 13.2/17.8，
  单跑数值不可复现（warp/numpy 侧抽样未播种）。可用的结论是两次一致的项：
  **非四脚着地占比 = 1.00**（p1c 恒 2 脚、p1d 恒 3 脚）与 belly 恒 0。

### Phase 2 平地直行 —— 「无异常支撑」成立，但**移动本身有三条异常**（2026-09-11 补，`out/v10_run4`，3 seed × 3 档）

先说原判据（头颈承重 / 搭腹）的结论：**依然全部未见**——`neck_frac`、`belly_frac` 9/9 恒 0，
`first_event` 全是 `feet_lift`，「先低头撑地」从未发生，tilt_max 10–14°。

补录（`out/v10_run4`，加横向/逐脚口径后重跑；脚序 = rr, rl, rf, lf，**lf/rf = 前肢**，
`check_joints_v8.py:7-8` 已钉；crab=体坐标 atan2(lat, fwd)，与坐标系约定无关）：

| cmd | fwd | overshoot | fwd_std | lat_mean | drift_x | drift_y | crab | v_world | feet<2 | foot_duty rr/rl/rf/lf | foot_load_frac rr/rl/rf/lf | ledger |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 0.3 | 0.439/0.464/0.451 | +0.47/+0.55/+0.50 | 0.13–0.14 | −0.149…−0.167 | 3.92–4.29 | **−2.31…−2.67** | **−18.3…−20.8°** | 0.47 | 0.15–0.19 | 0.24/0.09/0.65/0.80 | 0.07/0.06/0.22/0.39 | 1.465–1.489 |
| 0.5 | 0.624/0.626/0.656 | +0.25/+0.25/+0.31 | 0.14–0.15 | −0.223…−0.246 | 5.62–6.09 | **−3.25…−3.59** | **−18.8…−21.5°** | 0.67 | 0.22–0.25 | 0.25/0.13/0.59/0.73 | 0.07/0.08/0.23/0.35 | 1.473–1.497 |
| 1.0 | 1.054/1.075/1.075 | +0.05/+0.08/+0.08 | 0.12–0.20 | −0.381…−0.393 | 9.48–9.99 | **−4.90…−6.26** | **−19.5…−20.1°** | 1.11–1.14 | 0.32–0.34 | 0.23/0.18/0.48/0.62 | 0.06/0.08/0.22/0.33 | 1.446–1.479 |

**异常 1 —— 恒定 ~20° 蟹行（9/9 一致，与档速无关）**：直行命令下横向速度 −0.15…−0.39 m/s，
10 s 世界横移 **−2.3…−6.3 m**，同时前进 3.9…10.0 m。`crab` 角 18–22° 极稳（不是随机噪声、
不是出生速度残留——`lat_speed` 时序全程持续，见 json）。世界位移与 `dx/dt`、`dy/dt` 互证，
不受体坐标约定影响。

**异常 2 —— 超速而账本满分**：`overshoot` 低速 +47…55%、1.0 档 +5…8%；跟踪账本却 1.45–1.50
（上限 1.5）——`track_lin_vel_xy_lin`（`teacher_mdp.py:455-472`）把超速 `clamp(max=speed_c)`，
**只罚慢、不罚快**，误差最优解天然朝超速偏。账本不能用来判"跟得准不准"。

**异常 3 —— 前后肢分工失衡 + 随速度腾空**：`foot_duty` 前肢 0.48–0.81（几乎常触地）vs 后肢
0.09–0.28（多数时间悬空）；`foot_load_frac` 前肢合计 0.54–0.66。前后差**不能全算策略锅**
（`check_joints_v8.py:19-24`：前腿 + 球头挂在 chest/neck ~1 m 杠杆上，前肢静态承重偏高是
资产属性），但**左右差**（走时 lf/rf≈1.6×、站时 2.7×）+ 20° 蟹行说明推力左右不对称。
`feet<2` 随速度单调升 0.15 → 0.34（1 m/s 时 1/3 时间少于两脚着地）。

> **修正（前版本记录错误）**：本节曾写「脚载荷后重前轻（约 42/25/7/7%）——蜥蜴后肢主导」。
> 数组序是 `[rr, rl, rf, lf]` 且 lf/rf 为前肢 → 实际**前重后轻**，且 42 是 lf 不是 rr。方向反了。

### Phase 3 单级台阶 5–20 cm 上/下（0.3 m/s 正对）—— 全档 3/3 通过

progress 一律 ~10 m、**腹/胸/颈接触全程为 0**、无卡住部位；up_20 攀爬期
feet 断续 + tilt 瞬态 ~12°，down_20 下落期 tilt 瞬态 15.9°，均数步内恢复。

### 诊断表回填

| 观察 | 结论 |
|---|---|
| 默认关节目标也站不稳 | **未见**（6.1° 稳站，四脚着地） |
| 头颈承重、脚悬空，belly≈0 且 tracking 高 | **脚悬空部分复现**（零命令全程非四脚着地，右后脚恒 0 力）；但头颈承重 0、非"找支撑"型异常——是姿态偏好，非接触覆盖/奖励漏洞 |
| **零命令下脚不在地上（用户提出）** | **复现**：全程仅 2–3 脚承重，`rl_foot` 悬空 6–15 cm；tilt 与 drift 判据看不出来（着地闸已补进 Phase 1，见上） |
| **平地直行横向漂移（用户提出"移动很奇怪"）** | **复现**：恒定 ~20° 蟹行（9/9），10 s 世界横移 2.3–6.3 m；档速无关 → 推力左右不对称，与站立期左右载荷差同源 |
| **平地速度不跟命令** | **复现**：低速超速 +47…55%（账本仍是满分 1.49）→ 奖励核单边（只罚慢），账本不能判"跟得准" |
| base 持续倒置但 eval 不报 fall | 未复现；**变体已复现**——支撑退化（三脚站）同样躲过 eval 几何判据 |
| 平地正常，超过某级高度才卡住 | **未见**（≤20cm 全过，无能力边界） |
| 搭腹能通过但受明显惩罚 | **未见**（无任何搭腹） |

**结论：在 0.3–1.0 m/s、nominal DR 关、单级 ≤20cm 台阶条件下，原担忧的"支撑异常"
（头颈承重、搭腹越障）完全不出现；但发现三条原判据看不见的真实异常——① 静止期
退化为 2–3 脚支撑（右后脚全程悬空）、② 平地直行恒定 ~20° 蟹行（10 s 横移 2.3–6.3 m）、
③ 低速超速 +47…55% 而跟踪账本仍满分。三条都在 tilt/几何判据的盲区里。**
剩余未覆盖区：更高速度、robust 级扰动（4 m/s 踢 + DR——即 robust eval 里穿透异常
的那个 regime）、连续窄踏面（0.7m 金字塔楼梯，eval 已知 completion 0.83）。
若需继续，下一步应测踢后恢复行为（悬空脚在扰动下是否会代偿）而非再扫台阶高度；
蟹行的左右归因需要横向漂移的过程数据（已入库），或改测"命令 yaw 分量"是否被跟踪。

### 实现备注（坑与修法，防再踩）

1. `reset_base` 基类默认 yaw ±π 随机——体坐标命令导致各 env 往随机方向走，
   台阶列全部失真；诊断脚本钉 `pose_range["yaw"]=(0,0)`。
2. 单级台阶条件：`platform_width` 必须**严格大于** `size−2*border`；恰好相等
   时 floor 除法给 num_steps=1 = 两级台阶（离线 trimesh 校验抓出）。
3. rsl_rl wrapper 的 `reset()` 返回 `(obs, extras)` 元组，策略吃裸 obs。
4. 接触力/体重需先走若干物理步再读（reset 后首读为 0）。
5. 脚着地要同时存**接触力**（判据）与**脚 body z**（几何复核）：只存 `feet_down`
   整数计数就没法回答"哪只脚悬空"，逐脚 `fz_feet`/`foot_z` 必须落进 json
   （2026-09-11 补，p1c 存档因缺逐脚时序只能重跑）。
6. 脚 body 原点在贴地时有 ~0.07 m 常量偏置——"悬空"要跟同 run 的贴地脚比，
   不能拿绝对 z 比 0。
7. **脚序 = `[rr, rl, rf, lf]`，且 lf/rf 是前肢**（body 序 = `find_bodies(preserve_order=True)`；
   前后归属由 `check_joints_v8.py:7-8` 硬闸钉住）。读载荷数组前先确认顺序——
   首版记录就在这里把前后读反了。
8. **别手写 yaw**：本任务的 `root_quat_w` 与奖励核的 yaw 帧差 180°（自检：只有
   `−R(ψ)·v_b` 才等于 `root_lin_vel_w`），手写 `atan2` 会给出反 180° 的"朝向"。
   判横向漂移一律用世界位移/世界速度（`drift_x/drift_y` 与 `dx/dt`、`dy/dt` 互证）。
9. **别把仿真的 stdout 接管道过滤器**（`| findstr ...`）：过滤器提前退出会掐断
   python 的 stdout → 进程被杀、只落一半 json（首次 Phase 2 重跑就 5/9 断）。
   重定向到文件再筛（`> "%TEMP%\x.log" 2>&1`）。
