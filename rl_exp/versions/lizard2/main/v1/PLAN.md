# main/v1 方案（全量式，新线）

> 骨架落位 2026-09-22，由 `rl_exp/tools/pipeline/declare_family.py` 生成；本文按 versioning.mdc §A-2 写满。
> 无上游母本（`base.json` 为 `null`）⇒ 按 §A 分线条款用**全量式 PLAN**：新线的 v1 是多变量设计
> （换骨骼 + 换命令窗口），不用单变量小改的简短骨架。
> 结果见 [NOTES.md](NOTES.md)；完整差异与逐项理由以 [diff.json](diff.json) 为准，本文不复述路径清单。
> 修订：v1.5（2026-09-22，命令窗口 0–2 → 0–3 m/s：上界取 3.0、分带与硬前置同步加宽，风险第 1 条补 3 m/s 的
> 估算——已超力矩预算约 1.5×，故"到不了上界"不能自动算策略失败）；v1.4（2026-09-22，终止条件加 `head_contact`：
> 头颈/前胸触地 > 1.0 N 即终止，与 base 同一个接触值、不加 dwell）；v1.3（2026-09-22，第二次评审后的修正：
> 执行器预算改为"估算 + 必须实测"并更正数字与分母口径、非足承重补总量判据且方向修正、新增自碰撞检查与步态判据、
> 低速带声明不可比、髋惯量表述更正）；v1.2（2026-09-22，验收节重写：判据点名 `CRITERION_KINDS`；姿态判据方向修正；
> 补零命令口径、聚合顺序、失败后帧计入方式；两条待拍板按所有者决定落定）；v1.1（2026-09-22，初稿写满）

## 目的与假设

**问题**：把竖直轴从股骨远端（旧构型 `hfe`）搬到腿根（新构型的 `hip`）之后，同一套 v1 配方
（平地、无课程、无 DR、7 项奖励、躯干/头颈接触终止）能不能在 **0–3 m/s 的区间命令**下学会持续行走？

**为什么问这个**：旧构型下"名义迈步轴装在错的那一段"是实测结论（`hfe` 力臂 0.06–0.07 m ⇒ 扫满行程
也只有 0.11–0.13 m 脚掌位移；见 `acceptance/records/2026-09-21-lizard-leg-axis-kinematics.md`）。
新构型把这条轴放到腿根，力臂 0.49 m、承重姿态下四腿前后行程 0.546–0.558 m
（`acceptance/records/2026-09-22-lizard2-stride-at-load.md`）。**几何证据已成立，训练要回答的是它够不够学**。

**假设（可证伪）**：若旧构型"竖着走不动"主要来自迈步轴的力臂而非奖励/课程/网络，则在新骨骼上跑
同一配方，失败形态应当是**够不到区间上界**（3 m/s）或**姿态/承重越界**，而不是**完全不动或原地扭**。
**并且**：按量级估算 3 m/s 已超出髋的力矩预算（见「风险与挂账」第 1 条），所以"够不到上界"本身要由
硬前置 4 分两层去判定是**策略没学会**还是**执行器给不出**——这两件事的结论完全不同。

**归因边界（先说清）**：本版与旧线 baseline v1 之间同时变了两件事 —— 骨骼与命令窗口（定点 0.5 → 区间
0–3）。因此本版结果**只能**回答"新构型 + 本配方能否走"，**不能**单独归因于骨骼修正；要把两者分开需要
后者单开一版（见「风险与挂账」第 2 条）。

**判据的可执行口径**（固定窗口、失败后如何处理）见「验收」，不以形容词代替。

## 配方

逐项（值 + 依据；差异的唯一维护位置是 [diff.json](diff.json)，本文只给设计理由）：

| 项 | 值 | 依据 |
|---|---|---|
| 资产 | `assets/lizard2/lizard2.usda`（30 关节；四腿各多一条竖直轴 `*_hip`，位于腿根；其余 27 个 link 的质量与网格与 lizard 逐字节相同） | 换骨骼是本版的目的；资产差异的逐位证据见 `acceptance/records/2026-09-22-lizard2-family-landing.md` |
| 地形 | `plane`，`terrain_generator=None`，无 `height_scanner` | 平地是问题本身；扫描器在平面上扫的是常数 |
| 命令 | **区间** `lin_vel_x [0.0, 3.0]`、`lin_vel_y 0`、`ang_vel_z 0`；`heading_command=False`、`rel_standing_envs=0`；框架默认 10 s 重采样 ⇒ 20 s 回合含两次命令与一次换速 | 区间命令测的是"区间内会走"，不是"一个点会走"；上界取 3.0（与旧线同值）由所有者定，**上界能否达到留给验收的带报告回答**；两个改写开关必须显式关，否则配置写着 0 而运行时被改写（启动探针读**实际下发**的命令张量，不信配置） |
| 观测 | 单组 `policy` = 7 项 proprio，**102 维**（3+3+3+3+30+30+30）；`height_scan=None`；`enable_corruption=False` | 30 关节的直接后果（旧线 26 关节为 90 维）；宽度已实测批准（`versions/obs_protocol_anchors.json`，协议 `a25a8c39001b`，按资产键）；观测噪声也是随机化，显式关 |
| 网络 | MLP `[256,128,128]` elu，单组，`obs_normalization=False` | 与旧线 baseline v1 逐项相同：算法不是本版变量 |
| 奖励 | **7 项**：`track_lin_vel_xy_miki 1.5`、`track_ang_vel_z_exp 0.5`、`lin_vel_z_l2 -2.0`、`action_rate_l2 -0.01`、`dof_torques_l2 -1e-5`、`undesired_contacts -1.0`、`belly_contact_force -0.5`；置 None：`track_lin_vel_xy_exp`/`ang_vel_xy_l2`/`dof_acc_l2`/`feet_air_time`/`flat_orientation_l2`/`dof_pos_limits` | 逐项照旧线 v1 的冻结值（同一配方）；满值口径见其 PLAN，本线不重定义 |
| 终止 | `time_out` + `base_contact`（`base_link` > 1.0 N）+ **`head_contact`（`chest_.*`/`neck_.*` > 1.0 N）** | 平地上有**两条**"倒下"的路：躯干触地，以及头颈/前胸触地。头守卫用**同一个 1.0 N**、框架 `illegal_contact`（接触传感器 history 窗口取最大，**不加 dwell**）⇒ 不新增核、不引第二个数；它**是接触判据而非承重判据**，因为旧线 v1 的失败形态是"66% 的帧把重量压在脖子上、却从没连续够久"，正好穿过 dwell 型承重判据。2026-09-22 开训前修订加入（v1 未训练，属修订不属新版本） |
| 课程 | 全无：`terrain_levels` 置 None；阶段速度/地形课程属别线代码，本线不存在 | 与旧线 v1 同 |
| DR | **随机化全关**：`physics_material`/`add_base_mass`/`base_com`/`base_external_force_torque`/`push_robot` 置 None；`reset_base` 保留、`pose_range`/`velocity_range` 逐轴归零；`reset_robot_joints` **保留并钉死**（`position_range=(1.0,1.0)`、`velocity_range=(0.0,0.0)`） | 逐项显式关闭；`reset_robot_joints` 是本框架**唯一**的关节复位写入者，置 None 等于取消复位（旧线 v1.1 的实测教训，本线照抄结论） |
| PD / 动作接口 / sim | 冻结为族群实际生效值：PD `800/40`（legs，**含 `*_hip`**）·`200/12`（feet）·`150/10`（spine）、动作两组 `legs 0.5`（20 维，含 hip）+ `spine 0.25`（10 维）、`dt 0.005`×`decimation 4`（50 Hz）、`episode 20 s` | 平台接口不是本版变量；动作覆盖已实测（30/30，无未接通道） |
| PPO | 继承 `LizardBaselinePPORunnerCfg`（框架 velocity 平地配方：`num_steps 24`、5 epochs、4 minibatch、lr `1e-3` adaptive、`gamma 0.99`、`lam 0.95`、`entropy 0.005`、`clip 0.2`），本版只改 `experiment_name=lizard2_v1`、`max_iterations=10000` | 算法不是变量；迭代预算与旧线 v2 同值（区间命令 ⇒ 单回合含两次命令，且新骨骼学习曲线未知）。**继承是有意的**：改旧线的 PPO 会同时改本线，接受该耦合（改动在代码里可见） |

## 修订记录

| 日期 | 版本 | 变更 + 原因 + 依据 |
|---|---|---|
| 2026-09-23 | v1 | **步态补测：四个量同一次 run**（`acceptance/records/2026-09-23-lizard2-v1-gait-skate.md` 的 ⑤ 有全文与读数）：新探针 `rl_exp/tools/diagnose/gait_probe.py`（口径数学入 `diag_metrics.py`）在同一 checkpoint 上测脚底净空（碰撞网格最低顶点）、接触点切向速度（`v_com + ω × r`，**以 COM 为参考**——框架 `body_lin_vel_w` 是 COM 速度的别名，与 `body_pos_w` 混用就是把 `ω × r` 算错）、足端 yaw 帧前后轨迹、髋/膝目标 vs 实际。结果把成因分开：**目标层已发出摆动**（hip/hfe 目标幅度大且随速度涨）⇒ "没发动作"不再成立；**摆动帧净空贴地**（0.5 档中位 0.7–15 mm）且**承重接触点在后腿滑移**（1.5 档 p50 已超机身速度）；**执行器误差随速度涨成立但不解释低速净空**；⑥ 再把髋/膝**目标角经一阶正运动学**（`body_link_jacobian_w` × 目标误差 + 绕最深顶点的转动项，`diag_metrics.target_vertex_delta`）映射成脚底高度 ⇒ **低速档的贴地是目标层命令的**（摆动最高帧的目标净空只有毫米级）、**高速档剩余差才是跟踪**、**lf（左前）三档目标都要求脚底入地**（⚠️ 同日晚该 lf 条**降级为待验证**：左右关节目标是镜像的、脚网格同形，符号翻转来自预测的线性（瞬时修正）项，见记录 ⑧）⇒ "目标本身就低"与"被执行器按住"已分开；⑦ 另报**位置参考在高速档系统性超出关节限位**——⚠️ **这不是接口缺陷**：位置 PD 下更远的参考本身就是产生驱动力矩的手段（`τ ≈ Kp(目标−实际) − Kd·速度`），裁剪可能砍掉能力，且越限帧误差更大是构造性的；**是否裁剪待独立证据**（贴限位时长、投影回限位后的力矩变化、部署端是否接受限位外参考），见记录 ⑦ 第 6–8 条。仍未答的是策略为何学到这个目标（属奖励侧）。**本行不复述数值** |
| 2026-09-23 | v1 | **首次判决 + 步态实测（判据侧缺项暴露）**：判决 `pass` 八条全过（含判决后才修的两处尺子缺陷：零命令带在命令阶跃帧取样、零命令带位移读数随批量大小变）。但**所有者目视录像指出"垫着走"**；2026-09-23 复核探针源码后**撤回"实测证实"**（探针读的是足端原点高度与足端刚体速度，不是脚底净空与接触点切向速度）⇒ 症状目视成立、定量定性未完成，能确认的是两侧缺口；出处是旧线自己的病历——`lizard/baseline/v1/PLAN.md:125` 预注册"删掉 `feet_slide`/`foot_clearance` 可能重现拖脚/划脚"（本版逐项照旧线，取舍到期），`lizard/main/v15/main_params.yaml:262-270` 记录退役线为同一病症把 `r_slip` ×10。**判据侧的账在本文件验收表"步态摆动"行**（`min_lift_m` 未实现、`foot_slip_mps` 未计算）⇒ 八条闸门看不见步态质量，该 `pass` 只证明"不倒、方向对、位移对"。**数值与对照标准不在本行复述**：判决见 `acceptance/records/2026-09-23-lizard2-v1-first-eval.md`，步态实测见 `acceptance/records/2026-09-23-lizard2-v1-gait-skate.md` |
| 2026-09-23 | v1 | **尺子两处缺陷 + 一处只在 GPU 上才现形的 bug**（`acceptance/records/2026-09-23-lizard2-v1-first-eval.md` 有全文）：① 分带位移闸门读 `meta["start_pos"]` 时假定设备（采集器透传 CUDA 张量、`frames` 是 CPU 拷贝）⇒ **该冻结 judge 在 GPU 上从未产出过判决**，改为 `.to(frames["pos"].device)` + `meta_plain` 反例；② 零命令带在**命令阶跃那一帧**取样（零命令段全起于 10 s 重采样边界），新增 `settle_s=1.5 s`（由实测减速度与箱上界反推）并在报告里给出裁剪前的读数；③ 零命令带位移读数在**全部 env 上求和**却比米限值 ⇒ 读数随批量大小变，改为读最差单个 env。三项均带反例，旧 reader 的语义块未动（其 fail 报告保留）；v1 读者判 fail、修正后判 pass，两次判决的帧数据逐位相同 ⇒ 差别只在尺子 |
| 2026-09-22 | v1 | **启动探针的观测口径再修一轮**（评审：下压动作没真正下发、力在复位后取样、"终止集合一致"只查缺不查多且后缀推导不全）：压头改为经部署动作接口构造并真的 `env.step`，头链另用关节状态钉住；力/项值在 `_reset_idx` 包装里**复位前**同帧读取并打印 `terminated`/`truncated`；终止集合由 yaml `terminations.terms` 点名、探针按等号判；另加 `head/fires-with-the-head-at-the-floor`（力必须能由声明连杆贴地解释）与 `head/fires-only-on-contact`（前面必须有静默帧）；`--shot` 出多帧供目视。结果：站立 0.00 N 静默 / 压头离地 50 mm 起降，前 18 帧 0.00 N 静默、触地那帧 1108.12 N 同帧触发；接触刚度 ≈4×10⁶ N/m ⇒ 阈值是接触检测器，"温和下压穿过 1 N"据此撤回。依据：`acceptance/records/2026-09-22-lizard2-probe-observer-fixes.md` |
| 2026-09-22 | v1 | **命令窗口加宽 0–2 → 0–3 m/s**（所有者指示，同日、开训前）：上界取与旧线相同的 3.0；随 `lin_vel_x` 把分带改为 `[[0.1,1.0],[1.0,2.0],[2.0,3.0]]`、硬前置 2 的区间改 `[0.0,3.0]`、风险第 1 条补 3 m/s 的估算（f≈2.73 Hz、τ≈277 N·m，**超 180 上限约 1.5×**）；cfg_lock golden 与 v1 asset_lock 重钉（`--reason` 记"开训前加宽"）。结论口径随之收紧：**"到不了 3 m/s"不能自动算策略失败**，先看硬前置 4 |
| 2026-09-22 | v1 | **终止条件加一条**（所有者指示）：`head_contact` = `chest_.*`/`neck_.*` 接触力 > **1.0 N**（与 `base_contact` 同一个"接触"值）即终止——平地上"头颈/前胸触地"是第二条"倒下"的路，而旧线 v1 正是被"压着脖子走"拖垮的（neck_pitch 82–90 N、占体重 11.6–12.8%，且 66% 的帧压在 10% 体重之上却从未连续超过 0.22 s ⇒ dwell 型承重判据拦不住它，接触判据可以）。实现只用框架 `illegal_contact`（不加 dwell 核）；yaml 加 `names.head_contact_body_names` 与 `terminations.head_contact_threshold`，元素 `lizard2_base_contact` 更名 `lizard2_contact_gate` 并同时写两条终止；`diff.json` 的路径 64 → **65**、`EXPECTED_DIFFS` 107 → **108**、cfg_lock golden 与 v1 asset_lock 随之重钉（`--reason` 记"开训前修订"） |
| 2026-09-22 | v1 | 验收节重写（评审 #3/#4）：① 判据点名 `CRITERION_KINDS` 种类 + 参数，而不是散文——散文让门槛含义漂移（v4 `why_v4`）；② 姿态判据方向修正：`sustained_tilt_v1` 是"超过阈值连续 ≥ 0.5 s 即跌倒"，活体实现是 `metrics.fall_flags` 的 `sustained_any`，且**任一 env 破线即不通过**，不是比例；③ 零命令口径/位移聚合顺序/失败后帧计入方式写死（旧实现 `clamp_min(1e-6)` 与"逐 env 先除后平均"在含零窗口上不稳定）；④ 非足承重按所有者决定：持续承重升门槛并作为通过条件，瞬时碰撞留诊断；⑤ 跟踪 0.25 定为预注册工程阈值并注明它不是从 0.2 推导的 |
| 2026-09-22 | v1 | 第二次评审后的修正：① **执行器预算**改为"量级估算 + 必须实测"，更正数字（2 m/s ⇒ f≈1.82 Hz、τ≈123 N·m = 68% of 180、峰值 ≈6.9 rad/s；2 Hz ⇒ ≈83%），说明 `velocity_limit` 被本 fork 丢弃（`actuator_pd.py:80-91`）故速度分母不成立、`computed/applied_effort` 只是近似力矩，并新增**硬前置 4**（悬空跟踪 / 承重协调两层 + 上限口径实测）；② **非足承重方向修正**：`fraction` 是破线水平、调高更宽松，新增总量判据 `non_foot_load_sum_v1` 补"分散承重"漏洞，阈值改用正常/异常样本 + 噪声标定；③ 新增**硬前置 5**（自碰撞：本线 `enabled_self_collisions=False`，须查交叠/有符号穿透、区分相邻关节允许重叠、覆盖实际轨迹）；④ 低速带声明为**不可比**（要另跑定点 0.5 的同窗口测试）；⑤ 新增步态判据 `gait_swing_v1`（只在移动命令上判、多指标合取、`min_swing_feet` 由所有者冻结）；⑥ 髋惯量表述更正（有效惯量是整条下游链，1e-6 只是数值条件）+ `*_kfe` 区分"没建碰撞形状"与"惩罚列表保留无效条目" |
| 2026-09-22 | v1 | **两条硬前置（4 执行器、5 自碰撞）跑完并按评审修正**：执行器结论改为"频率能力"——重力关、单关节、其余保持参考下，跟踪误差 0.5 Hz 9% → 3 Hz 50%，PD 估算 3 Hz 仍只有 74.8 N·m（限幅 180）；**撤回**"0.5 Hz 就 163 N·m / Kd 吃掉限幅 / 1.1 m/s 速度上限"（PD 估算符号写反 + 由合成限幅反推速度上限，两处都错）；0–3 保留为探索范围、按带报告，不据此调参或移动通过线。自碰撞：凸包候选 24 条、**逐条网格复核 0 条确认**（最坏真实间隙 +33.6 mm），**撤回**"折进躯干 165 mm"；是否开自碰撞按收益决定 |
| 2026-09-22 | v1 | 初稿（生成器落骨架后写满）：换骨骼 + 区间命令；配方逐项照旧线 v1 冻结值；验收与判废线见下。依据：资产差异记录、行程实测记录、动作/观测实测 |

## 明确不做

地形与地形课程 · 高度扫描与脚环 · 特权观测与蒸馏 · 阶段速度课程与桶命令 · c_k 时钟 ·
任何域随机化与外力/推力 · `head_load_penalty`/`feet_slide`/`foot_clearance` ·
头链触地终止（属别线的 v2 变量）· 三编码器与 obs 归一化 · 多 seed 并行 · 第二臂观测 ·
脚板去动作权限（旧线 v2 的变量）· 侧向/转向命令。

## 验收

**硬前置（开训前必须完成，否则不得开训）**：

1. **冻结本线的评测协议（已完成；2026-09-23 首次判决后又修了两处尺子缺陷）**：
   现行协议 `ablation_harness/protocols/lizard2_flat_v2.json`（`Lizard2-Flat-v2` v2、`judge:
   baseline-criteria-banded-settled-1`、命令箱 `[0,3]`、20 s、plane、逐帧实际下发命令）；v1 协议与
   它那份 fail 报告保留（无 settle 读者的语义原样冻结）。带＝`[[0,0.1],[0.1,1.0],[1.0,2.0],[2.0,3.0]]`，
   零命令带是**声明出来的带**；`non_foot_carrier_v1` 的 `fraction 0.05`/`sustain_s 0.5` 定死并给标定依据
   （正常样本＝探针零动作 40 步非足载荷 0.00 N；异常样本＝压头 1108 N；退役线故障量级 11.6–12.8% 体重 ⇒
   0.05 不贴故障值；**缺"持续部分承重"这一档样本，未测**）。
   **首次判决（`model_13999`、256 envs、seed 123）：`pass`，八条全过**——零命令带 `|v|` 0.045（限 0.15）、
   最差 env 漂移 0.155 m（限 0.3）、三个相对带误差 0.036/0.011/0.0063（限 0.25）、位移比 0.988–1.006、
   存活 1.0、姿态 12.5°、非足载荷全 0、摆动脚数最少 3。判决书与两处缺陷见
   `acceptance/records/2026-09-23-lizard2-v1-first-eval.md`。
   **两处缺陷都是尺子的，不是策略的**：① 零命令带在**命令阶跃那一帧**取样（15 段零命令全起于第 499 帧
   ＝10 s 重采样边界），新增 `settle_s=1.5 s`（由实测减速度 ≈2.5 m/s² 与箱上界 3.0 m/s 反推），报告同时
   给出裁剪前的 `tracking_band_unsettled_max`；② 零命令带的位移读数在**全部 env 上求和**却去比米限值 ⇒
   读数随批量大小变（16 envs 与 256 envs 不同），改为读最差单个 env 的米数。
   **偏离与未完成**：(a) `gait_swing_v1` 去掉 `min_lift_m`——记录里没有足端高度列，而"闸门不许读没测过的量"
   是硬规则，"落地后恢复承重"改由 `min_landing_load` 承担；(b) `min_swing_feet` 现填 **2**，正文说该数由所有者
   冻结 ⇒ **待确认**（实测 4 足里最少 3 只完成摆动、均值 3.84，1 只脚 20 s 内一次完整摆动都没有，属产品判断）；
   (c) 正文要求的"协议不存在或摘要不符 ⇒ 拒绝开训"**启动闸门尚未存在**（`manifest.begin` 的拒绝集只有
   lifecycle 与脏树），形态归 `work/active/eval-protocol-before-training.md`；(d) `report_only` 声明 15 项、
   报告实际产出 8 项（`foot_duty`/`foot_load_fraction`/`feet_down_mean`/`foot_yaw_deg`/`foot_slip_mps`/
   `dof_torque_frac_of_limit` 无人计算），要么补计算要么收窄清单，**未修**；(e) 逐带判据要求每带在 `alive`
   帧上被覆盖，而 `baseline_eval.py` 现在用环境自己的 10 s 重采样 ⇒ 零命令带的覆盖率依赖采样（本次 256 envs
   下 15/256 个 env 落在该带，6370 帧）。
2. **启动探针按本版接口复测（已改造；观测口径 2026-09-22 再修一轮，见
   `acceptance/records/2026-09-22-lizard2-probe-observer-fixes.md`）**：`baseline_probe.py` 不再硬编码任何一条线的期望——
   命令框、观测宽度、终止集合都取自**任务自己的参数文档**（`recipe_params.load(cfg.params_line, cfg.params_version)`、
   `obs_protocol.recorded_dims(task)`），`resolve_task_cfg` 也不再只认 `lizard/baseline` 一条线。
   在 `Lizard2-Flat-Play-v1` 上实测通过：命令逐 env 落在 `[0.0, 3.0]`（8 envs，spread 2.5 m/s）、
   `lin_vel_y`/`ang_vel_z` 恒 0、无 standing/heading env、obs `policy=102`（与批准宽度一致）、
   30 个关节各恰好一个动作通道且维数一致。
   - **终止集合改由 yaml 点名、探针按等号判**：`terminations.terms` 列出生效项（`base_contact`/`head_contact`），
     生效集合 == 该列表 ∪ 框架自带 `time_out`，多与少都红；旧的 `*_threshold` 后缀推导覆盖不了 dwell / 速度闸
     等其它参数形状的终止项。
   - **压头与取样已按评审修**：压头经**部署动作接口**构造并真的下发（旧版构造了 `press_action` 却仍 `env.step(zero_action)`）；
     头链另用**关节状态钉住**——只下发动作时驱动在下巴自重下饱和、机降 283 mm 的同时链回缩 0.3 m，即"命令的姿势"
     不是"保持住的姿势"；力与终止项在 `_reset_idx` 包装里于**复位前**同帧读取（`env.step` 返回前会复位并清零接触
     传感器缓冲，返回后读到的是**下一回合**的力，旧读数因此假报 0.00 N），并打印该步返回的 `terminated`/`truncated`。
     对照（同一台仪器，边界两半都在）：站立 40 步头部力峰 **0.00 N、守卫不触发**；压头从**下巴离地 50 mm** 起降，
     **前 18 帧 0.00 N 静默**，下巴触地那帧力 **1108.12 N 且同帧**触发（窗口最大同值、该帧头部网格进入地面 0.3 mm），
     `terminated=True`、`truncated=False`、`base_contact` 不在其中。**接触是硬的（≈4×10⁶ N/m ⇒ 1 N ≈ 1 µm 穿透）**
     ⇒ 该阈值是"接触检测器"不是"力门限"；据此**撤回**"温和下压穿过 1 N 仍欠着"（斜坡给不出该读数，也不需要）。
   - **死条目已清（单独一次清理）**：yaml 的 `head_contact_body_names` 由 `chest_.*`/`neck_.*` 收窄为
     `chest_pitch`/`neck_pitch`——原模式同时命中的 `chest_yaw`/`neck_yaw` 没有碰撞网格，接触力结构性为零，
     那半个守卫永不触发。golden 与 v1 asset_lock 随之重钉（差异恰好 2 条路径 × 2 个 task）。
   - **零动作目视**：`--shot`（需 `--enable_cameras`）把零动作回合的多帧写到 `_tmp_zero_action_shots/`；
     已看末帧 = 8 台平地站姿、四足平贴网格、无可见穿地。静态帧不证明动态稳定，动态侧仍以数值为准
     （base z 均值 0.937 m、tilt 均值 0.43°、载荷全在四足）。
3. **落地姿态目视**：本版是 plane，目视只需确认零动作站姿与脚掌接触正常（碎石/地形那两条不适用）。
4. **执行器响应曲线（已跑，见风险第 1 条与 `2026-09-22-lizard2-actuator-capability.md` 修正版）**，两层都记读数：
   - **unloaded**：`cfg.sim.gravity=(0,0,0)`（真无载），**只驱动一个**指定关节、其余保持起始参考姿态，
     记跟踪误差（rms/峰值/超差步占比）、峰值角速度、PD 估算力矩、以及**单独一列**惯性需求估算。
   - **loaded**：重力开、放在平面上、四腿划水，记实际前进速度（**下界，不是判据**）。
   - **上限口径实测**：`velocity_limit_sim=None`（本 fork 丢弃 cfg 的 `velocity_limit`）、`effort_limit_sim=180`；
     `applied_torque`/`computed_effort` 对本线**结构性为零**（implicit 执行器）⇒ 只报 PD 估算并标注；
     **不得**由 effort/velocity 反推速度上限（限幅作用于合成力矩，P 项可抵消 D 项）。
5. **自碰撞：筛选干净、无确认穿透（2026-09-22，`acceptance/records/2026-09-22-lizard2-self-collision-sweep.md` 修正版）**：
   凸包筛选在 lizard2 的 132 个限位内姿态里报出 **24 条候选**（旧族 60 姿态 0 条），但**逐条真实网格复核后 0 条确认**——
   最坏候选（`chest_pitch` vs `lf_foot`，全折叠姿态）真实间隙 **+33.6 mm**。**撤回**先前"折进躯干 165 mm"的说法
   （那是凸包含裕量，不是穿透）。仍然成立的两条事实：本线 `enabled_self_collisions=False`（训练期不惩罚自接触）、
   `*_kfe` 无碰撞网格使奖励里那条 `.*_kfe` 永不触发。
   **是否开自碰撞按收益决定**（收益＝物理上堵住不合理折叠；代价＝求解器接触开销），不因本记录而必须开；
   收窄 `hfe`/`kfe` 限位属资产改动（须重测行程），暂不建议。若开，随后要重测站立、跟踪与行程。
   **所有者决定（2026-09-22）：本版不开**。理由三条：① 极限姿态 24 候选逐条网格复核 **0 条确认**，
   即靠 URDF 限位基本折不出自穿，开了没有合法目标可抓；② 候选 24/24 都是**凸包过近似**（最坏那条凸包含裕量
   −164.7 mm 而真网格 +33.6 mm 分离），开 = 把"看着分离"的对变成真实力，引入不在奖励/终止设计内的推力；
   ③ 变量代价：开会改物理 ⇒ golden/lock、探针、执行器曲线全要重测，而本版要回答的是"新轴线能不能学会走"。
   **改判条件**（下一版用）：训练后诊断在**实际轨迹**上给出正穿透 ⇒ 必须开或修几何；或认为"极限姿态乘积"
   采样太粗、可达集未覆盖 ⇒ 先扩扫描再谈；真要开就按单变量做（重钉 + probe 重测 + off/on 对照）。

**固定窗口**：每个 env 固定评测 20 s 的**首回合**；**失败后不得重建 env 续算位移**，**不得只截取失败前的帧**；
确定性策略（`-Play`）与训练采样策略**分开报告**；无 checkpoint 的零动作运行标 `smoke_only`，不算通过。
（口径与旧线 baseline v1 一致，见 `ablation_harness/baseline_eval.py` 的协议族；本线的协议文件待冻结。）

**主轴（下列各条同时成立才算通过）——判据点名 `CRITERION_KINDS` 的种类 + 参数，不写散文**（v4 协议族的理由：
散文让"门槛 key"在不同版本间含义漂移，而种类自带比较、单位与聚合顺序）。**其中四条现有种类不够用，
冻结时必须新增，不许改既有种类的语义**（改了会同时改旧线判据；v4 的 `why_v4` 已把"新观察需要新种类 +
新 judge id"写成规则）：

| 轴 | 种类 + 参数 | 现成？ | 聚合与失败后帧 |
|---|---|---|---|
| 姿态 | `sustained_tilt_v1 {threshold_cos 0.766, sustain_s 0.5}` | ✅ | 逐 env：`alive` 期内出现 `cos(tilt) < 0.766`（即 tilt > 40°）**连续 ≥ 0.5 s** 即跌倒；**任一 env 跌倒即不通过**（`any`，不是比例）。这是"不得出现"的判据，不是"至少稳住 0.5 s" |
| 存活 | `survival_v1 {threshold 0.9}` | ✅ | 未终止 env 占比 > 0.9；失败后帧的速度/位移按最差计入（见下两行） |
| 跟踪 | `tracking_banded_v1 {threshold 0.25, floor_mps 0.5, zero_band_mps 0.1, zero_abs_mps 0.15, bands [[0.1,1.0],[1.0,2.0],[2.0,3.0]]}` | ❌ **新增** | 逐帧：`v_cmd ≥ zero_band_mps` 的帧按 `\|v_fwd − v_cmd\| / max(v_cmd, floor_mps)`；`v_cmd < zero_band_mps` 的帧**单独成一条"零命令带"**、不参与相对误差，改按绝对口径 `\|v_fwd\| ≤ zero_abs_mps`，且**只在 `alive` 帧上算**（否则跌倒后速度≈0 会白拿"站得住"的分）；失败后帧在相对口径里记 **1.0**；**逐带全过才算过**（含零命令带），并报告每带帧数——带与带不重叠，所以没有"同一帧按两种口径各算一次"的含糊 |
| 位移 | `displacement_banded_v1 {threshold 0.8, zero_band_mps 0.1, zero_abs_m 0.3, bands 同上}` | ❌ **新增** | 聚合顺序写死为**先按带求和再相除**：`Σ_env 实际前向净位移 / Σ_env Σ(v_cmd·dt)`（全窗口、用实际下发命令）；零命令带单独按绝对米数验收 `\|实际\| ≤ zero_abs_m`；**失败后帧的命令仍进分母**（命令照发、位移没有 ⇒ 比值自然被拉低），不允许截掉后半窗 |
| 非足承重（单部位） | `non_foot_carrier_v1 {fraction, sustain_s}` | ✅ | 逐帧逐 body 比阈值、**body 维先 `any`** 再判持续（`_gate_non_foot_carrier_v1`）⇒ 管"某一个部位独自承重"。`fraction` 是**破线水平**：调高更宽松 |
| 非足承重（总量） | `non_foot_load_sum_v1 {fraction_sum, sustain_s}` | ❌ **新增** | 逐帧把**全部非足 body 的竖直向上载荷求和**再比阈值、再判持续 ⇒ 补上"两个部位各 8%、合计 16%"绕过单部位门槛的漏洞 |
| 步态摆动 | 要求 `gait_swing_v1 {min_swing_feet, min_air_time_s, min_lift_m, band_mps}`；**实交** `{min_swing_feet, min_air_time_s, min_landing_load, band_mps}` | ⚠️ **交付缺项** | 只在 `v_cmd ≥ band_mps` 的帧与完整窗口上判（零命令下四脚着地属正常）；要求"每条摆动脚同时满足**离地高度**、持续离地、**落地后恢复承重**，并报告支撑期足端滑移"。**`min_lift_m` 没有实现**（协议 `why_v1` 记的冻结理由是"记录里没有足端高度列"——该句对**判决帧记录**成立，但它只说明缺一列，正确处置是**给记录加列 + 新种类**，不是删判据。冻结件不改字：它的 sha256 被报告 pin 住，事后改字会让报告对不上文件；更正记在步态记录里，由下一次协议版本的新 `why` 条承接）⇒ 现判据只问"有没有动一下"；`foot_slip_mps` 同样声明而未计算。**探针一侧已有的"高度/滑移"列不是这两项**（那是足端原点相对自身最低点的高度与足端刚体速度）⇒ 脚底净空与接触点切向速度仍属未测，见记录"更正与撤回"。**实测读数与出处见 `acceptance/records/2026-09-23-lizard2-v1-gait-skate.md`**（本行不复制数值）。修复归下一次协议版本 |

**旧实现的三处不适用（冻结时必须一起解决，不能照抄 `baseline_flat_*.json`）**：

1. `tracking_v1.normalized` 的分母是 `command.clamp_min(1e-6)`（`ablation_harness/baseline_metrics.py`）：
   在含零窗口上把误差放大成噪声；`displacement_v1.normalized` 是逐 env 先除后平均，`expected_m` 接近 0 时
   比值不稳定。上表的 `floor_mps` 与"先和后除"就是替换它。
2. 分带报告是**判据的一部分**，不是补充项：总均值会掩盖"低速段不走 / 高速段跟不上"这类分段失败。
3. 零命令段必须**写在判据里**（绝对口径），不能靠"反正命令很少"忽略掉。

**非足承重：两条判据、两个都作通过条件（评审 #2 方向修正）**

- `non_foot_carrier_v1`（现成）管**单部位**：逐帧逐 body 比 `fraction`、body 维先 `any` 再判 `sustain_s`。
- `non_foot_load_sum_v1`（新增）管**总量**：逐帧把全部非足 body 的竖直向上载荷**求和**再比 `fraction_sum`、再判持续。
  没有它，"两个部位各 8%、合计 16%"会无声通过。
- **方向**：`fraction`/`fraction_sum` 都是**破线水平**，**调高更宽松**（"抬 fraction 更严"是反话，不作数）。
- **标定**：两个阈值**不得贴着历史故障值选**（旧线 v1 的失败量级 11.6–12.8% 体重）。冻结时用**正常样本 + 已知异常样本 + 噪声**
  标定——健康站立（应远低于阈值）与"脖子压地"复现（应显著高于阈值）——并把标定样本/读数记进记录。
- 载荷聚合（逐 body 竖直反力的和）与接触归属（"非足" = 除 `*_foot` 外全部 link）冻结时写死；后者按本线资产说，换家族要重述。

**补充报告项（明确不作通过门槛）**：侧向速度绝对值、相对初始朝向的 yaw 偏移、逐步 foot duty / 脚载荷份额、
非足 body 的**瞬时**接触与载荷（`limit_n` 口径）、接触期**脚偏航与滑移**（本构型髋摆腿会连脚板朝向一起摆，
是新轴的固有副作用，至少要有读数）、每 body 网格最低 z（`mesh_clearance_v1` 只有在有实测校准值时才可用；
入地读数不得当允许阈值）、动作均值/差分/限位占比。

**跟踪阈值 0.25 的性质（所有者 2026-09-22 的决定）**：作为**本版预注册的工程阈值**接受；**它不是从旧版 0.2
推导出来的**（窗口不同、归一不同），依据只是"区间下界含零、按 0.5 归一后允许更松"。因此配套要求：
**固定评测命令覆盖**（逐 env 固定基命令序列 + 逐帧记录实际下发值）并**分低速/高速报告**，避免总均值掩盖分段失败。

**低速带 ≠ 旧线的定点 0.5（评审 #4）**：本版低带是 `[0.1, 0.5)`、命令随 10 s 重采样变化，**与旧线"定点 0.5"不可比**。
要拿到可比切片，必须另跑一个**同初态、定点 0.5、同窗口、同评测模式**的测试；即便如此，两个方案的**训练命令分布不同**，
结论仍只到"两个训练方案在同一测试上的表现差异"——**不能**由此恢复"骨骼是唯一变量"的归因。

## 启动方案（计划命令）

```bat
python scripts\reinforcement_learning\rsl_rl\train.py --task Lizard2-Flat-v1 --num_envs 4096
```

开训前：工作树必须干净（脏树开训被硬拒，文档脏不拒）、`check_cfg_lock.py --update --line lizard2/main --reason "..."`
（如参数有动）、冻结状态打锚点 tag（本线现为 `lizard2-main-v1.6`；裸锚点 `lizard2-main-v1` 已于 2026-09-22 删除）。
**上述硬前置未完成前不得开训**——本线 2026-09-22 的开训是在**第 1 条的启动闸门尚缺**的情况下按所有者决定启动的
（`--max_iterations 14000`，其余硬前置均已完成）。

## 风险与挂账（预注册）

1. **执行器响应已实测，结论是"频率能力"而不是"速度上限"**（`acceptance/records/2026-09-22-lizard2-actuator-capability.md`，2026-09-22 修正版）：
   重力关、只动一个髋（±0.6 rad 正弦）、其余保持参考姿态下，跟踪误差 rms/幅值 = 0.5 Hz **9%** → 1 Hz **18%** →
   2 Hz **35%** → 3 Hz **50%**；PD 估算力矩 3 Hz 也只有 **74.8 N·m**（限幅 180），峰值 |qd| 9.15 rad/s。
   生效限幅实测：`legs` Kp 800 / Kd 40 / **effort 180** / **velocity_limit_sim 无**（cfg 的 10 rad/s 被本 fork 丢弃）。
   承重"划水"（不是步态）最好一档 1.5 Hz → **+0.49 m/s** 均值。
   **两处撤回**：① "0.5 Hz 就 163 N·m、限幅被 Kd·qd 吃掉"——那是 PD 估算**符号写反**造成的虚高（正负相加），实测 **12.4 N·m**；
   ② 由合成限幅反推"纯摆动速率上限 4.5 rad/s / 可行速度 1.1 m/s ⇒ 0–3 上界超能力"——限幅作用于**合成**力矩、P 项可抵消 D 项，
   不能这么反推。**本版不据此调整执行器参数、也不据此移动通过线。**
   仍然成立的只有：单关节全幅 ±0.6 rad 到 2 Hz 起明显跟不上；这**不是**对 0–3 窗口上界的判决。
   **0–3 按所有者意见保留为探索范围**，验收按带报告能力（逐带全过才算过）；若要"只判可达部分"，必须**开训前**定义。
2. **变量不隔离**：本版相对旧线 baseline v1 同时改了骨骼与命令窗口。"骨骼不行"与"区间命令太难"无法区分；
   分带报告给出的低速切片**不是**单变量对照（低带是 `[0.1,0.5)` 且命令在变，见「低速带 ≠ 旧线的定点 0.5」）。
   **本版结论只到"新构型 + 本配方"。**
3. **判废线（开训前写死，事后不改）**：
   - 若表现为**几乎不动**（`|v_fwd|` 时均 < 0.2 m/s 且位移比 < 0.3）或**存活 < 0.5**，
     判"骨骼 + 配方组合不成立"，**先复测资产与几何（行程/力臂/接触归属）再谈调参**，不得先改奖励权重。
   - 若 `non_foot_carrier_v1` 或 `non_foot_load_sum_v1` 破线（非足部位**持续**承重 / 非足载荷总量超标），
     判"没学会走"——两条都**是通过条件**，不给最终 pass、也不折进"过口径但有瑕疵"；下一版要改的是机制
     （终止/奖励/动作权限），不是重读读数。
   - 若训练期命令实际下发值被改写（硬前置 2 未过），该 run 作废——这不是调参问题。
   - 若硬前置 4 的层二证明承重下到不了区间上界，本版按**实测可达上界**重述问题与判据，不把"达不到的那一段"
     记成策略失败。
4. **无 DR ⇒ 策略不可直接部署**：本线产物是**能力基线**，鲁棒性是后续版本要逐项加回的变量。
5. **继承旧线 runner cfg**：旧线 PPO 一处改动会同时改本线；这是有意的同配方约束，但要在改动当时意识到。
6. **30 维动作与 102 维观测是新的尺寸**：checkpoint 与 UE 导出都按此宽度，跨族 checkpoint 不可互换
   （观测宽度不同即加载失败，这是设计而非缺陷）。
7. **头链与尾链的 PD 参数照抄旧线**（`150/10`）：新骨骼只在四条腿上加了轴，躯干链未动；若首跑出现
   头/尾振荡，先读探针的实际量级再判断，不凭现象改刚度。
8. **髋阶跃测试（评审 #6 更正）**：髋执行器驱动的是**整条下游链**，其有效惯量**不是** dummy link 的 `1e-6`；
   微小惯量只可能影响数值条件，**不能**据此推断"PD 必然过硬"。阶跃测试保留，记录**超调、稳定时间、限幅、
   时间步敏感性**。`*_kfe` 无碰撞形状这件事要说清两层：**"没建碰撞形状"**（几何缺失）与
   **"惩罚列表保留了一个永不触发的条目"**（`undesired_contacts` 里的 `.*_kfe`）是两个问题，
   后者不能靠删条目了事——要么补碰撞形状，要么在 yaml 里说明为什么保留。
9. **头守卫的触发已观测，且它测的是"接触"不是"力的大小"**（2026-09-22）：站立时头部够不到地（极限头姿下
   头部网格最低点仍高出 0.478 m），下压是把鼻子朝下的机体送到地面。实测接触刚度 ≈ **4×10⁶ N/m**（触地帧
   1108 N / 0.3 mm 穿透）⇒ **1 N 只值约 1 µm 穿透**，任何姿势斜坡下力都从 0 直接跳到数百牛，**不存在**"从下方
   缓慢穿过 1 N"的读数。故 `head_contact_threshold=1.0` 的语义是**接触检测器**：判据实际判的是"下巴有没有碰到地"，
   与 1.0 这个数无关（改成 0.5 或 5.0 不会改变任何一次触地判决，除非有人去测 µm 级穿透）。据此**不得**由本版
   结论推断"阈值选得合适/不合适"，也不得据此调阈值；已证的边界是"离地则静默、触地即触发"。
