# ARCH_PLAN —— 实验可复现性与配置组织重整（提案，待审核）

> 意图文档，非 SSOT。事实侧见 `FAMILY.md` / `OBS.md` / `HARNESS.md` / `FILEMAP.md`。
> 最新修订：**v0.23（2026-09-17，去状态化）**。本文件只答"要做什么、判据是什么"。
> **本文不承担"当前状态"**：标题、表头、出口一律写判据，不写"已完成/已落地/待实现"；正文只写**当时证据与实现形态**（带日期），不写"当前通过与否"。当前状态一律以 `versions/lizard/ACCEPTANCE.md` 为准。**别在本文手工同步状态**（改一次就分叉一次）。
> 现状 → `FILEMAP.md` / `FAMILY.md`；修订史 → `git log -p ARCH_PLAN.md`。挂账归 `PLAN.md` #14，归档敞口 #18。
>
> **读法**：§速读 → §详案 → §不做。附录只留已删内容去向。

## 速读

**目标**：一次实验绑定明确的**代码版本、输入数据与资产版本、最终运行配置**；恢复训练**延续已声明覆盖的学习与课程状态**；新实验用**显式配方**表达变化。

**总判据**：宁可承认"某项条件不可证"，也不要"全绿却仍不能证明实验条件一致"。

**不变量**

1. 已发布任务 ID 与公开入口保持兼容（命令、类名、`class_name` 点路径可加载）。
2. **在指定代码 + 框架 + 资产版本组合下**，历史配置构造语义不变。任务 ID 未变 ≠ 实验可复现。
3. checkpoint 加载兼容性**单独验证**（载荷键、协议 id、状态格式版本）。
4. 历史实验的重建能力**按证据等级声明**，不默认宣称"可复现"。
5. teacher 零家族 import；已发布 term 不改语义；冻结目录只读。
6. 不新造训练框架；上游补丁只减不增（现 3 个）。

**三阶段一览**

| 步 | 做什么 | 出口（判据） |
|---|---|---|
| 1.0 | `params_version` 字段性质实测 | 结论落到"字段 vs 类属性"二者之一，它决定整改形态（提成显式入参并保字段与日志面）；闸门进套件 `[21]` |
| 1.1 | 配置快照（recipe/run）+ 值差异报告 + golden + 闸门 | golden 覆盖全部注册任务与 PLAY、按框架组合保存且**不为 run 增长**；改一个叶子只动一行；`--update` 无 `--reason` 拒写（套件 `[23][24][25]`） |
| 1.2 | 会话记录 T0–T3 + 外部 ckpt 索引 + 两维度 `--verify` | T1 在 `learn` 前冻结；构造/恢复失败无有效 T1；引用漂移必失败；记录的是**恢复后实际生效**的 lr（套件 `[26]`） |
| 1.3 | 状态注册表（joint SIR + 行 SIR + c_k）+ 边界收紧 | 逐项核验后**原子回填**（半恢复禁止）；缺证据列未知、不以当前值补造；声明任务在接线缺失时训练前终止（套件 `[15]`） |
| 1.4 | 恢复验收三层（A/B/C） | A 层证载荷无损、B 层证"状态→更新"映射（分支级 + c_k 迭代边界）、**接线时序只能由 C 层真跑证** |
| 1.5 | 恢复演练（**条件触发的独立验收**，不进主出口；日常运行仍走路径配置连现有环境） | 演练四步齐 + 只对"声明重建"的材料做来源断言 + 缺件负测试；按次声明"重建/复用"清单与结论强度（`[27]`） |
| 2 | 显式配方化 + 实验线生命周期 | 硬 A/B/C 与 L01–L06 全过；新配方不要求数字命名，历史入口保留 |
| 3 | 协议对象 + eval 四项绑定 + 地形映射 A→B + 导出校验 | 协议摘要进 manifest 与 eval |

**六条硬约束**（易被绕过的都在这里）

1. **两维度**：证据等级（记录完整/可重建/已验证重建）× 检查结果（通过/**失败**/未知）。失败阻塞验收；未知允许推进不依赖它的工作但**不计通过**；**必需硬验收项未知 ⇒ 该步不得宣告通过**；历史非必需追溯项未知不阻塞。
2. **重建评级要写范围**：重建目录与依赖（见 1.5）+ 验证项清单 + 环境 + 比较方法 + 容差 + **是否做了系统级访问切断**（未做则范围限定为"受检查路径"，措辞即用该限定词）；不推导未验证的行为等价。关键重建检查失败 ⇒ 不得再标当前"通过"（旧通过记录保留 + 注明时间与对象）。
3. **无自引用哈希**：文件哈希不写进文件自身；`infos` 只放 run id + T1 manifest 摘要 + 迭代号 + 载荷摘要（声明非文件哈希）。
4. **不用豁免掩盖字段面变化**：硬 A 基线 = 整改前 golden；确需改字段面 → 逐**字段路径**白名单（旧值/新值/转换规则/语义无影响验证），**禁止整字段或整子树豁免**。
5. **manifest 不可变**：T0 启动证据 → 环境构造阶段快照 → **runner 构造、恢复与实际来源核验完成，`learn` 前** T1 最终 manifest 冻结 → T2 引用 T1 的 SHA-256；索引与验证结果另存不回写；T1 前任一步失败均保留已有证据并标注未完成 T1。
6. **不补造历史**：不改写原始记录；历史结论只有"按证据判失败/未知"两种，且追加说明。

**待定已结 2 项**（用户拍板 2026-09-15）：① 归档 = **先不归档**（走 c）——凡依赖"可取回"的声明一律标**未知**，敞口登记为 `versions/lizard/PLAN.md` 挂账 #18，触发条件是"第一次要'已验证重建'评级或第一次要声明地形产物一致"；② golden 体积**接受**，但须按明确版本组合保存、不为每个 run 增长、文本格式适合 Git 差异、**基线更新必须审查具体差异且禁止自动覆盖消警**（已按此改 lock 格式，见 1.1c）。

## 详案

### 验收记录通则

以下新增标准是实施要求，**不代表测试已实现或已通过**。每次验收保存：检查编号、任务 id、命令、代码/框架与资产摘要、设备与 env 数、seed、输入 checkpoint/样本摘要、预设容差、实际结果、证据路径和通过/失败/未知。必需项未跑、样本前提不满足或证据缺失记未知，不能算通过。回归负测试须先证明旧实现失败，再证明修复后通过。

下文步数与数值容差是本次补充的默认验收口径，不是已测结论；执行前冻结在验收记录中。确需调整时先说明原因并保存新口径，不得看结果后放宽阈值消警。各阶段独立验收，不能用离线通过替代真跑或恢复演练。

### Step 1 —— 保护当前结果

**1.0 实测 `params_version`（2026-09-15；闸门 `rl_exp/tools/verify/check_configclass_fields.py`，入离线套件 `[21]`）**

扫描 `rl_exp/tasks` 全部 34 个带 `params_version` 的配置类，实测结论：

- **是 dataclass 字段（34/34）**。机制：装饰器先补齐注解（`configclass.py:99`），每个成员被换成 `field(default_factory=...)`（`:475`），`dataclass()` 包装（`:115`）时类属性被删除。
- **因此类属性读不到**：`getattr(Cfg, "params_version")` 抛 `AttributeError`，值只存在于实例上。源码注释（`teacher_env_cfg.py:490` 称"普通类属性、非字段"）与实测不符，按实测记。
- 字段进入 `to_dict()`（34/34），`from_dict()` 往返后取值不变 ⇒ 日志面与从日志回放不受影响。
- `None` 不是漏配：`_load_params(None)` 读家族 live dev yaml（`lizard_env_cfg.py:53-54`），非 teacher 家族基线类如此。
- `env.yaml` 等价性：`train.py:273` → `dump_yaml` → `class_to_dict`（`utils/io/yaml.py:53`），而 `to_dict` 即 `class_to_dict`（`configclass.py:109`）⇒ **实例 dump 与 `to_dict()` 键集相同**。本机未见 `params/env.yaml` 样本（已搜本仓与 `e:\`），闸门留 `--env-yaml` 供样本出现后实测。

**整改形态（Step 2 依据，走"提成显式入参"分支）**：优先保留字段、默认值与序列化兼容性（CLI 覆盖、`from_dict` 回放、日志读取均不受损）；禁止任何 `getattr(cls, 'params_version')` 式读取（类上不存在）；不得借整改删字段或改子树；确需改字段面 → 走 §2 硬 A 白名单。

**1.1 配置快照**（产物 `versions/lizard/vN/cfg_lock.json`，闸门 `check_cfg_lock.py` 入离线套件）

三类产物分开，**离线 cfg 工具不得被迫实例化环境**：

| 产物 | 内容 | 时机 | 需环境？ |
|---|---|---|---|
| ① 配置快照（recipe / run 两份） | env/agent cfg 字段树；run 另记录实际解析的模型、算法与恢复后生效值 | recipe：`gym.make` **前**；run：T1；环境构造后留阶段快照 | recipe 否；run 需环境与 runner |
| ② 生成结果映射 | 列 → sub-terrain 名（`<type>\|<levels>`）→ 参数 | 地形生成后 | 需生成 |
| ③ 课程状态 | `_n_levels`/`_combo_cols`/particles/权重/时钟/env_pair/desired_vel | 运行时 collect | 需 manager |

①的序列化规则（不排序语义序是重点）：

- 顺序有语义（obs 组/term 序、action 序、joint/foot 序、地形列→combo 序、`sub_terrains` 键序）→ **有序条目并纳入摘要**。
- 浮点无损往返（`float.hex()`/`repr`），禁定长截断。
- callable → 稳定标识（模块路径 + qualname + 类名），代码版本单独绑定；不存地址/`repr`。
- 缺失 ≠ None（哨兵 `__MISSING__`）。
- 路径相对化（`<ISAAC_ROOT>`/仓根）；资产走内容哈希。
- 构造后被替换的 `cfg.func`（`gym.make` 时被换成实例）→ run 快照只记类型 + qualname。

**1.1a 小范围验证（2026-09-15；`cfg_snapshot.py` + `test_cfg_snapshot.py`，入套件 `[23]`）**：规则在真实 v14 树上落地并逐条可失败。实测要点：

- **必须遍历实例命名空间，不能遍历声明字段**：`V3.__post_init__` 用赋值建 obs 组（`self.observations.extero = ...`，`teacher_env_cfg.py:976`），这些组不在 `__dataclass_fields__` 里 —— 按声明字段遍历会**静默丢掉 proprio/extero/priv**，正好是本方案要防的失效模式。现已按 `vars()` 序遍历，并把"声明了但实例上没有"的字段写成缺失哨兵。
- **规则 4 的真实来源是 agent cfg**：env cfg 装不下 `MISSING`（默认值在装饰期就报错，`configclass.py:304`），但 `RslRlOnPolicyRunnerCfg` 的 `stochastic`/`init_noise_std`/`obs_groups` 等字段就是未设值、待 runner 填。**且必须按类型判定**：构造期深拷贝会造出新的 `_MISSING_TYPE` 实例，`value is dataclasses.MISSING` 对这些字段全为 False（1.1b 实测：整个 agent 树因此退到兜底分支，把对象地址写进摘要）。
- **摘要必须跨进程稳定**（同进程重复构造相等不足以证明）：兜底分支不得输出含 `0x...` 的文本；已加双进程（不同 `PYTHONHASHSEED`）比对。
- 浮点按 `repr` 往返（含 1e-320/5e-324/-0.0/1e308 位级一致）；非有限值走标签（裸 `NaN` 在 `allow_nan=False` 下本就非法）。
- 顺序纳入摘要：把 extero 组倒序后摘要必变。
- 摘要可跨机：v14 摘要 49.9 KB，仅剩外部内容 URL（`omniverse-...s3...`），无遗留绝对路径；外部 URL 原样保留。
- `class_type` 等已是 `ResolvableString`（`module:qualname`）⇒ 原样作字符串，不再包标签；`slice`/tuple 等真实数据确实走到标签分支（非死代码）。

**1.1b golden 注册表（2026-09-15；`check_cfg_lock.py` 入套件 `[24]`，负测试 `[25]`）**

- **落点改为家族级单文件 `versions/lizard/cfg_lock.json`**（原计划写 `versions/lizard/vN/cfg_lock.json`）：`versioning.mdc` 红线"冻结目录只读"且 `check_version_docs.py` 看守 vN 的五件套，而 golden 是随树变化重生成的闸门产物，不该写进冻结目录。条目按 gym 任务 id 索引，每条自带 `version`。（**现状**：路线级 `entries` + 共享 `versions/cfg_baselines.json`，`lock_format` 3；"不写进冻结目录"这条结论不变，变的只是落点粒度。）
- 覆盖 **34 个注册任务**（含全部 `*_PLAY`、parkour 线）；条目含 env+agent 解析后的字段树、摘要、`params_version`、类引用。体积 **2.9 MiB / 1 个框架组合**（体积已获用户拍板接受，增长约束见 1.1c）。
- **离线**：从 `gym` registry 取任务，按 `env_cfg_entry_point` 解析类并**只构造**，绝不 `gym.make`（不碰 sim）。
- 校验项：摘要一致 / golden 条目自洽（摘要与自带快照必须匹配，防手改）/ `params_version` 与条目一致 / **任务 id 声明的版本与 cfg 实际加载的 yaml 一致**（`Lizard-Rough-vN` ⇒ `params_version == vN`）/ 条目与注册任务一一对应 / 快照格式版本一致。
- **值差异报告**：`--vs-upstream` 对 `LocomotionVelocityRoughEnvCfg` 默认逐字段比值，并**按 MRO 归因到"最后改这个值的版本"**（`__configclass_own_fields__` 只说明字段声明处，说明不了值来源，故不用它当来源列）。v14 实测 121 处差异，归因 base 66 / V3 24 / V5 22 / V4 3 / V14 3 / V13 2 / V10 1，且 V11/V12 不出现（与 `V14←V13←V10` 绕开 V11/V12 一致）。
- 实测发现（已修）：上述 MISSING 深拷贝与地址泄漏两条；另外**首版摘要跨进程不稳定**导致 34 个任务全报漂移——没有跨进程比对就发现不了。
- 生成时记录 `created_at` / `reason` / `created_rev` / `created_dirty`：dirty 树生成的 golden **不是干净基线**（重生成后归假）。

**1.1c lock 格式（v1 → v2 → v3）**

- **落点分两层（v3）**：`versions/cfg_baselines.json` 只存 `baselines[框架组合]`，`versions/<family>/<line>/cfg_lock.json` 只存该线的 `entries[任务 id]`。动机是**改写范围**：`--update` 原为全量写，给一个新任务建 golden 会**静默吸收**其它任务的漂移并挂在同一条 `--reason` 下；现 `--update` 强制 **`--line`** 且只写该线文件（负测 `update/did-not-touch-other-line` 得证）。**线归属是声明式的** `params_line: ClassVar[str]`（`ClassVar` 被快照格式 2 排除 ⇒ 34 任务 golden 零漂移；缺声明 / 未知线 / 缺锁报红），不再靠任务名或入口路径猜。**发现规则唯一入口** = `recipe_lines.py`（约定 `<line>_params.yaml`；零份 / 多份 / 名字不符 / 父目录非 `vN` **抛错不跳过**），`check_dr_parity`/`check_cfg_lock`/`check_version_docs`/`check_recipe_registry` 共用。
- **增长只由框架组合驱动，不由 run 驱动**：组合键 = `isaaclab=<rev>|rsl_rl=<editable rev 或 distribution 版本>|python=<ver>`（据 `runrecord/provenance.py`，与运行记录同一套 provenance 规则）。entries 只认注册任务 id，**跑 200 次训练也不增一行**；只在框架组合变化时新增**独立 block**，旧 block 保留（老组合必须仍可查）——**格式变更属另一条路径**：同一组合块被就地重写，旧内容以 **git 历史**为保留手段（格式 1 旧基线的 commit / 路径 / 摘要 / 取回方法见 `versions/lizard/ACCEPTANCE.md` §1.2b「golden 基线来源补记」）。另有结构校验：entry 若夹带 `run_id` 之类 per-run 字段即报失败。
- **文本按 Git 差异设计**：JSON `indent=1` 一 key 一行、键序 = 语义序（来自配方而非排序）、浮点 `repr` ⇒ **改一个叶子只动一行**，没改就一行不动；并有"重复落盘字节一致"断言。
- **禁止自动覆盖消警**：`--update` **必须带 `--reason`**，先打印**逐字段差异**（不是只说"摘要变了"），无 reason 直接拒绝且**不写文件**；框架组合变了但没有对应 baseline ⇒ 报失败并给出"另建基线"的指令，绝不就地重写。
- 迁移实测：v1 → v2 内容中性（34 个任务快照与摘要一字未变）；v2 → v3 按**搬运**执行并逐条校验（32/2 拆分主线/支线，34/34 条目字节一致），**未用 `--update`**（它会触发全量重写，正是要防的）。

**体积已决**（用户拍板 2026-09-15）：接受全字段树（review 友好的逐行 diff），但增长只许由**框架组合**驱动（见 1.1c）；"只存摘要 + 差异子集"（约 50 KB）作为备选保留在案，不启用——它会牺牲"红必能说清原因"，且等于整子树豁免（违反硬约束 4）。

**1.2 会话记录（启动声明 + 构造证据 + 训练就绪核验）**

**1.2a（2026-09-15 落地；`rl_exp/tools/runrecord/manifest.py`，套件 `[26]`，训练侧补丁 `fork_patches/train_run_manifest.patch` 已应用）**：四个调用点（T0 在 `gym.make` 前 / 环境构造后核验 / runner 后装 save 钩子 / `learn` 前冻结）＋ ImportError 仅告警，逻辑全在仓内模块（补丁只留调用）。要点：

- **manifest 自身走 `cfg_snapshot` 落盘**：agent cfg 的 `MISSING`（`stochastic`/`init_noise_std`/`obs_groups` 就是未设值）首版直接 `json.dumps` 就抛 `TypeError`；改为一处序列化，MISSING/浮点/路径/地址全按同一套规则。
- **T0 立即落盘**（`gym.make` 中途挂掉也要留启动证据）；**T1 摘要可重算**：`t1_digest(manifest)` 在冻结与校验两侧共用，手改 manifest 无法留下匹配摘要。
- **校验不信记录自称**：`verify` 自己重算 T1 摘要、自己重比构造期"声明 vs 实际"对儿 —— 只改一侧、甚至连摘要一起伪造（把 `t1_sha256` 重算过）都能抓出来。
- **见证物分开**：ckpt `infos` 只带 run id + T1 摘要 + 迭代 + 载荷摘要；文件 sha256 落外部 `checkpoints.json`（写完才哈希，不回写 T1）；T1 未冻结时存档的 ckpt 标 `incomplete` 且不指向摘要。
- 资产按该版本 `asset_lock.json` 逐文件哈希（**allow-list，非依赖闭包**，已在记录里写明）；与锁不符即记失败。
- `--verify` 两维度：记录完整 / 可重建 / 重建演练 × 通过 / 失败 / 未知；"重建演练"在没做过对应演练前恒为**未知**，不冒充。

**1.2b 运行时验收（2026-09-15；证据见 `versions/lizard/ACCEPTANCE.md` §1.2b）**：验收 ①（T1 记录仅在 runner 阶段动态导入的模型类）、②（配方 lr ≠ ckpt lr 时记录恢复后实际生效值）、③（构造/恢复失败时无 T1）、④（引用漂移或冻结后出现未声明来源即失败）**在离线侧已按 stub 覆盖**，真跑已落到实物记录上（另有 resume 一次形成对照，含负对照与"原记录未污染"复核）。

| 时点 | 动作 | 内容 |
|---|---|---|
| **T0 `gym.make` 前** | 启动清单（`stage=pre_make`，立即落盘） | 引用清单（recipe/asset/terrain/obs_protocol 的 id + 摘要）+ 代码来源证据 + 资产闭包哈希 + seed/num_envs/sim dt/控制频率 |
| 环境构造后（T0 与 T1 之间） | 阶段快照（`stage=env_constructed`） | env cfg + 各 term `func` 类型与 qualname + 实际 num_envs；核验配方、资产、协议的实际读取内容；尚不宣称最终训练条件已确定 |
| **T1 runner 构造、恢复与核验完成，`learn` 前** | **最终 manifest 并冻结**（`stage=ready_to_learn`） | env/agent 配置 + 实际解析的算法、actor、critic 类型与代码来源 + 实际 obs 分组/归一化配置 + 恢复后生效的学习率等学习设置；绑定 resume 源文件哈希、加载选项及已恢复/显式丢弃的状态范围 |
| T2 保存 checkpoint | `infos` 只写 | run id + **T1 manifest SHA-256** + 迭代号 + 载荷摘要（非文件哈希） |
| T3 保存完成 | 外部 ckpt 索引（另存） | 文件 SHA-256 落 `checkpoints.json`，不回写 T1 |

**代码来源按实际加载记录**（`importlib` 解析模块所属仓根，新增依赖自动纳入）：Robot（本仓）与 IsaacLab fork 各记 rev + `status --porcelain=v2` + `diff HEAD`；RSL-RL **editable** 记源码树 rev + staged/unstaged + 未跟踪源码归档，**普通安装**记包版本 + 可取回安装产物。**未跟踪文件仅哈希不足重建 ⇒ 归档内容。**

**冻结规则**：T0 记录启动时已加载来源，T1 补齐 wrapper、runner 与恢复阶段实际加载的来源；不能把 T0 的模块枚举当完整代码清单。训练所需的动态组件须在冻结前解析并核验。若训练中才出现未声明的代码来源，另记失败证据并阻塞该 run 的完整性验收，不回写 T1 或继续宣称来源完整。T1 前的构造、恢复或核验失败保留 T0/阶段快照及失败原因，禁止写出 `ready_to_learn` 或绑定一个未完成的 T1 保存正式 checkpoint。

**核验对象分开**：配方/资产/协议等被引用内容与启动声明的摘要不符 ⇒ 失败；CLI 覆盖与 checkpoint 恢复引起的合法生效值变化，记录声明值、实际值及来源，不把不同阶段配置的摘要直接要求相等。resume 源内容哈希与加载选项进入冻结清单；学习状态摘要另指向 checkpoint，避免把完整张量载荷塞进配置快照。

**1.2 必需验收**：① 使用仅在 runner 构造时动态导入的模型，确认 T1 包含其实际来源与可取回内容；② 配方学习率与 checkpoint 学习率不同，确认记录的是恢复后算法/optimizer 实际生效值，并标明来源；③ 构造或恢复失败时无最终 T1；④ 引用文件漂移或冻结后出现未声明代码来源时，完整性检查必须失败。此处验收只证明记录与生效条件一致，不证明学习状态的后续演算等价。

其余：manifest 不做参数源（参数只从配方读）；入库只放小型索引，补丁与资产进归档（不能只留哈希）；`--verify` 输出两维度 + 缺失清单 + 已验证重建的范围。

**1.3 状态恢复**（组件表 + 三条边界）

| 线 | 组件 | 现状 | 方案 |
|---|---|---|---|
| v11/v12 | `JointSIRTerrainCurriculum` 粒子/权重/episodes/in_band/tr/回放池/env_pair/desired_vel/env_type | 已有 collect/apply/校验 | 纳入注册表 |
| **v5–v10** | **`SpawnWeightSIRTerrainCurriculum`** | 未覆盖 → 时钟恢复、SIR 冷启动 ≠ 连续 | 新增独立 collect/apply + 指纹 |
| 全部带 c_k | `common_step_counter` | 仅 joint SIR 路径回填 | 解耦：有 c_k 就恢复 |
| 其它课程 term | 任何 `ManagerTermBase` | 仅 WARN | 注册表未登记 **且任务声明要求恢复 ⇒ 硬失败** |

1. **补丁 ImportError** 现 WARN 后继续 → 对声明要求课程恢复的任务改硬失败（声明 `REQUIRES_CURRICULUM_STATE`（`ClassVar[bool]`）放任务 cfg 侧，读 `type(env_cfg)` 不依赖 rl_exp 导入；**已落地**），其它任务透传。保存 hook 安装失败同样只在声明任务上终止。
2. **`--weights_only` 正名**：实测只丢课程状态，rsl_rl **仍加载 model + optimizer**（`_peek_lr` 读 optimizer 学习率即证）→ 新增 `--drop_curriculum_state`，旧名保留为别名 + 弃用提示（不改公开行为）；`PLAN.md` #11 措辞同步。**已落地**：CLI 与 Python 入口（`apply_resume_state`/`freeze`）双名并存，显式给出互相矛盾的取值时明确报错。
3. **多 GPU 不在保证范围**：状态只从 rank 0 写；manifest 记 `distributed`/rank；README 与本文各写一句。**已落地**：非 0 rank 拒绝恢复、不装保存 hook，T1 记 `runner_is_distributed`/`runner_gpu_global_rank` 与 `multi_gpu_resume_verified: false`。

**1.3a 载荷与接口验收（2026-09-15，离线；证据见 `versions/lizard/ACCEPTANCE.md` §1.3a）**

采用顶层 v2 格式：公共 task/num_envs/written_at_iter，`clock` 保存 counter 与 c_k 静态参数，`terms` 按实际 term 实例名索引；每个 slot 标明 adapter、term 类型及 static/runtime。一个顶层版本即可，不要求通用插件或独立 adapter 版本体系。发现按实际类型匹配；实例名仍用于载荷身份核验，改名必须显式迁移。

| 编号 | 输入/操作 | 必须满足的结果 |
|---|---|---|
| S01 完整往返 | joint SIR、行 SIR、c_k-only 各一份非初始状态；序列化到文件再加载 | 首次 reset 前所有已声明字段与 counter 精确一致；张量 shape/dtype 保持，比较前仅允许设备搬移；collect 得到独立副本，原对象后续更新不改变已采集状态 |
| S02 行 SIR 指纹 | 分别修改网格、有序类型名、地形配置摘要及 9 项 cfg | 每项单独变化均拒绝；9 项为 command_name/band/eval_every/n_traj_min/p_transition/p_replay/success_ratio/soft_edge/steps_per_iteration；同占比类型互换但 type_cols 不变也必须检出 |
| S03 行 SIR 状态 | 保存 particles/weights/episodes/successes/history/env_type/next_eval_step | 全部恢复；不虚构 joint 专有字段。env_type 参与实际恢复及首次重生检查，不能无证据省略 |
| S04 c_k 指纹 | counter 不变，分别改变 c0/decay/steps_per_iteration，或改变 c_k 存在性 | 默认拒绝；`ck=None` 表示明确无 c_k，缺失指纹表示证据不足；c_k-only 的空 terms 合法 |
| S05 身份与损坏 | 错 task/env 数、缺/多 slot、未知版本、错误类型、NaN/Inf、负权重、越界粒子/历史/env_type、错误 shape/dtype、非法计数/时钟 | 明确报错；权重非负且每组和与 1 的误差不超过 1e-6；计数非负，行 SIR successes 不大于 episodes；调度时点按真实调用语义校验，不凭日志值猜测 |
| S06 原子回填 | 多 slot 中最后一项损坏；同类型多实例 | 所有校验在修改对象前完成，失败后全部对象及 counter 不变；多实例要么分别恢复，要么明确拒绝，禁止键覆盖或任选其一 |
| S07 硬失败边界 | 要求恢复但无载荷/缺 slot/未覆盖 term；已知类尚未实例化；调用侧导入失败（含模块内部 ImportError）；保存 hook 安装失败 | 未显式 drop 时，在首次 reset/训练前终止；新训练要求记录课程状态时，保存 hook 失败也终止。非声明任务的未覆盖项只透传并明确报告，不伪称已覆盖 |
| S08 兼容与 drop | v1 原始样本；CLI 与 Python 旧 weights_only 入口；新 drop 入口 | v1 可解析为唯一 joint slot，缺失指纹列为未知，不以当前值补造；缺少必需证据时不通过完整恢复验收。显式 drop 保持课程冷启动并继续原模型/optimizer 加载行为；旧入口有弃用提示；新旧参数冲突明确报错 |
| S09 声明与分布式 | `ClassVar[bool]` 声明（V5 声明 / v6–v14 继承 / 8 个 PLAY 显式 False / V4 无；快照格式 2 排除 ClassVar）；模拟 rank 0/非 0 | 全部注册任务 golden 不变，声明不进入配置数据（快照/golden/run manifest 的 cfg 摘要）；`to_dict()` 仍带该键（上游按实例命名空间遍历），属记录面不作比较。PLAY 行为符合显式声明。非 0 rank 不安装课程保存 hook；T1 记录真实分布式标志/rank，不宣称多卡恢复验证通过 |
| S10 实际恢复证据 | 成功恢复、显式 drop、未覆盖、失败分别运行 | manifest 读取 apply 的实际结果，列出源版本、实际挂载/已恢复/丢弃/缺证据项及 c_k 状态；注册表支持列表或 counter 非零都不能充当恢复成功证据 |

**1.4 恢复验收三层**

| 层 | 验收 | 证明 |
|---|---|---|
| A 对象级 | 保存前 `collect()` 与恢复后、首次 reset 前 `collect()` 逐位一致 | 载荷无损 |
| B 演算级 | 给两者相同 episode 统计 + 受控随机数，下一次课程更新输出一致 | 状态→更新映射一致 |
| C 集成级 | 恢复后跑 N 步：时钟推进、课程分布合理、有限值、训练正常；**冷启动作负对照** | 接线时序正确，非"复现" |

不要求冷热前 N 步逐位一致（RNG、出生、per-episode 瞬态刻意不存）。负测试保留：有 term 无状态 → 硬失败 + 显式放行参数。

**1.4a A/B 层执行口径（2026-09-15，离线；证据见 `versions/lizard/ACCEPTANCE.md` §1.4a）**

- A 层执行 S01–S10；joint SIR 保留旧字段的回归覆盖（槽字段集缺失即失败）。通过只声明载荷与回填正确。
- B 层对两种 SIR 使用真实生产更新函数，在相同设备/dtype 下给原对象和恢复对象相同统计输入，每次调用前恢复相同测试 RNG 状态。分别覆盖流量不足、正常权重更新、全带外回退、随机游走与 replay；particles/weights/history/统计清零及下一调度时点精确一致。fixture 必须实际触发目标分支；只运行 collect/apply 不算 B 层。**已落地**：五例分支夹具各先断言分支签名（含 `_fallback_weights`/`_walk` 探针），再过生产入口 `term(env, ids)`（含节流与重生）；`[15]` 26/26，7 例闸门反证全 FIRED。
- c_k-only 在迭代边界前、边界及边界后比较 counter 和按保存参数计算的 c_k（float64，绝对误差不超过 1e-12）。**已落地**：值取自载荷保存参数独立重算，边界处确实变化。测试可控制 RNG，但不新增生产 RNG 恢复保证，也不改冻结 term 的演算语义。

**1.4b C 层真实恢复（2026-09-15 真跑，证据见 `versions/lizard/ACCEPTANCE.md` §1.4b）**

覆盖 c_k-only（v3）、行 SIR（v14）和 joint SIR（v12）三个代表任务；gym id 已按注册表核验并落记录。其它任务仍必须通过全部离线/golden 检查，重型代表任务不能替代它们。单卡、每任务 64 env、seed=42；源 run 与恢复 run 使用相同框架、资产、配置、env 数和设备（实际组合：RTX 3060 Ti 8 GB、IsaacLab `28a37cecdd43` + 两补丁、Python 3.12.13）。

**观察方式（执行时定型）**：观察点必须落在**真实 trainer 进程**里——另写一套"逐行对齐 train.py"的流程会与 trainer 分叉，只能当辅助。实际做法：`tools/verify/cstate_observer.py` 经 `sitecustomize`（`PYTHONPATH` 注入）在真进程内**只包装读取** `apply_resume_state` / `OnPolicyRunner.load` / `learn`（单次调用跑满，**不循环 `learn(1)`**：rsl_rl 以 `start_it + n` 计数且每次收尾保存并关 writer）/ `env.step` / `alg.update` / 两个 SIR 的更新函数；`tools/verify/check_c_layer.py` 判据（含 `--resave`）。臂共 15 个：3 源 + 6 短路径（`--max_iterations 1`，给 P0/P1/P2 与 load 后模型/optimizer 对比）+ 6 主证据（`--max_iterations 84`）.

1. **准备真实源 checkpoint**：训练至少 2 个课程评估块并确认发生更新；c_k-only 至少 2 个 PPO iteration。保存时 counter>0，至少一个声明状态字段不同于冷初始化（如 history 长度或统计），禁止手工造值伪装训练产物。条件未达到则本次 fixture 不合格，记录未知。**已落地**：源落 20 it（v14/v12 各 4/3 次真实更新、v3 counter=480），ckpt 从**实际索引**读（`model_<最后完成迭代>`，20 it 的末件是 `model_19.pt`），并按"至少一个持久字段 ≠ 冷初始化"用**同任务 drop 臂的 P1 冷样本**对照（v14 差 3 项 / v12 差 11 项）。
2. **新进程恢复**：结束源进程后分别启动正常 resume 和显式 drop 两个进程，使用同一 checkpoint。在 apply 前、apply 后且首次 reset 前、首次 reset 后、首次课程更新后采样证据。**已落地**：P0/P1 由 `apply_resume_state` 包装采样，P2 在首次 `env.step` 前采（首次 full reset 由 `RslRlVecEnvWrapper.__init__` 完成），P3 在每个真实更新后采；c_k-only 线无 term 故无 P3。
3. **运行长度**：SIR 令 B=eval_every×steps_per_iteration，H=ceil(episode_length_s/(sim.dt×decimation))；恢复后至少运行 `max(2B, 2H)` 次 env.step，向上取整到完整 PPO iteration，并观测至少 2 次真实课程更新。c_k-only 至少 `max(2×steps_per_iteration, 2H)` 次 env.step。未观测到要求的事件不能仅按步数判通过。**已落地**：B=240、H=1000 ⇒ 84 it = 2016 步（≥2000），单次 `learn()`；主证据每臂观测到 ≥2 次更新（v14 8、v12 9、drop 各 8）。

| 检查 | 通过阈值/事件 |
|---|---|
| 回填与首次 reset | reset 前满足 A 层；初始 reset 不把未运行 episode 算进统计，不清空持久粒子/history 或归零 counter。若已到评估时点，只允许生产规则规定的更新，并记录更新前后状态。**执行细化（两案分开判）**：持久字段（particles/weights/episodes/successes/history/调度）必须逐位保持；出生相关字段（env_type/env_pair/desired_vel/出生列）允许按生产规则重采样，只查合法——不无条件要求 P1/P2 位级相等 |
| 时钟 | 每次 env.step 后 counter 增量严格为 1；c_k 与已核验调度参数的参考计算一致（绝对误差≤1e-12）；不能乘 env 数或 physics substep 数。**执行细化**：增量在每次 `env.step` 前后直接测（不按 iteration 总量反推），终态再交叉校验 `resume: final−source == 84×24`、`drop: final−0 == 84×24` |
| 课程合法性 | 每次更新后粒子/历史索引、env_type 合法；权重有限非负且各组和误差≤1e-6；统计有限非负；出生列属于相应 type/combo、行来自对应粒子集合；joint 的命令与所选 velocity bucket 及配置 jitter/clamp 规则一致。**执行细化**：索引/权重和/env_type/env_pair/desired_vel 有限性在每次真实更新后按生产口径核一遍 |
| 更新次数与时点 | 记录每次实际更新及 next_eval_step，符合 eval_every×steps_per_iteration 的生产节流语义；一次触发不能重复重采样；每个已声明 SIR 至少触发 2 次。**执行细化**：更新由 `_reset_idx` 调用，落在块沿之后**第一个带 reset 的 step**（实测 247/250），故判据是 `clock ≥ 边沿` 且 `next_eval` 精确 +B，不要求恰好在 240 的整数倍步发生 |
| 训练推进 | 全部采集到的 obs/action/reward/loss、模型参数与 optimizer 浮点状态均有限；至少 2 次实际 optimizer 更新，参数至少一项改变；再次保存的 checkpoint 可加载，T1 绑定及外部文件摘要核验通过。**执行细化**：记实际 `alg.update` 次数与 Adam `step` 增量（每次更新每参数 +1）、参数摘要每次均变化；最终 ckpt 的 `iter` == 最后完成迭代、载荷 counter == 观测终值 |
| 冷启动负对照 | drop 分支首次 reset 前保持新对象的课程初值，正常 resume 对应源载荷；预先选定的非初始字段与 counter 确实不同；两个分支都正常推进。不要求短期 reward、终止率或粒子直方图必然不同。**执行细化**：drop 的模型/optimizer 加载**不用 reward 近似判断**（该推断不成立），改为 `runner.load()` 后、首次 `update()` 前与同一 ckpt 逐项比 actor/critic 参数与 optimizer `param_groups`/`state`（含 step/动量） |

**执行中发现并修复的产品缺陷**：旧 `_check_eval_clock` 要求 `next_eval_step > counter` 严格成立，于是"已过块沿但 term 尚未被调用"的**挂起**状态被判为不一致 ⇒ v12 源 ckpt（counter=960、`next_eval_step=960`）硬拒恢复，而 v14 同迭代数因更新恰在 960 触发而侥幸通过——同配方可恢复与否取决于 env 是否恰在边界步 reset。新规则 `next_eval_step % block == 0 and next_eval_step > counter − block` 接受挂起/已到期，仍拒绝落后整块及以上的调度；新增离线用例 + 反证（改回旧规则必失败）。

“课程分布合理”在此限定为结构、概率、索引和更新规则合法，**不以短跑奖励提升或训练统计等价为硬门槛**。不要求冷热轨迹逐位一致，也不据此宣称长期统计连续。C 层证据保存源/恢复/drop 三份 run id、checkpoint 摘要、关键时点状态、事件计数与有限值检查结果。

**1.2b 联合检查**：真实 runner 动态加载的类须在 T1 有实际来源；单独一次 lr 对照在构造后、首个优化步骤前核对配方值与 checkpoint 恢复值，记录有意配置差异，不混入上述同配置 C 对照。另在可丢弃的运行副本分别注入构造失败、恢复失败、引用漂移和冻结后新增未声明来源，确认前两者无有效最终 T1、后两者核验失败；不得修改原实验输入。无归档的可取回项保持未知。

**阶段出口**：S01–S10 与 A/B 通过可标“1.3 离线状态恢复验收通过”；三个代表任务 C 层及 1.2b 实物记录通过后才标对应运行时验收通过。**1.5 恢复演练是按需的独立验收，不进本阶段的交付出口**，也不阻塞日常训练与 Step 2 迁移。

**1.5 恢复演练（条件触发的独立验收，不进主交付出口；2026-09-15 用户拍板重定位）**

日常运行**不复制源码、不重建 venv、不建隔离目录**：机器路径配置（`paths.yaml` / `RL_ISAAC_ROOT` / `RL_PYTHON`）管"在哪里跑"，实验配方管"跑什么"，run manifest 管"这次实际用了什么"。**路径能找到环境 ≠ 环境内容没变**，所以实际版本与必要摘要仍按 1.1/1.2 记录。

演练是**按需**的独立任务，随时可做，不阻塞日常训练与 Step 2 的配置架构迁移。触发条件（任一）：某实验值得长期保留；要对外声明某次实验可重建；归档/迁移前要留一份可验证的恢复证据。

**演练四步**：① 选定实验（一个具体 run）；② 准备它需要的代码、配置、资产与 checkpoint（材料按需取材，见 1.5a）；③ 用**另一份机器路径配置**指向重建位置与**声明的兼容环境**；④ 验证配置构造、模型加载与来源，记录覆盖范围。

**覆盖范围按次声明**（决定结论强度，不得混说）：

| 演练范围 | 能得出的结论 |
|---|---|
| 重建项目材料 + 复用已声明的现有环境 | 该项目材料在该环境下可恢复 |
| 同时重装 Python / IsaacLab 环境 | 连环境安装过程也得到验证 |
| 只换路径配置、仍指向全部原材料 | 路径配置有效，**不能算材料恢复演练** |

- **每次演练明确哪些材料重建、哪些依赖复用**：重建项 = 代码 / 配置 / 资产 / ckpt 中这次真正重新取得的那部分；复用项 = 显式声明的运行时依赖（解释器、预装框架、二进制运行时），逐项记版本 / 来源与验证边界。**不统一要求复制大环境**；不能核验的必需依赖记未知，不计通过。
- **来源断言（只断言声明为"重建"的材料）**：在演练进程内记录实际导入模块与资产解析后的真实路径，**只对被声明重建的那部分**做判定——任一项落回原源码 / 资产目录 ⇒ 失败。声明为"复用"的依赖（解释器、预装框架、二进制运行时）**只记录不断言**：`paths.yaml` 只给 `isaac_root` 与 `python`，config 指对了环境就是对的，再断言是自证。注意 config 恰恰不含项目根：项目自身的导入路径来自 venv 里的 `.pth`（现有 `rl_exp.pth` 指向原仓），所以"项目材料是不是从重建位置来的"必须查，这也是覆盖范围表第一行与第三行的分界。只换配置、材料仍在原地者按上表直接判"不构成材料恢复演练"。
- **边界声明**：不做操作系统级访问切断（不建专用账号、不改 ACL），原目录"仍可读"只作信息记录；不覆盖任意隐藏读取（包内部 / 缓存 / 环境变量旁路）；需要更强保证时另找一台机器或沙箱环境验。
- **正向验收**：核验代码与资产内容摘要、完整 env/agent 配置及 T1 生效值、模型加载；配置中的运行目录 / 设备重定位逐项记录映射，不整树忽略。
- **缺件负测试**：在可丢弃副本中移除一个必需材料文件（如引用 mesh 或动态模型源码），演练必须明确失败，不得从原目录、共享缓存或未声明下载回退补齐；恢复该文件后必须通过。
- **出口（仅对本次演练）**：四步齐、来源断言与缺件负测试通过、覆盖范围已声明，方可标"**本次演练范围内：配置与加载级重建通过**"（附重建 / 复用清单）；不承诺推理或训练结果等价。

**1.5a 演练工具（2026-09-15，离线闸门 `[27]`）**：`rl_exp/tools/runrecord/rebuild.py` 是**演练工具，不是所有 run 的强制入口**（日常训练不依赖它）：`--capture` 按 run 的 T1 声明取材并逐文件摘要（干净代码按 rev 可取回；脏树 diff 与代码根内未跟踪文件必须有 `--archive` 落点，否则**拒采且不写材料**——`PLAN.md` #18 变成闸门）；`--check <dest> --root <重建位置> --dep <复用依赖> --original <原目录>` 查材料摘要、模块与资产来源、是否落回原目录、ckpt `infos` 是否回指本记录 T1，并判"只换配置"情形；`--maintest` 删一个必需载荷必须失败、还原必须通过。全离线（不 `gym.make`），只到"配置与加载级"。实测负证据：R1 真 run 被拒采（记录 `cc25f24b` ≠ 现树 `13aee682`，脏树 diff 内容从未落盘、未跟踪 `spider/` 无落点）。

### Step 2 —— 显式配方与实验线生命周期

- **前置**：按 1.0 实测定整改形态。
- **单写者**：结构级 5 类（`terrain_generator`/`height_scanner`/`commands`/obs 组/`terminations`）各一个写入点；逐版本重复 `_load_params` 收敛为一次；构建路径固定：读取校验 → 构建组件 → 组合 → 兼容检查 → 落快照。
- **起点**：保留 lizard 家族，新架构承接新的实验线；旧 v1–v14 配方及公开入口保持冻结。架构迁移本身不要求新建一个相同家族；`v15` 只是原方案的示例名，新配方可使用语义名称。实际实验线名、首个新配方名与旧线退休时点在实施时显式登记，本轮不执行退休。

**2.1 统一配方目录与命名**

- 显式映射 `任务 ID → 配方 ID + 修订 → 配置入口`，同时关联 family 与实验线。语义名称表达实验意图，修订和内容摘要确定唯一内容；发布内容不可覆盖。旧任务仍映射到原配置类，禁止静默重定向到新配方。
- 生命周期只设 `active` / `retired`，放在独立目录索引的实验线条目，配方引用该线；不写入冻结 YAML 或 env/agent cfg，不制造第二份可手改的训练参数源。目录校验配方身份唯一、引用存在和任务映射无歧义。
- 退休条目记录原因、时间及后继实验线（若存在）。退役状态可随目录修订变化，但已发布配方内容不变，旧 run 的 manifest 不回写。
- Golden 的版本身份检查改为消费显式映射；历史 `vN` 的对应关系仍须保持。新配方不再依赖正则从名字推断版本；legacy 的 `params_version` 字段保持兼容，不借命名迁移删除字段。

**2.1a 布局改造 A0 与生命周期权威（2026-09-16 用户拍板）**

布局（A0，证据见 `ACCEPTANCE.md` §2.4）：

- 主线不再"家族根即主线"：搬进 `versions/lizard/main/`；新支线 `baseline`（无上游、思路与主线不连续、**不叫 v1**）；家族共用资产（`lizard.urdf`/`meshes/`/`assets/`）与家族级文档（`FAMILY.md`/`PLAN.md`/`OBS.md`/`REWARDS.md`/`ACCEPTANCE.md`/`verify_logs/`）留家族根
- 参数文件按 `recipe_lines` 闸门规则 `<line>_params.yaml` ⇒ dev 1 个 + 冻结 16 个 `lizard_params.yaml` 全部改名 `main_params.yaml`。`asset_lock.json` 的 yaml 路径键本来就要随目录重写，改名是其上的增量
- `base.json` **不改**：规则是"线内裸 `vN` 先命中本线"，`main/v14` 写 `v13` 仍解析到 `main/v13`
- 线键随家族相对路径变化：`lizard` → `lizard/main`；新增 `lizard/baseline`
- **`is_main_line` 必须改为 `name == "main"`**：现判据 `"/" not in key`（`recipe_lines.py:88`）在搬完后会把 `lizard/main` 与全部支线一起判成主线，机器的关节序/body 名契约会拿反
- A0 触碰冻结物（16 个 yaml 改名 + 16 个 `asset_lock.json` 键）⇒ 按 `versioning.mdc` A 记一次"记录修改"；历史 run manifest 里的路径字符串**不补造**（硬约束 6），按 rev 取回仍成立
- `v15` 留在 `main` 线内递增（`base = v14`），不进 `baseline`

生命周期权威只有一层：`lines.json` 里的 `status`，取值二值（`active`/`retired`）。退休是**一次目录修订**，不是时间到点，所以这一层不读时钟：

| 层 | 输入 | 行为 |
|---|---|---|
| 运行期 | 启动时读到的 `status` | 只按 2.2 表放行/拒绝；不重读目录、不看时钟、不推导第三状态 |
| 离线闸门 `[29]` | 目录（格式 / 二值状态 / 退休证据 / `successor`） | 结构或证据不一致即红 |

**2026-09-17 收缩（原三层设计删除）**：原设计是 `deprecation` + `retire_not_before`（允许退休的最早条件）+ 离线硬红 / 运行期 WARN 分层。实测依据：三条线的 `deprecation` 全为 `null`、9 类操作只有 2 类真的到达 `judge()`、唯一 retired 线（`lizard/parkour`）构不出 env 因此续训豁免路径不可达 ⇒ 删掉**全部**未接线部分：时间条件（`date` / `registry_revision`）、WARN 层、`--now`、`--allow_retired_resume`（含 fork 参数）、"到期仍 active ⇒ 红"这条规则，以及 `lifecycle_entry_fixture.py`。附带收益：闸门不再读时钟，"同一个提交随日期由绿转红"的机制随之消失。**保留**：二值 `status`、缺失≠active、退休证据（`retired_at` + `reason`）、`successor` 结构校验、内容摘要。**同轮删掉 `revision`**（同一次收缩的第二处）：它当时唯一的读者是已删的 `registry_revision` 条件，剩下只是"给人看的计数器 + 与摘要冗余的跨入口对照"，而计数器会撒谎（改目录忘 bump、bump 了内容没动，都没有东西变红）—— 目录身份由 `lines.json`/`recipes.json` 的内容摘要给出。`format` 不动：它是真闸门（本闸门只读一种格式）。

配方目录**两半**：

- 生命周期半边：`rl_exp/versions/lines.json`（键 = `recipe_lines.discover()` 的线句柄）+ 套件 `[29]`/`[30]`
- 身份半边：`rl_exp/versions/recipes.json`（`recipes["<id>@<修订>"]` + `tasks[任务 id]`，**全部注册任务必须覆盖**，配置入口与 `gym.register` 逐字一致）+ 套件 `[31]`/`[32]`。**天花板**：`[31]` 用 `ast` 解析注册模块而非 import registry ⇒ 不被树里的半成品拖红；`[24]` 走真注册表，两者不一致本身可查
- 附带闸门 `[33] check_suite_banners.py`（**它拦下的真实故障**）：套件 `echo` 横幅里未转义的 `<`/`>` 在 bat 里是**重定向** —— 本批的 `->` 真往仓库根写过一个 `config` 垃圾文件并吞掉自己的横幅，`[24]` 行的 `\<line>\` 每跑一次报输入重定向错，而**套件不会因此变红**（echo 的 errorlevel 无人检查）⇒ 只写约定必然复现，故加扫描闸门（自带探测器自检 11 形状 + 空扫描防护）
- `legacy_task_version` 绑定规则：teacher 12 个任务（v1–v14，无 v7/v9）= `vN`、parkour 2 个 = `v1`、v0 家族 8 个 = `null`（其 `params_version` 即 `None`，id 后缀 `v0` 不是配方声明 ⇒ 闸门只做单向校验：**声明了版本就必须是任务 id 的 dash 分词**，`v1` 不被 `Lizard-Rough-v14` 满足）
- A3 配置侧绑定：`[31] --bind-config` 把每个声明的 `env_cfg_entry` 逐个导入并**构造**实例，比对 `params_version` 与 `legacy_task_version`（构造失败记红、不记跳过；类属性读不到故必须构造，1.0 已证）。绑定只依赖声明的入口点、**不读锁** ⇒ 不必等锁冻结

**2.2 生命周期接入运行流程**

统一流程：解析配方目录 → 按操作类型检查生命周期 → 构建配置 → golden/兼容核验 → 恢复状态 → 冻结 manifest。生命周期检查在仿真构造前完成；退休只停新增工作，协议、资产与课程状态校验不因它放松或收紧。

| 操作 | active | retired |
|---|---|---|
| 普通新训（无 `--resume`） | 允许 | 拒绝：`refusing new_train` |
| 恢复既有训练（`--resume`） | 允许 | 拒绝：`refusing resume`（历史续训豁免已于 2026-09-17 删除） |

- 只判断**真正接线**的操作。`tune`（`ablation_harness`）经 `subprocess` 落到 `train.py`，与普通新训同一判定，不再单列；`eval` / `export` / `rebuild` / `load_ckpt` 不经过这张表，入口也不因退休被拒（结构上未接线 ⇒ 不写进表里冒充已覆盖）。
- 拒绝文案不承诺后继线：索引允许没有 `successor`（`lizard/parkour` 就没有），文案只陈述"该线已对新增工作关闭"；`successor` 只做结构校验（存在、非自身、本身 active）。
- 只增加一份目录与一个共用检查函数，不新建生命周期框架。新启动器与仍可直接使用的旧训练入口均接入同一检查；调参入口不能绕过。库级任务注册与构造能力保留，不以取消 gym 注册来实现退休。
- 历史续训豁免（原 `--allow_retired_resume`）已删：它豁免的是"健康但已退休的线"，而目前唯一退休线构不出 env，该路径不可达；真有健康线退休时再加，并同时补回"源 checkpoint 已解析"的前置。`--drop_curriculum_state`（含弃用别名 `--weights_only`）"无 resume 即拒"的校验保留，但已迁到启动参数检查，不再依赖生命周期判定。
- T0/T1 绑定启动时实际读取的目录修订/内容摘要、配方与实验线身份、生命周期与操作类型。当前目录改变不改写历史记录，也不改变正在运行任务的冻结条件。
- 已发布入口收紧仍是用户可见行为变化，但提示与生效不用机器判定的条件表达：生效 = 目录显式转 `retired`（同一次修订里写 `retired_at` + `reason`），**没有"到点自动"**；旧线不会被自动退休，也不存在运行期硬截止日。
- `manifest --verify` 把"在 T0 被拒"当成**终态**而不是半途死掉的 run：记录完整（`allowed=False` + 原因 + `failures`），训练后才有的断言（阶段、产物、重建）记不适用/未知且不阻塞。反向仍红：拒绝与训练阶段并存、或拒绝无 `failures` 佐证。

**2.3 生命周期验收**

| 编号 | 检查 | 通过标准 |
|---|---|---|
| L01 显式身份 | 数字历史任务、语义新配方、重复/悬空映射 | 两种合法身份正确解析；重复、缺项、悬空引用明确拒绝；内容变更必须产生新修订，不按名称猜配置 |
| L02 入口执行 | 真实退休线（`lizard/parkour`）：启动器档（无需 sim）+ trainer 档；合成树覆盖缺失/未知/身份不一致 | 新训与 resume 均在 gym.make 前拒绝；launcher 退出码 2 且未 spawn trainer（无新 run 目录），trainer 退出码 2 且留下 T0 拒绝记录；拒绝文案不承诺后继线；缺失生命周期不能默认视为 active |
| L03 历史续训 | 退休线上的 `--resume`；`--drop_curriculum_state` 与弃用别名 `--weights_only` 无 resume 使用 | 退休线一律拒绝续训（豁免已删）；课程 drop 无 resume 即拒，两种拼写一致，且这条校验不依赖生命周期判定 |
| L04 历史兼容 | 退休前后全部历史任务/PLAY 与实际旧 checkpoint | 注册、类路径和 golden 保持；评估/导出/重建可进入原检查流程；无 checkpoint 实物的加载项记未知，不以构造成功代替 |
| L05 记录冻结 | 启动后修改目录生命周期；被拒启动的记录 | 原 run 的 T1 与摘要不变；新 run 读取新目录；记录能追溯启动时状态与操作；被拒记录在 `--verify` 下记"启动被拒"且不阻塞，拒绝与训练阶段并存仍报红 |
| L06 ~~迁移生效~~ **已删（2026-09-17）** | — | 弃用声明机件随 2.1a 收缩删除：退休只在目录显式修订时生效，无时钟语义、无 WARN 层、无"到点自动" |

**2.4 施工阶段 A/B/C 与依赖（2026-09-16 审核后定稿）**

> **命名提醒**：本节的"阶段 A/B/C"与下面验收表的"**硬 A/硬 B/硬 C**"**不是一回事** —— 后者是验收项，前者是施工序。

### 阶段 A —— 显式配方目录与生命周期

| 件 | 内容 |
|---|---|
| A0 | 布局迁移：主线 → `versions/lizard/main/`；17 个参数文件按闸门规则改名 `main_params.yaml`；`base.json` 不动；`is_main_line` 判据改 `name == "main"`；17 个 `asset_lock.json` 键随路径重生成。脚本 `_a0_layout_migration.py` 默认 dry-run（`--apply` 才动），证据落 `ACCEPTANCE.md` §2.4 |
| A1 | 目录数据：`lines.json`（生命周期）+ `recipes.json`（身份映射） |
| A2 | 生命周期判定：离线闸门 `[29]` + **共用判定契约** `rl_exp/tools/verify/recipe_lifecycle.py`（`judge(operation, status)`：2 类接线操作 × 二值 status、未知操作/未知 status/缺索引一律拒绝、拒绝文案不承诺后继线）；契约自带 case 表随文件走（7 判定 + 2 文案检查）；套件入口 `[36]`。2026-09-17 收缩：原"9 类操作 × 二值 status + `flag_effects()` 两开关互不推导"整段删除，`--drop_curriculum_state` 的无 resume 校验迁入 `runrecord/lifecycle.curriculum_flag_problems()` |
| A3 | 身份检查消费显式映射：结构半 + 配置侧绑定；**删 `check_cfg_lock.py` 的冗余 `_TASK_VERSION`** |
| A4 | 闸门与反证 `[29]`–`[33]` |

**A0 前置（2026-09-16 修正，取代早先的"整棵树干净"）**：① **A0 写入集与脏集相交即拒**；② 被搬目录内存在未跟踪文件即拒（`git mv` 报 "source directory is empty" 并整批不动）。A0 写入集 = 被搬路径 ∪ `{recipe_lines, check_dr_parity, check_version_docs, manifest, check_cfg_lock}.py` ∪ `{recipes.json, lines.json}` ∪ 相关文档。**不再等 `baseline` 线**（该线不落地）。

**A0 的允许变化字段清单（必须先声明、后审查）**：A0 会改 16 个冻结物的 `asset_lock.json` 键与 17 个文件名。执行前先落清单：**允许变化**（仅 yaml 路径键与文件名）与**不允许变化**（资产 sha256、其余键、键序）；`--update-locks` 的逐字段 diff 对照清单审，**清单外任何变化即失败**。理由：否则"重生成后全绿"可能把意外变化一并接受。

**A 出口分两段，不得合并宣告**：

- **A-配置与身份段**（判据）：A1/A2(离线)/A3(结构+绑定)/A4 各自判据全过；L01 全过；L05/L06 只算**离线半**，入口半**未验证前不得计入**；实跑条目 `[29][30][31][32][33]` + `framework_pin_check` + `recipe_lifecycle.py`（`[36]`）必须全绿。**未跑全量套件端到端不得写"套件全绿"**。
- **A0 段**（与上段分开宣告）：重跑 `[2][12][24][28][29][31][33]` 全绿；`asset_lock` 差异对照已声明清单；`check_cfg_lock.py` 的冗余正则必须删（纳入 A 出口 —— 保留两套身份判断必然出现"显式映射允许、旧正则拒绝"）。
- 运行期验收（L02–L04、L05/L06 入口侧）留至 C。

### 阶段 B —— 新构建器与首个新配方

| 件 | 必要前置 |
|---|---|
| B1 组件库：五类结构各一个写入点 + 逐版本重复的 `_load_params` 收敛为一次 | 可独立开发；**但其验收基线的采集必须先于相关实现改动** |
| B2 首个新架构配方（登记 `recipes.json`，新键 + 修订 1） | B1 + A1；验收需 A3 |
| B3 **硬 A**：新构建器表达既有配方（v14）与**整改前 golden** 逐字段一致 | B1 + **已冻结的 v14 golden**；**不必等 B2** |
| B4 **硬 B**：首个新配方相对基准只允许一份显式差异清单 | B2 + 已冻结基准与差异清单 |

**golden 独立性（最大的隐藏风险）**：硬 A 的 golden 必须来自**可追溯的整改前基线**，并在 B1 动实现之前固定。预期值与实际值都经新构建器产生时，逐字段相等**证明不了兼容性**。三条约束：

1. **比较对象与规范化规则事先写清**：缺失字段 vs 默认值、顺序语义、浮点比较口径（`repr` 还是容差）、路径类允许变化逐条列明。
2. **差异清单绑定基准身份与修订**，双向校验：既拒绝**未声明**的变化，也拒绝**声明了却没生效**的变化（清单与实际差异必须恰好相等）。
3. **B 期间冻结所有验收预期值及其生成来源**：只禁 `cfg_lock --update` 不够 —— 手改锁、替换 golden、调比较器同样能移动验收标准，三者一并禁并记录在案。

**B1 的写入点按配置所有权约束**（不只集中赋值位置）：每个组件负责哪些字段、配方允许覆盖哪些字段、冲突如何报错。只有"一个写入点"仍可能被后续覆盖破坏。

### 阶段 C —— 接线与真跑

| 件 | 必要前置 |
|---|---|
| C1 旧入口接线（`gym.make` 前调同一检查；`--drop_curriculum_state` 无 resume 即拒） | A2 **共用判定契约冻结** |
| C2 新启动器（走目录 → 构建配置 → 检查 → 恢复 → 冻结 manifest） | B2 + A2 + A3 |
| C3 T0/T1 绑定（启动时读到的目录修订/配方身份/生命周期/操作类型） | **C1 与 C2 各自实现绑定并交叉验证一致性** —— 只依赖 C1 会让新启动器缺同一条证据链 |
| C4 真跑（L02–L04、硬 C 全量、被拒启动的终态记录） | A 完成 + 硬 A/B 通过 + C1/C2/C3 完成 + 真跑窗口 |

**快照与 manifest 绑定（把"树一动 rev 就断"说准）**：真正的危险是**启动时记录的内容与实际消费的内容不一致**。须明确：配置与目录**何时读取**（启动时一次，此后不重读）；**是否保留快照**（留启动时读到的目录与生效配置副本）；**manifest 绑定哪些内容摘要**（T1 记目录修订 + 配方键与修订 + 生效配置快照摘要 + 代码 rev）。**一个 git revision 覆盖不了未提交改动** —— 未提交态必须显式标记，不能靠 rev 假装干净。

**L05 与冻结窗口的关系**：L05 本身要求"启动后改目录"，所以冻结窗口必须**允许这项受控变更**并记录改动前后证据，否则冻结规则与测试自相冲突。同时写死预期：运行**继续使用启动快照**，还是**检测到变化后拒绝后续操作** —— 二选一，不留给实现者猜。

**验收**

| 项 | 内容 |
|---|---|
| 硬 A | 新构建器先表达一个既有配方（如 v14），与**整改前 golden** 逐字段一致（前提：仍输出兼容配置）；旧入口不动 |
| 硬 B | 首个新架构配方相对基准**只允许一份显式差异清单**（写入该配方的计划记录，位置由目录关联）；清单外变化即失败 |
| 硬 C | **全部注册任务 + PLAY** 的 golden 全过；"代表版本"只减重型仿真成本，不减 golden 覆盖 |
| 软 | 同 seed 200 iter **非硬门槛**；若用须声明误差口径与确定性前提（同 seed/env 数/设备/`--deterministic`/单卡），前提不满足只记录 |

硬 A 字段面白名单：逐条给**字段路径** + 旧值 + 新值 + 转换规则 + **不影响运行语义的验证**（CLI 覆盖仍生效、`params/env.yaml`/日志读取不受损、其余 golden 不变）；禁整字段/整子树豁免。

**上游升级**：旧框架组合基线保留，新组合另建基线（按 `framework=<isaaclab rev>` 分组）；不得把"更新 golden"当常规消警。

### Step 3 施工件表与依赖（2026-09-16 审核后定稿）

> **范围声明**：本节只登记本轮约定的实施件与出口。几何一致性、导出校验与蒸馏本轮**不执行**，按未知/未执行登记；两段出口都不得称为"Step 3 全部通过"。

**步骤 0 —— 动工前冻结三项**（未冻结即动工，后面的门就无处可比）

**0a 身份表（受支持任务/入口 → 协议身份）**。逐条显式写：TRAIN 与 PLAY 各为独立条目、parkour 两条、baseline、v0 家族（多个 v0 配置靠任务 id 区分，**版本号本身区分不了**）。多任务可显式引用同一协议身份；**禁止按版本邻近隐式继承**。

**须区分"有历史目录"与"无对应注册入口"两件事**：v7 与 v9 的目录**都存在**（各有 `main_params.yaml`／`base.json`／`asset_lock.json`），但两者都**没有注册任务** ⇒ 它们是"有历史目录、无注册入口"，不是"无目录"，也**不得由声明给未注册的版本补造出一个入口**。

覆盖条件三条（**不是**"声明集 == 任务集"——多任务可共用协议身份，故等式不成立）：

1. 身份映射的**任务键集** == 受支持任务集；
2. 所有被引用的协议身份都存在；
3. **未被任何任务引用的历史协议**必须显式登记用途，既不得默认判为错误，也不得默认删除。

版本分支禁令（门内不出现 `if version == "vNN"`）只作辅助检查，**不能替代**上述覆盖测试。

**0b 历史目录表**。`versions/**/vN`（含 `main/` 与各支线）逐个登记**可验证状态**：有无协议声明、有无可构造 cfg、有无注册入口、有无 PLAY 变体。该表是**登记**，不是身份来源。三种集合不得混用：**注册任务**、**可构造配置**、**历史版本目录**。

**0c 记录格式（`record_format`，独立于 eval 协议版本）**。协议语义变化（时间线/阈值/地形/DR/指标/采样帧）才开新 eval 协议；纯溯源字段补充**不开 v3**，改为记录格式独立版本号。四条读侧规则成文：

- 无 `record_format` = legacy 记录：缺字段一律读作**未知**，不回写、不补齐、不推导；
- 有 `record_format` 但缺该格式的必需字段 = **记录不完整**：不得按 legacy 待遇放行，也不得算通过；
- 可比／不可比／未知**分别**有测试；缺证据**不得**自动升级为可比；
- 补充记录行为不得改变 rollout、随机数消费或指标计算（负测试见 3.2）。

**0d 验收矩阵**。受支持协议 × 配置变体（任务、mode、PLAY）逐格登记 pass/fail/unknown。**一次 eval 或一个任务不得代表全部协议**；某格的通过不覆盖另一格。



**3.1 协议对象**（声明不参与 cfg 构造 —— 这是与并行批次写点隔离的取舍，代价是声明与代码双写，故闸门必须两侧都断）

| 件 | 内容 | 类型 |
|---|---|---|
| 3.1a | 声明模块 `rl_exp/tasks/obs_protocol.py`：每协议身份一条（版本串、组序、项序、维度、脚本序、clip/scale/噪声、部署标志、内容摘要） | 离线 |
| 3.1a′ | **摘要锚点** `versions/obs_protocol_anchors.json`（与声明同层、全仓一份）：每协议身份一条**已审** sha256 + 已审宽度及其摘要。改锚点须带 reason 且**禁止自动更新**（照 `check_cfg_lock` 纪律）；同版本被静默改写即红 —— 自动重算摘要只会得到新值，不会自行拒绝 | 离线 |
| 3.1b | 静态门 `check_obs_protocol.py`：按 0a 的三条覆盖条件（任务键集、引用存在、未引用历史协议须登记用途）＋ 构造后 cfg 的组名与项序比对；构造失败记红不记跳过 | 离线 |
| 3.1c | 消重复源：`check_obs_layout.py:45-59` 字面量、`test_cfg_snapshot.py:66` 的镜像、`teacher_networks.py:21` 与 `teacher_smoke_runner.py:31` 脚本序、`teacher_smoke_runner.py` 的 90/208/83 维度，全部改为消费声明 | 离线 |
| 3.1d | `manifest.py:174` 的 `obs_layout_digest`（现注释自认 stand-in）改接协议身份＋摘要 | 离线，**实测计入出口**：未接 = 未通过，不许只记"待挂" |
| 3.1e | **真 env 断言**：实际 manager 的组序/term 序、**逐 term 维度**与最终张量维度；实际解析出的 joint 名与 body 名及其索引序（现状只断过 usda vs yaml `joint_order`，真 env 序无门） | 真跑 |
| 3.1f | **标记输入**验网络拆分与重排：独立构造一热/切片标记张量（**预期不由声明生成**），覆盖 `teacher_networks.py:165` 的脚 reshape 与 `student_networks.py:226-257` 的段序；含"故意错序必须红" | 离线＋真跑各半 |
| 3.1g | 静态可读的 clip/scale/噪声改为**机器断言**：`v3.foot_ring.clip`、base 扫描 `(-1,1)`、action `legs_scale/spine_scale`、v12 `height_noise`。单位与坐标系等**不可验证项逐项记未知**，不留在散文级 | 离线 |
| 3.1h | 文档对齐：`OBS.md` 演进表补 v13/v14/v15，注明真源已迁声明 | 离线 |

**只比 shape 会一起过**：网络按错误脚序解释输入时维度完全正确。故 3.1e 与 3.1f 必须同时存在，且 3.1f 的预期值独立构造。

**3.2 eval 记录（四项绑定＋完整评估快照）**。落点 `ablation_harness/`；新文件 `record.py` 承担记录格式（纯 stdlib，不 import torch/sim，不触碰 RNG），`HARNESS.md` 新增 record_format 规则节。

`gym.make` 后的 `mbenv.cfg` **不包含完整评估条件**：命令播放器、robust push、指标阈值与评估步数都在其后另建（`eval.py:459-481`）。只改命令或指标阈值时 env cfg 摘要可以完全不变，故下列各项**各自命名**：

- env cfg 快照 ＋ agent/wrapper 配置（含 `clip_actions`）；
- **实际命令时间线**（交给 `CommandPlayer` 的对象）与其解析出的实际 segment 列表；
- **扰动**：`t`、`kick_mps`、kick 方向 seed、`num_steps`；**指标参数**：fall 阈值、`base_height_ratio`、dwell/sustain；
- `eval_protocol_digest`（协议 yaml 内容＋name＋version）与 `obs_protocol_digest`（3.1a 身份＋摘要）**分开命名**；
- **实际加载的 checkpoint**：在 `_make_policy`（`eval.py:174`）**加载时**哈希，记 `sha256`＋`size`＋`mtime`，加载后复核一致；不一致则该记录无效。**不得运行结束后重读同一路径**；
- **资产**：声明锁摘要 vs **实际文件校验结果**（pass/fail/unknown 三态）；
- **运行环境与代码来源（必需）**：lizard 代码 rev 与 IsaacLab rev、rsl_rl 版本、sim 版本、实际 `device`、实际 `num_envs`。**实际值与声明值分开字段**（env cfg 不能替代依赖版本与实际运行值）；来源不可确认时写 unknown，不得留空或从 env cfg 反推。

| 件 | 内容 | 类型 |
|---|---|---|
| 3.2a | `record.py`：格式定义＋读侧三态判定（legacy 未知 / 不完整 / 完整），对应 0c 四条规则 | 离线 |
| 3.2b | 写入实现：上述六类内容在 `eval.py` 的采集点（`_make_policy` 前、`gym.make` 后、rollout 前、`_persist`） | 离线 |
| 3.2c | **run 唯一性**：`run_id`（`eval.py:495`）加 variant 后缀；同 run_id 已存在且摘要不同 ⇒ **拒绝写入**，需显式覆盖。P04 四项替换各起独立 run，原始与替换证据都留 | 离线 |
| 3.2d | 不变性负测试：记录代码依赖检查（不得触碰 RNG）＋ 真跑同 seed 开关对拍。**前提写明**：逐位相同**要求基线自身可重复**，故须先做一次同 seed 无记录重复（`A/A`）；基线本身不可重复时结果记**未知**，不得归因于记录代码 | 离线＋真跑 |
| 3.2e | P04 反证：ckpt / suite / 资产 / 协议四项单项替换各改变对应摘要；可比／不可比／未知三态各有测试 | 离线 |
| 3.2f | 真跑一项：一个真实 eval run 落全六类内容并核验 | 真跑 |

**3.3 地形映射**。真实切分在仓外（`E:\IsaacLab\...\terrain_generator.py:243-247`），`sub_indices` 为局部变量、生成后即丢；课程侧两处**逐字复制**同一算法（`teacher_mdp.py:752-765`、`:1107-1121`），一致性证据只有 mock 上的硬编码期望，从不跑真生成器。

**证据路线**：探针**同时包装两个调用点**，零上游改动（**不打核心补丁**：不变量 6 上游补丁只减不增）。单个调用点不够用：

- **实际 `difficulty` 不在 `_add_sub_terrain` 的入参里**（`:264-266`、`:233` 传的是**原始** sub-terrain cfg）；真实值经 `_get_terrain_mesh`（`:342`），并在其内 `cfg = cfg.copy()`（`:360`）后才写入 `cfg.difficulty`（`:362`）；
- 故 `_get_terrain_mesh` 处捕获 `difficulty` 与 cfg 身份，`_add_sub_terrain` 处捕获 `sub_row`/`sub_col`，**按生成器实例配对**，并**验证配对关系**（每个 add 恰有一个 get、cfg 身份一致、无漏无重）；配对失败即拒绝，**不得从行号或循环序重新计算 `difficulty`**（重算还会消费同一 RNG 流）。

防自证的关键：探针**不算切分**，只读生成器当时传出的对象；声明侧独立算切分 —— 执行者不同，才有证据力。

| 件 | 内容 | 类型 |
|---|---|---|
| 3.3a | 可行性实验：headless 构造 `TerrainGenerator(build_param_grid_terrain_cfg(v14_grid))`（`terrain_preflight.py:59-71` 已证 sub-terrain 函数 headless 可调，列切分未验），并**同时验证两个调用点的关联采集**（generate 捕获 difficulty、add 捕获行列，按实例配对且对应关系成立）。**本项是闸口** | 离线，半小时级 |
| 3.3b | 唯一真源 `tasks/terrain_map.py`：切分算法＋列→combo 记录结构（含 levels→参数表摘要） | 离线 |
| 3.3c | 探针 `terrain_split_probe.py`：**调用时立即快照**（两调用点配对），记稳定 sub-terrain 标识、参数摘要、`sub_row`/`sub_col`、**实际** `difficulty`、生成模式；**不留对象引用**。健全性拒绝：未知对象、配对失败、漏格（行×列不齐）、重复格、列索引越界 | 离线（3.3a 通过）／真跑（3.3a 失败） |
| 3.3d | 课程消费记录：两处课程 `__init__` 改用记录，删重算；**必须等 3.3c 验证通过**（对齐本节 3.3 前置顺序），且带"副本不得重现"扫描闸门 | 离线 |
| 3.3e | 删测试副本：`test_v5_terrain_sir.py:87-94`、`test_joint_sir.py:129-141,157`、`test_resume_state.py:477-490`。同样**不得早于 3.3c** | 离线 |
| 3.3f | 必需负测试：同配方不同 seed，列映射同、几何不同 ⇒ 映射比对通过、产物比对必须失败。本轮产物比对未启用 ⇒ **记未知**，不得记通过 | — |

**模式区分**：curriculum 按列分配（`_generate_curriculum_terrains`），随机模式按格采样（`:228-233`）。**不得**把"列→combo"推广到所有生成模式；随机模式只记逐格采样，无列映射可声明。

**顺序纪律（本文件早先已定，先前"3.3d/e 可先做"的说法作废）**：接口与比对器可提前开发；**实际替换课程与删除旧独立测试，必须等探针验证通过**。否则生产代码与测试先共同转向一个未验证的计算源，又回到自证。

**几何一致性不在本轮**：要声明地形产物一致须归档并核验实际 mesh/heightfield，本轮不执行，记未知。映射一致不能替代几何证据。

**3.4 蒸馏与导出：本轮不执行**。待做两项：导出前协议校验（`teacher_networks.py:176-243` 的 flat/分段与 ONNX/JIT 路径）、蒸馏数据 manifest＋分片哈希（Phase 2 建）。P05/P06/P07 标"未执行"，不得通过。现存冲突记敞口：冻结 yaml `obs_layout` 与协议声明**不同形**（yaml 写 `joint_pos_rel`，代码 term 是 `joint_pos`），它是 UE 侧契约，本轮不动导出。

**挂账**：`PLAN.md` 新增一条（蒸馏数据与导出校验），`HARNESS.md` 挂账节新增一条（记录格式读者接线与真跑不变性对拍）。

**执行顺序**

1. 冻结身份表、历史目录表、记录格式、验收矩阵（0a–0d）；
2. 3.1 静态部分（a/a′/b/c/g/h）＋ 3.3a 可行性实验；
3. 3.2 记录实现（a/b/c）＋ 3.3 声明与探针（b/c 接口）；
4. 真跑验证：3.1e、3.1f、3.1d 实测、3.2d/f、3.3c 实际观测；
5. 课程迁移与删除副本：3.3d/e；
6. 全套回归与出口登记。

可并行：3.1c/e 与 3.2a/b 文件不重叠；3.3a 不阻塞 3.1。**本文件早先"3.3d/e 可先做"的说法作废。**

**两段出口（逐项引用件号；**必需项未知 ⇒ 该子项不得签收**，不得用"三态反证"之类概括替代具体件）**

- **3-离线段**：0a、0b、0c、0d 冻结；3.1a、3.1a′（含锚点变更须带 reason、门只报红）、3.1b、3.1c、3.1g、3.1h；3.2a、3.2b、3.2c（含 run 唯一性拒覆盖）＋ 3.2e 三态与四项替换反证；3.3a 结论、3.3b 真源、3.3c 健全性与配对反证。**未跑全量套件不得写"套件全绿"**；挂载行以 `offline_suite.py` 为准（未挂即记"入口待挂"，入口待挂 ⇒ 该项未通过）。
- **3-真跑段**：3.1d 实测接线、3.1e、3.1f、3.2d（**须先有同 seed 无记录的 A/A 可重复性**，否则记未知）、3.2e 的 **P04 真实替换实验**（ckpt/suite/资产/协议四项各一次，原记录与替换记录并存）、3.2f、3.3c 实际观测、3.3d 实际消费、**3.3e 删副本后的回归**（`test_v5_terrain_sir.py`、`test_joint_sir.py`、`test_resume_state.py` 全绿）、**P02（预处理/转换：基向量、裁剪边界、独立参考）与 P03（噪声与状态）按协议逐格状态** —— 全部按 0d 矩阵**逐格**登记，一格通过不覆盖另一格。

两段都**不得**称为"Step 3 全部通过"。几何一致性、导出校验、蒸馏明写**未执行/未知**。不得用真跑通过抵消离线红灯。

**冲突面**：不碰 `offline_suite.py`、`components.py`、`teacher_env_cfg.py`、`manifest.py`、`lifecycle.py`、`test_component_ownership.py`。3.3d 可能撞 B1 的 `terrain_generator` 写点，需先协调。开工前重查 `git status`，按 A0 的"写入集与脏集相交即拒"处理。`HARNESS.md`、`PLAN.md` 当前无人占用，可改。

**最可能一年后失效处**：① 锚点没人改就成废纸，故"改锚点须带 reason、门只报红"要写进门本身，不靠纪律；② 记录字段只增不改的诱惑会让 legacy 判读被滥用，故有 `record_format` 就必须满足其必需集，"不完整"不等于"未知"；③ 3.3 若长期停在探针，课程侧副本会悄悄长回第三份，故 3.3d 必须带"副本不得重现"的扫描闸门。

### Step 3 —— 验收清单

3.1 协议对象 / 3.2 eval 记录 / 3.3 地形映射 / 3.4 蒸馏与导出的设计与件号，一律以上一节"Step 3 施工件表与依赖"为准。旧版此处另有一套更早的 3.1–3.4 描述（含与施工件表冲突的 3.3 步骤 A/B 口径），两套并存即两套规格，已删。本节只留验收清单。

**3.5 协议、评估与导出验收清单**

每个待支持协议/转换/导出组合单独登记，不要求尚未启动的 student/蒸馏功能提前实现；未执行的组合不得标通过。测试输入及参考值冻结后再运行，不能由被测转换函数同时生成期望输出。

| 编号 | 样本与操作 | 通过标准及负测试 |
|---|---|---|
| P01 协议结构 | 当前每个受支持任务的实际 obs/action 布局，对照版本化协议 | 名称、顺序、维度、单位、坐标系、joint/foot 顺序、缩放/裁剪/归一化逐项对应；同维度交换两个 term/joint 或改协议摘要必须失败，不能只查 shape |
| P02 预处理/转换 | 每个输入维度的单独非零基向量、零输入、正负方向、裁剪边界内/等于/外，以及 seed=42 的 256 组有效范围样本 | 与独立手算/参考实现逐元素比较，float32 默认 atol=1e-6、rtol=1e-5；离散顺序与索引精确一致。交换坐标轴、缩放错误、缺必需输入必须失败；teacher/student 信息不可获取时拒绝转换，不用零值冒充 |
| P03 噪声与状态 | 固定噪声输入检验变换；冻结归一化统计。无噪声路径单测 | 检验范围、作用维度、裁剪顺序和归一化统计来源；不以相同 seed 假设不同引擎噪声相同。NaN/Inf 输入按协议明确拒绝或显式处理，不能静默通过 |
| P04 eval 绑定 | 一个真实评估 run，另对 ckpt/suite/资产分别做单项替换 | 记录四项绑定与覆盖后的实际配置；任一内容变化均改变对应摘要；比较规则对允许/禁止的差异各有一个测试，摘要不同不自动意味着不可比较 |
| P05 导出数值 | 同一 checkpoint 的确定性推理路径，冻结归一化；P02 样本加真实 rollout 前 256 帧（不足则样本不合格） | 原模型与实际目标 runtime 逐元素比较全部输出，float32 默认 atol=1e-5、rtol=1e-4，全部有限；记录 runtime/provider/opset/dtype 与最大误差。只通过 ONNX 图检查或只成功加载不算通过；缺 runtime 记未知 |
| P06 时序与动作 | 循环模型输入至少 128 步，包含中途 episode reset；检查隐藏状态输入/输出。再检查动作缩放、裁剪、关节映射 | 动作与隐藏状态每步满足 P05 容差，reset mask 生效；隐藏状态漏清零、动作顺序互换或缩放错误必须失败；非循环模型注明不适用。默认比较确定性动作，不比较独立随机采样动作 |
| P07 蒸馏数据（启用时） | 至少一个真实采集分片，绑定 teacher checkpoint、采集/预处理协议与分片清单 | 每个分片摘要、条数、shape/dtype 与 episode 边界吻合；删分片、篡改数据或换预处理协议必须失败；数据可取回性另按归档证据判定 |

P02/P05 容差仅适用于这里声明的 float32 路径；其它精度或后端须在执行前单独约定，不能沿用“已通过”标签。数值对齐证明限定样本上的计算一致，不替代部署安全性、闭环控制效果或训练效果验证。

## 数据版本 §0.2（决定训练输入与实验条件的非代码内容）

| 对象 | 版本包含 | 方式 |
|---|---|---|
| 实验配方 | YAML 参数、组件选择、协议引用 | Git；发布后冻结 |
| 机器人资产 | USD 及引用的 mesh、材质、物理配置 | 文件清单 + 每文件哈希 + 可取回归档；**按依赖闭包**（只哈希顶层 USD ⇒ 引用文件可悄悄变） |
| 地形输入 | 生成参数、生成器代码版本、seed；必要时存实际结果 | 配方版本 + 生成结果清单 |
| 评估数据 | suite、命令序列、扰动、种子集合 | 独立协议版本 + 内容摘要 |
| 蒸馏数据 | teacher ckpt、采集配置、轨迹分片、预处理协议 | 数据集 manifest + 分片哈希 |
| checkpoint | 模型与训练状态 | 保存后算文件哈希，绑定来源 run |

**两层标识**：语义版本（`lizard_v15`、`lizard_anatomy_v3`）+ 内容摘要（manifest SHA-256）；同名内容变 ⇒ 摘要必变。

```yaml
recipe: {id: lizard_v15, sha256: "..."}
asset: {id: lizard_anatomy_v3, manifest_sha256: "...", archive_uri: "..."}
terrain: {config_sha256: "...", generator_code_revision: "...", seed: 42}
observation_protocol: {id: teacher_obs_v3, sha256: "..."}
```

**四条落地规则**：① 发布过的版本不可覆盖，改动即新修订，旧版仍可取回；② 小文件进 Git（清单也进），大文件进持久化归档，无需新平台；③ 哈希证明"是不是同一份"，归档地址解决"能不能找回来"；④ 生成数据按保证需求保存。

**归档位置：仓内 git（2026-09-15 用户改判；原判"暂不设"已被演练触发）**。演练按需取材时，凡 rev 取不回的内容（脏树 diff、代码根内未跟踪文件）写入 `rl_exp/archive/<run_id>/` 并提交——小文件进 Git，随仓库一起可取回，不需要另建 NAS / 对象存储 / 工单附件。未跟踪内容若既无归档落点又不进仓，取材**直接拒采**（`rebuild.py` 硬门），因此"哈希齐了却取不回"不会被当成可重建。仍依赖外部可取回的项（如 3.3 地形产物一致）继续**一律标未知**。

**这是引用清单，不是参数源**：参数仍从配方读，启动时核验实际读取内容再记摘要。

## 不做

搬目录 · 合并 teacher 与家族基类 · 序列化 PhysX 内部状态 / 逐位复现 RNG · 给每个实验复制代码树 · 新造训练框架 · 文件哈希写进文件自身 · `--verify` 失败降级为 WARN · 运行时结构塞进离线 cfg 快照 · 用当前工作树补造历史事实 · 把 manifest 当参数源 · 给每一步仿真数据做版本。

## 附录 · 已删内容去向

早期版本在此存"现状与证据（file:line）"表与逐版修订留痕（v0.1–v0.21）。两者已删：前者与 `FILEMAP.md` / `FAMILY.md` 重复且已与代码分叉，后者与 `git log` 重复。

- **现状**（哪个文件、哪些写入点）→ `FILEMAP.md`、`FAMILY.md`
- **判据怎么来的**（决策来源：用户明示 / 审核建议 / 提案选择）→ `git log -p ARCH_PLAN.md`
- **做到哪一步** → `versions/lizard/ACCEPTANCE.md`
