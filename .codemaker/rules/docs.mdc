---
description: 文件地图写作纪律——FILEMAP.md 条目只写"是什么/何时用"的最小判别信息，溯源与行为细节不入图。当用户提到"FILEMAP"、"文件地图"、"入图"、"新文件加进地图"、"改 FILEMAP 条目"、"文件地图瘦身"时使用。
globs: FILEMAP.md
alwaysApply: false
---

# FILEMAP 写作纪律

FILEMAP 条目 = 给下一个 AI 的路由信息：这文件是啥、何时选它。
只写让读者**找到并选中**该文件所需的最小判别信息。

**留**：是什么、核心用途、关键开关/输出（选它而不是邻居工具的判别点）。

**不入图**：

- **溯源**（"改名重写自 X"、"吸收自 Y"、"从 Z 迁来"）——归 git log。
  图里写 = 工具合并/演进完成后永不过期地占位，只增不删。
- **行为细节**（内部机制、打印节奏、逐步追踪过程）——归脚本 docstring。
  图管路由，不管实现。
- 别处已有 SSOT 的事实（版本号、参数值、路径）——留指针，不复制。

新文件入图：一行条目，与同节邻居条目同粒度，先看邻居风格再动笔。
