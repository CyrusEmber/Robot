---
description: 版本纪律——配方版本（vN）新建/冻结与计划文档修订（vN.M.K）的触发条件、权限与记录规范。当用户提到"开新版本"、"新建版本"、"升版本"、"冻结版本"、"修订"、"勘误"、"修订记录"、"记录修改"、"开新支线"、"换家族"、"新机器人"时，或操作涉及 versions/ 目录、PLAN.md、FAMILY.md、NOTES.md 时使用。改 reward/obs/action/终止 term、DR 范围、地形参数（多在 rl_exp/tasks/）同样适用——先过 A 节升版触发条件。
globs: rl_exp/versions/**, rl_exp/tasks/**, rl_exp/tools/pipeline/**
alwaysApply: false
---

# Versioning（版本纪律）

两级版本，不可混用：

| 级 | 形态 | 例 |
|---|---|---|
| 配方版本 | `versions/<family>/<line>/vN/` 目录 + 任务 id + git tag | v0 / v1 / v2 / v3 |
| 文档修订 | 版本目录内 PLAN.md 的修订记录行号 | v3.1 / v3.1.2 |

分层原则：**规则管行为（何时升版、怎么记录、升版流程），FAMILY.md 管事实（任务表/版本史/家族特有机制）**——会随版本增长的数据一律留在文档侧。本规则正文只写现在仍成立的句子：日期、事故名（如"vX 欠账"）、条目计数、示例版本号一律不进规则——理由与出处归 FAMILY/FILEMAP。

**家族文档地图与时态判据**：FAMILY.md = 已成立的事实（现在时：任务表/版本史/当前状态/机体几何——"待/未/若"字头禁止入住，最多留挂账号指针）；PLAN.md = 意图（将来时：路线/备选路径/挂账）；契约面各有家族级 SSOT（obs 布局 = OBS.md，奖励语义 = REWARDS.md）；版本方案 = `vN\PLAN.md`；结果 = `vN\NOTES.md`；命令与摆位 = 仓根 README。判别法：一句话明天还必然为真 → 事实侧；靠行动才兑现 → 意图侧。同一事实不得两侧同时为真（意图兑现的瞬间 = 在 PLAN 关闭、在 FAMILY 出生）。

**仓根层（跨家族的重整工作）**：`ARCH_PLAN.md` = 意图（要做的事 + 判据 + 不做），其**验收与推进状态唯一归属 = `versions/<family>/ACCEPTANCE.md`**。故 `ARCH_PLAN.md` 的标题/表头/出口列不得出现"已完成/已落地/待实现"，正文只写当时证据与实现形态；**通过率与计数（`[15]` 27/27、"11/11 绿"、"34/34 一致"）只进 ACCEPTANCE**，`FAMILY.md`/`PLAN.md`/`FILEMAP.md`/`HARNESS.md` 写覆盖面而不写通过数。挂账行里的优先级记号（🟡/🟢/🟠）是**意图**，可留。同理，**规则正文里的路径/形态必须能被磁盘验证**——规则写错比文档写错更贵（会被当规范执行）。本条的机器看守 = `check_version_docs.py`（扫这四份文档里的闸门判词；**比率不扫**——`90/208/83` 是 obs 宽度、`400/20` 是 PD 增益，不是通过数，拿比率当模式会误报；只带比率不带判词的读数仍靠 review）。同一闸另扫 `ARCH_PLAN.md` 的**加粗**状态词（`**已完成**`/`**已落地**`/`**待实现**`）——首段引规则那句不加粗，故不触发。

**家族文档路径约定**：每家族一份 `FAMILY.md`，新家族放 `versions/<family>/FAMILY.md`（`versions/**` glob 自动覆盖，本规则无需改）。

**范围边界**：家族外子系统不进本规则与家族文档——各自 SSOT 文档自治（评测台 = `ablation_harness/HARNESS.md`，含升 `versions/` 目录化的触发条件）。

## A. 新建配方版本（vN → vN+1，家族内）

**触发（满足其一）**：obs / reward / action / 终止的 term 结构变更；DR 范围或超参变更（yaml 数值也算）；**地形资产变更**（terrain 生成器、障碍参数、缩放）。换 seed **不触发**（NOTES 记 seed 即可）。纯参数变更开 vN+1 但 spec 集合不变时，不新增任务 id。

**前置门**：上述触发仅对**已冻结**版本生效——冻结后再改配方 = 开 vN+1。**未冻结的版本（提案态 / 已撤 tag 解冻且未训练）改方案不开新版本**：走 B 节 vN.M 修订，重新冻结时重打 tag。用户明示开 vN+1 不受此门限制。

**状态机（冻结与训练启动是两个独立锚点，分开管）**：

```
提案 ──冻结──► 已冻结未训练 ──启动训练──► 已训
  ▲              │
  └── 撤 tag ────┘   唯一可逆段；vN.M 修订只发生在提案/解冻态
```

- **冻结（tag）**= 训练候选锚：preflight/预检验证的对象，可逆（未训练时撤 tag 回提案态）。
- **训练启动**= 结果归属锚：run/checkpoint/eval 全绑在实际跑过的配方上，**不可逆**——已训版本没有"解冻"一说。
- **已训版本改配方 = 开 vN+1，无解冻豁免**；撤已训版本的 tag 禁止（tag 是结果复现锚，撤了丢归因）。
- **启动训练时 tag 不存在 → 视同冻结**：立即补打 tag（堵"忘打 tag 就开训"）。
- **启动训练前工作树必须干净**：脏树默认**硬拒**（`run_manifest.begin` 直接终止训练进程，T0 已落盘留证），因为没有任何 rev 能还原它、该 run 的"可重建"永远无法证明。确需在脏树上开训必须**显式声明理由**：`RL_ALLOW_DIRTY_TREE="<为什么>"`，理由写进 manifest（记为偏差，不是免检）。IsaacLab 树带未提交的 fork 补丁属**预期状态**，只记录不阻拦——否则每次开训都被拦，等于教人把覆盖开关当习惯。

**越级（不走本节）**：机器人本体/构型变更 = **换家族**——机器人即家族身份，新开 `versions/<new_family>/v1/` + 新 FAMILY.md（只写家族事实，升版流程从本规则继承，不复制）+ 全套注册。同构型的资产换代仍是家族内事务（asset_lock 哈希看守 + 有意换代同 commit `--update-locks`，见家族文档挂账），影响配方行为时走本节升版。

**分线（新计数器）**：配方无上游的全新算法不开 vN+1，开 `versions/<family>/<line>/v1/` 独立计数。支线/主线 = 算法区别：机器人/assets/eval 全家族共用，除独立版本链外一切照本节规则。判据：依赖在 = 线内 vN+1（哪怕 diff 巨大）；无上游 = 新支线（v1 PLAN 全量式）。**新线（新支线或新家族）必须同时在该家族的 `versions/lines.json` 落一条生命周期记录**（`status` 及退休字段）——发现到一条线却没有它的记录是**拒绝项**，不是默认 `active`（闸门 `check_recipe_registry.py` 看守；线与记录的句柄同源，见 `recipe_lines.py`）。

**流程（家族无关，通用五步）**：

1. `copy versions/<family>/<line>/vN versions/<family>/<line>/vN+1`（含 yaml），改新目录参数；
   **copy 后重置正文**：`NOTES.md` 只留骨架、`PLAN.md` 按本版重写——继承上版的差异段 / 结果表 /
   修订历史就是给自己造第二份会失真的副本，而完整差异只有 `diff.json` 一份；
   runner cfg 逐版本化：`experiment_name = <family>_<场景>_vN`（一版本一日志目录，
   禁共用；存量目录不迁移）。**新线**（无上游母本可 copy）另做两件：`versions/lines.json`
   落生命周期记录（见本节分线条款）；`cfg_lock --update --line <线> --reason "<why>"`
   建该线自己的配方锁（**一条线一个锁文件**，落点 = **线根目录**，即
   `versions/<family>/<line>/cfg_lock.json`，与线 dev yaml 同级、不在 `vN/` 内；
   `main` 也有自己的线根，不是家族根特例；框架组合块全仓共用 `versions/cfg_baselines.json`）
2. 写 `versions/<family>/<line>/vN+1/PLAN.md` 与 `NOTES.md`，并落 `base.json`（`{"base": "vN", "note": ""}`，血统根用 `null`）。base = 改配方那天眼红的**唯一母本快照**；对其他版本的决策引用只进 PLAN 散文，不进 base.json（一个 run 只有一棵代码树）。重基（废案迁移到新母本）= 改 base.json 的 base 字段并在 note 记一笔。base 按家族相对路径解析：主线写 `vN`；支线内裸 `vN` 先命中本线（parkour 的 v2 写 `v1` 指 `parkour/v1`，绝不落主线），跨线引用写全路径。**PLAN 每版必写**（变更少简短即可）：多变量设计（新 obs 布局 / 奖励包 / DR 协议 / 新支线 v1）用完整版——提案态载体，评审与决策记录所在，含修订记录节，冻结前定稿；单变量小改（一个 term 增删或 yaml 数值档，不涉 obs 布局与多 term 权衡）用**简短骨架**：目的/假设（≤3 行）/ 差异清单（指向 `diff.json`，不复述逐项路径）/ 验收 / 启动方案（计划命令）。NOTES **必含骨架**：设计的引用（目的/假设见 PLAN，不复制判据）/ **本版重点变化（≤2 句阅读提示，不作完整性承诺）**+ 差异声明链接（完整差异与逐项理由以 `diff.json` 为准）/ 实际执行与偏离（计划命令归 PLAN；实跑必须写 **run 目录路径** `logs/rsl_rl/<experiment_name>/<时间戳>`——同一 `experiment_name` 下会有多次 run，缺时间戳指向的是目录、不是那一次；命令行、覆盖参数、seed、checkpoint 的正文留在该目录的 `run_manifest.json` / `checkpoints.json`，**记录本体机器本地、不进仓**（IsaacLab 根由 `paths.yaml` 解析），所以引用给的是**路径 + 复读命令**：不承诺仓内可点、也不复述内容；该次 run 的记录若不完整（`manifest --verify` 报 blocking），在 NOTES 写明缺什么、结果凭什么锚住，不假装它齐）。**数值不复述**：读数只有一个家 = `acceptance/records/`，这里放的是 run 路径 + 复读命令 + 报告路径 + 一句判定（见 `AGENTS.md` §Where the work lives 的"只有一个家"条）/ 结果回填 / 结论
3. 代码级结构变更（新 term）走版本差异机制 + 新任务 id 常驻注册（机制各家族自定义，见其 FAMILY.md）；纯参数变更（yaml-only）不需要新任务 id。**新任务 id 必须配 golden**：`cfg_lock --update --line <线> --reason "<what moved and why>"`（漏 `--line` 拒写——一次全量更新会把别条线的漂移并进同一条理由；漏 `--reason` 也拒写且不落盘）。**归属必须声明**：任务 cfg 类上挂 `params_line`（`ClassVar`，不进配方快照），线句柄 = 家族相对路径（`lizard` / `lizard/<line>`）；缺声明、指向不存在的线、或该线无锁文件，一律红。**冒烟脚本同理**：运行时契约核对集中在 `tools/verify/teacher_smoke_runner.py`，新版本 = 给其 `SMOKE_SPEC` 加一行 + 建薄壳 `teacher_smoke_vN.py`（调用 `main("vN")`）；**复制整份冒烟脚本造新版本 = 违规**（复制件无闸门，漏改一条断言永久静默）。表行与上一版逐字段人审，并按该版特有的一条断言做破坏测试确认咬得住。
4. 训练 → 曲线导出（`rl_exp/tools/trainlog/dump_tb.py`）→ eval（`ablation_harness/eval.py`）→ 结果回填 NOTES.md
5. 家族文档版本历史表加一行；git 侧 commit + tag `<family>-vN+1`（tag = 整树快照；commit/push 细节走 git-auto-sync skill）

**机器闸门**：第 2/5 步可量化项（每版本目录四件套 PLAN/NOTES/yaml/asset_lock 锁自身 + **base.json 血统边合法** + FAMILY 版本史行）由 `rl_exp\tools\verify\check_version_docs.py` 看守——`run_offline_checks.bat` + pre-commit 均跑，漏记录与 DR 漂移同级红；tag 缺失仅 WARN（历史前缀可不统一）。线身份与生命周期另由 `check_recipe_registry.py` 看守（**发现到的线必须有记录**、退休须带 `retired_at` 与非空 `reason`、`successor` 必须存在且为 active 的另一条线；闸门**不读时钟**——退休是目录修订，不是日期到期），配方 golden 由 `check_cfg_lock.py` 看守（**一条线一个锁文件**、`--update` 必须带 `--line` 与 `--reason`、任务必须有 `params_line`），四份文件约定（yaml 命名 / 线目录 / 锁落点）由 `recipe_lines.py` 单点供应。纯约定没闸门 = 必然欠账。

**红线**：已发布 term 的实现永不改语义（新版本只允许加 term）；冻结目录只读；**配方锁正文（`versions/**/cfg_lock.json`）是闸门产物、不是阅读对象**——取用与判定只走 `check_cfg_lock.py` 的输出（`--diff` / `--vs-upstream` / `--self-test`），改动只走 `--update --line <线> --reason`；逐字读正文、手改正文、把正文抄进文档或事项都算绕开闸门（结论与实测归 evidence 记录，事项只留落点引用）。

## B. 修订既有版本（文档级 vN.M.K）

**权限前提**：该版本配方处于可修订态——提案（未冻结），或**已冻结未训练**且用户明示撤 tag 解冻（解冻是用户决策，记入修订记录）。**训练已启动**的版本：方案永久冻结，只允许偏差声明与结果回填；要改方案 = 开新版本（走 A），**无解冻路径**（撤 tag 对已训版本无效且禁止——tag 是结果复现锚，见 A 节状态机）。

**修订号规则**：

- `vN` 初稿：首次生成。
- `vN.M`（M 递增）：内容修订批次——review 修复、拍板决策、需求变更，改的是**方案本身**。
- `vN.M.K`（K 递增）：记录性修订——合入/迁移、勘误撤回、格式修正，**不改方案实质**。

**每次修订必做**：

1. 修订记录（§10 或等价节）追加一行：`| 日期 | 版本 | 变更 + 原因 + 依据（论文节号 / 文件:行号 / 用户拍板） |`。勘误与撤回必须写明原委，禁止无痕覆盖。
2. 文档头部"修订："行同步最新版本号。
3. 决策来源分级标记：`用户拍板：<日期>`（用户明示）与 `review 定案`（AI 推断获默认通过）不可混写。
4. **单一真身守卫**：动笔前确认目标文件是唯一权威副本（git status 无同名未跟踪分叉、无旧路径残留）；发现分叉先合并再删副本，禁止在陈旧副本上继续修订。
5. **修订号 ↔ commit 绑定**：commit message 以版本号开头（初稿 `vN:`，修订 `vN.M.K:`）；commit 闭环/孤儿/amend 纪律走 git-auto-sync skill。
6. **差异集合变了 ⇒ 改声明，不改摘要**：修订动了 `diff.json` 的 allowed 集合，或动了 `base.json` 的比较基线时，改的是 `diff.json`；变化的**含义**改向（同一路径上方向或幅度反转）还要人工复核 `why`、`base.json.note` 与 NOTES 的重点摘要——闸门只比对条数与路径，`why` 的语义漂移无自动判据。历史版本已写下的漂移段**豁免，不清零**（冻结记录不改写）。

**当前计划同步——状态跃迁才做，内容修订不做**：`versions/<family>/PLAN.md` 头部更新行与 `FAMILY.md` 当前状态记的是**状态**（活跃版本/提案中/训练中），不是方案内容（`FILEMAP.md` 的逐版本行已于 2026-09-23 删除，理由见该文件对应节的说明）。方案细节修订（vN.M.K）不动这两处；只在状态跃迁时同步：

| 事件 | 同步两处 |
|---|---|
| vN 初稿建立 / 冻结 / 训练启动 / 结果回填 | ✅ |
| vN.M / vN.M.K 修订 | ❌（状态行失真时顺手修，如"部分待拍板"已不真） |

`FAMILY.md` 当前状态**保持粗粒度**：一句摘要 + 指向 `versions/<family>/PLAN.md`，方案细节永不进 FAMILY——SSOT 唯一 = 版本目录内文档，索引失真可容忍，方案失真不可。
