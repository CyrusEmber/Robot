---
description: 版本纪律——配方版本（vN）新建/冻结与计划文档修订（vN.M.K）的触发条件、权限与记录规范。当用户提到"开新版本"、"新建版本"、"升版本"、"冻结版本"、"修订"、"勘误"、"修订记录"、"记录修改"、"换家族"、"新机器人"时，或操作涉及 versions/ 目录、PLAN.md、FAMILY.md、NOTES.md 时使用。
globs: rl_exp/versions/**, rl_exp/PLAN.md, rl_exp/FAMILY.md
alwaysApply: false
---

# Versioning（版本纪律）

两级版本，不可混用：

| 级 | 形态 | 例 |
|---|---|---|
| 配方版本 | `versions/<family>/vN/` 目录 + 任务 id + git tag | v0 / v1 / v2 / v3 |
| 文档修订 | 版本目录内 PLAN.md 的修订记录行号 | v3.1 / v3.1.2 |

分层原则：**规则管行为（何时升版、怎么记录、升版流程），FAMILY.md 管事实（任务表/版本史/家族特有机制）**——会随版本增长的数据一律留在文档侧。

**范围边界**：家族外自有子系统（`ablation_harness/` 评测台）的工作不进家族配方计划与家族文档——基础设施待办挂仓根 `PLAN.md` 挂账清单，协议变更走冻结 yaml（`protocols/locomotion_eval_v1.yaml` 模式）；该子系统代码高频变更、多机器人共用或评测协议 v2 出现时，才升 `versions/harness/vN/` 管理（升级触发写在挂账行，防"永远不升"）。

**家族文档路径约定**：每家族一份 `FAMILY.md`，新家族放 `versions/<family>/FAMILY.md`（`versions/**` glob 自动覆盖，本规则无需改）。历史例外：lizard 在 `rl_exp/FAMILY.md`，引用已稳定，不迁移。

**范围边界**：家族外自有子系统（`ablation_harness/` 评测台）不进家族配方计划与家族文档——待办与版本记录挂 `ablation_harness/HARNESS.md`（自有版本文档 SSOT，修订沿用本规则 B 节纪律、编号独立于家族配方版本；仓根 `PLAN.md` 挂账只留指针），协议变更走冻结 yaml（`protocols/locomotion_eval_v1.yaml` 模式）；该子系统高频变更、多机器人共用或评测协议 v2 出现时，再议升 `versions/harness/vN/` 目录化（升级触发写在 HARNESS.md，防"永远不升"）。

## A. 新建配方版本（vN → vN+1，家族内）

**触发（满足其一）**：obs / reward / action / 终止的 term 结构变更；DR 范围或超参变更（yaml 数值也算）；**地形资产变更**（terrain 生成器、障碍参数、缩放）。换 seed **不触发**（NOTES 记 seed 即可）。纯参数变更开 vN+1 但 spec 集合不变时，不新增任务 id。

**越级（不走本节）**：机器人本体/构型变更 = **换家族**——机器人即家族身份，新开 `versions/<new_family>/v1/` + 新 FAMILY.md（只写家族事实，升版流程从本规则继承，不复制）+ 全套注册，不是 lizard vN+1。同构型的资产换代仍是家族内事务（asset_lock 哈希看守 + 有意换代同 commit `--update-locks`，见家族文档挂账），影响配方行为时走本节升版。

**流程（家族无关，通用五步）**：

1. `copy versions/<family>/vN versions/<family>/vN+1`（含 yaml），改新目录参数
2. 写 `versions/<family>/vN+1/NOTES.md`，**必含骨架**：目的 / 假设 / 相对上版 diff / 训练命令 / 结果回填 / 结论
3. 代码级结构变更（新 term）走版本差异机制 + 新任务 id 常驻注册——机制家族各异，lizard 实例 = `TEACHER_PRIVILEGED_SPEC` 剥离结构（FAMILY.md「版本差异结构」段），**禁止修改任何已发布 term 的实现**（破坏所有旧版本复现）；纯参数变更（yaml-only）不需要新任务 id
4. 训练 → 曲线导出（`tools/trainlog/dump_tb.py`）→ eval（`ablation_harness/eval.py`）→ 结果回填 NOTES.md
5. 家族文档版本历史表加一行；git 侧 commit + tag `<family>-vN+1` + push（tag = 整树快照兜底；**带家族前缀防多家族撞名**——历史裸 tag `v1`/`v2` 不迁移，新家族从 `<family>-v1` 起）

**红线**：已发布 term 的实现永不改语义（新版本只允许加 term）；冻结目录只读。

## B. 修订既有版本（文档级 vN.M.K）

**权限前提**：该版本配方尚未训练启动（版本目录内 PLAN.md 头部状态 = 提案，待确认）。训练已启动的版本：方案冻结，只允许偏差声明与结果回填；要改方案 = 开新版本（走 A）。

**修订号规则**：

- `vN` 初稿：首次生成。
- `vN.M`（M 递增）：内容修订批次——review 修复、拍板决策、需求变更，改的是**方案本身**。
- `vN.M.K`（K 递增）：记录性修订——合入/迁移、勘误撤回、格式修正，**不改方案实质**。

**每次修订必做**：

1. 修订记录（§10 或等价节）追加一行：`| 日期 | 版本 | 变更 + 原因 + 依据（论文节号 / 文件:行号 / 用户拍板） |`。勘误与撤回必须写明原委，禁止无痕覆盖。
2. 文档头部"修订："行同步最新版本号。
3. 决策来源分级标记：`用户拍板：<日期>`（用户明示）与 `review 定案`（AI 推断获默认通过）不可混写。
4. **单一真身守卫**：动笔前确认目标文件是唯一权威副本（git status 无同名未跟踪分叉、无旧路径残留）；发现分叉先合并再删副本，禁止在陈旧副本上继续修订。
5. 修订闭环 = commit（git-auto-sync 纪律）：新建文档必须 git add，杜绝未提交孤儿。
6. **修订号 ↔ commit 绑定**：修订闭环 commit 的 message 必以版本号开头（初稿 `vN: ...`，修订 `vN.M.K: ...`），双向查询 = `git log --grep`；hash 不写进文档（落地时未知名，避免回填流程）。历史欠账不 amend 已推提交，`git log --follow` 考古兜底。

**当前计划同步——状态跃迁才做，内容修订不做**：根 `PLAN.md` 头部更新行、`FAMILY.md` 当前状态、`FILEMAP.md` versions 行记的是**状态**（活跃版本/提案中/训练中），不是方案内容。方案细节修订（vN.M.K）不动这三处；只在状态跃迁时同步：

| 事件 | 同步三处 |
|---|---|
| vN 初稿建立 / 冻结 / 训练启动 / 结果回填 | ✅ |
| vN.M / vN.M.K 修订 | ❌（状态行失真时顺手修，如"部分待拍板"已不真） |

`FAMILY.md` 当前状态**保持粗粒度**：一句摘要 + 指向版本目录 PLAN.md，方案细节永不进 FAMILY——SSOT 唯一 = 版本目录内文档，索引失真可容忍，方案失真不可。
