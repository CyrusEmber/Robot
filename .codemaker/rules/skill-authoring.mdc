---
description: Skill 写作纪律——SKILL.md 的节级审查与分层归属（方法/契约/硬知识留，实例/状态/重复走）。当用户提到"写 skill"、"新建 skill"、"改 skill"、"skill 瘦身"、"skill 太复杂"、"审查 skill"、"清理 skill"时，或操作涉及 .codemaker/skills/ 时使用。
globs: .codemaker/skills/**
alwaysApply: false
---

# Skill Authoring（skill 写作纪律）

skill = 给 AI 的行为补丁：载入只为改变某域的干活方式。审查单位是**节**，
不是 token——实例词清干净 ≠ 结构没问题。

## 每节必问：删掉这节，AI 干活会变差吗？

**留**：

- **方法**——决策规则、流程、判据（删了 AI 不会做或做错）
- **契约**——红线与不变量（"绝不 / 必须 / 升版才能改"）
- **框架硬知识**——平台已验证事实（API 坑、语义），注明"源码验证 / 实测"
- **一行实例指针**——"本项目 X → 路径"，全 skill 唯一实例入口

**删或搬**：

- **状态 / 历史 / 挂账** → 各子系统 SSOT（HARNESS.md、版本 NOTES），skill 留指针
- **抄来的数值** → 协议 / 参数 yaml 才是真源，复制处即漂移处；留语义注记不留数字
- **重复** → 同一事实的第二个家；多处出现留最自然的一个
- **机器人 / 项目实例** → 占位符 + FAMILY.md 指针

## 结构基线

- frontmatter description = 路由层（触发词可实例化）；正文零实例事实
- 命令示例用占位符；已部署代码字段名照抄（改文档不改代码）
- references/ = 硬知识 / 实例参考领地，SKILL.md 内声明其用途
- 连续注释块超过 ~10 行 = 疑似在复述正文，回头查

## 审查时机

- 新写：成稿后逐节过表
- 改动：只审被改的节 + 它引用的节
- 批量清理：**禁止 token 驱动**（findstr 实例词 ≠ 审查），必须逐节过表
