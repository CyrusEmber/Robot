---
id: deployment-delay-injection-dr
title: 延迟注入 DR（UE 部署前，PLAN #6）
scope: rl_exp/tasks, rl_exp/versions/lizard
status: blocked
landing: rl_exp/tasks/components.py, rl_exp/versions/lizard/OBS.md
next: 先定落点再实施（**待决，由用户拍板**）：仿真训练侧（DR 事件 / obs 流水线，要进冻结快照与声明面语义）还是 UE 部署侧（不改训练配方，验收看导出契约）。仓内当前**零实现**（全仓检索无延迟注入代码），所以第一步是拍板范围，不是写代码。落点定案后按该侧实施：训练侧 = 采样一段延迟并施加，参数进声明面；部署侧 = 在导出链路上实现并证明不影响既有导出。另：parkour 线的"相机延迟随机化"同源但归那条线，不在本项
close_when: 落点定案后执行者实施并留一次观测：训练侧 = 同一 task 带延迟注入跑一条短 run，run 的声明 / 快照里能看到延迟参数且训练能起步；部署侧 = 导出链路仍过导出前协议校验。观测成立即关；落点仍未定或观测不成立 ⇒ 保持 blocked 并写明挡在哪一步
---

## 当前状态

挂账只有一行意图（EP 的延迟注入技巧），仓内既无实现也无落点：`rl_exp/tasks` 全量检索没有延迟/时延相关代码，方案文档里它只出现在路线参考行与 parkour 的相机延迟那一条旁边。

## 为什么它是 blocked 而不是 open

它不是"知道要做、只差动手"：训练侧与部署侧的验收条件不同（前者要进冻结快照语义，后者要不动训练配方），**先拍板落点才有可执行的下一步**。在落点定案前写实施步骤只会写出一份将来被推翻的方案。
